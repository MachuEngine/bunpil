# 분필(Bunpil) 평가(Eval) 문서

`evals/eval_exam.py`가 측정하는 지표, 골든셋 현황, 실행 방법, 결과 이력을 모아둔 참고 문서.

> **압축된 요약이 필요하면 [EVAL_SUMMARY.md](./EVAL_SUMMARY.md)를 먼저 읽을 것** — 런타임/
> 오프라인 평가 구분, 지표 의미, 개선 스토리(before→after)를 한 문서로 정리해뒀다. 이
> 문서(EVAL.md)는 그 요약이 근거로 삼는 회차별 raw 로그·시행착오 전체 기록이다.

> **2026-08-03 생기부 모듈 제거**: `evals/eval_record.py`와 VIOLATION/HALLUCINATION 골든셋도 함께
> 삭제됐다(사유는 14절). **아래 4·5절의 과거 회차 기록은 지우지 않고 그대로 둔다** — 그 시점의
> 의사결정 이력이기 때문. 현재 측정 대상이 아님에 유의.
모델 교체·프롬프트 튜닝 등 평가에 영향을 주는 변경이 있을 때마다 [결과 이력](#4-결과-이력)에 행을 추가한다.

> **2026-07-12부터**: 문항 품질 Judge·구조 유사도 Judge·RAG 품질(Faithfulness/Answer
> Relevancy) 3개 지표는 실행할 때마다 [LangSmith Experiments](https://smith.langchain.com)에도
> 자동 기록된다(9절 참고) — **최신 수치·회차별 비교는 LangSmith Experiments 탭이 더
> 정확한 소스**(매 실행 자동 기록, 사람이 옮겨적을 때 생기는 오류 없음). 이 문서(EVAL.md)는
> 그 3개 지표의 세세한 회차별 로그를 계속 남기기보다, **주요 마일스톤·시행착오·의사결정
> 기록용으로 씀** — "왜 이 프롬프트를 이렇게 바꿨는지", "이 방향을 시도했다가 왜 롤백했는지"
> 같은 서사는 LangSmith에 안 남기 때문. 과거에 이미 적어둔 회차별 기록은 지우지 않고
> 그대로 둔다(연동 이전 히스토리 보존용). 나머지 3개 지표(Recall@5, PII 마스킹, 키워드
> 사실추가율)는 결정론적 함수라 LangSmith Experiments에 연동하지 않았고, 계속 이 문서에
> 직접 기록한다.

## 1. 평가 항목 전체 목록

### 출제 모듈 (`eval_exam.py`)

| 계층 | 지표 | 방식 | 기준 |
|---|---|---|---|
| 검색 | Recall@5 | 함수 | ≥ 0.8 |
| 검색 | MRR | 함수 | 참고값 |
| 문항 품질 | 정답유일성·오답매력도·근거성 | LLM Judge | 평균 ≥ 4.0 |
| Judge 신뢰도 | Cohen's kappa | 사람 라벨 비교 | ≥ 0.4 |
| Judge 신뢰도 | ±1 일치율 | 사람 라벨 비교 | ≥ 0.7 |
| 구조 유사도 Judge 신뢰도 | difficulty 일치율, overall MAE (LLM Judge) + 문항 개수 일치(코드) | 사람 라벨(STRUCTURE_GOLDEN) 비교 + `len(draft_items)==num_items` | 참고값 (별도 pass/fail 게이트 없음 — overall 이진 kappa 게이트는 2026-07-24 폐기 결정, 6절 참고) |

> 2026.07 passage_text 리디자인으로 "세트 제약(유형/난이도/커버리지/중복률)" 함수 검증은 폐기되고 위 "구조 유사도 Judge 신뢰도"로 대체됨 (`check_duplicate`/`past_exams` 제거에 따름).
>
> **2026-07-09 count_match 개념 폐기**: "생성 개수가 예시 문제 개수와 일치해야 한다"는 전제 자체가 틀렸음이 발견됨 — 실제로는 개수가 예시와 무관하게 `ExamSpec.num_items`(명시 없으면 기본 5)로 별도 지정된다. count_match는 이제 LLM Judge/사람 라벨 대상이 아니라 `validate_node`가 `len(draft_items)==num_items`로 직접 계산한다. `STRUCTURE_GOLDEN`의 `human_label`·`eval_structure_judge()`·`similarity_judge` 도구 시그니처에서 count_match 전면 제거(자세한 내용은 `data/golden/structure_golden.json`의 `_schema.count_match_deprecated`, `bunpil_roadmap.md` 참고).

### 입력 PII 마스킹 (`tests/test_masker.py`)

| 지표 | 방식 | 기준 |
|---|---|---|
| PII 마스킹 FN율 | 함수 | = 0 |

생기부 모듈 제거 후에도 `mask_pii()`는 출제 경로가 쓴다(`app/main.py` `_build_spec()` —
`passage_text`를 모델에 넘기기 전 마스킹, 하드룰 2). `eval_record.py`가 채점하던
MASKING_GOLDEN 20건을 유닛테스트가 그대로 강제하도록 옮겼다.

### 출제 모듈 RAG 품질 (`eval_ragas.py`)

| 지표 | 방식 | 상태 |
|---|---|---|
| Faithfulness | 자체 구현(Ragas 알고리즘, LLM Judge) — `evals/eval_ragas.py` | ✅ 첫 측정 완료(n=5, 0.600) — Ragas 패키지는 의존성 충돌로 미사용, 8절 참고 |
| Answer Relevancy | 자체 구현(Ragas 알고리즘, 임베딩 코사인 유사도) — `evals/eval_ragas.py` | ✅ 첫 측정 완료(n=5, 0.631) — 8절 참고 |

### 출제 모듈 Agent Trajectory (`eval_trajectory.py`, 2026-08-03 신규)

산출물이 아니라 **과정**을 본다 — 골든셋이 없고, 이미 LangSmith에 쌓인 트레이스를 집계한다.
사람 라벨과 대조하는 게 아니라 실패 모드의 분포를 보는 것이라 **pass/fail 기준 없이 전부 참고값**.

| 지표 | 방식 | 기준 |
|---|---|---|
| 도구 호출 오류율 / 거부율 / 빈결과율 | LangSmith `run_type="tool"` 집계 | 참고값 |
| 재시도 원인 분류 (형식·절차 실패 vs Judge 판단 불일치) | `validate` 노드의 `validation_feedback` 문자열 분류 | 참고값 |
| malformed tool-call 비율 | `agent_node`가 주입한 재작성 요청이 LLM 호출 입력에 등장한 비율 | 참고값 |
| 궤적 형태 (세션당 agent 재진입 횟수, `submit_for_review` 도달률, validate 통과율) | 노드 run 집계 | 참고값 |

상세 정의·첫 실행 결과·한계는 11절 참고.

## 2. 골든셋 현황

| 골든셋 | 경로 | 규모 | 비고 |
|---|---|---|---|
| retrieval_golden | `data/golden/retrieval_golden_final.json` | 22개 (reviewed 22개, 2026-07-24 확인 — 이전 문서엔 21개로 기록돼 있었으나 실제 파일은 전량 검수 완료 상태) | 실데이터 기반, 사람 검수. 2026.07 past_exams 참조 8개 제거(30→22) |
| STRUCTURE_GOLDEN | `data/golden/structure_golden.json` | 14개 (라벨링 대기) | count_match 폐기·num_items 도입으로 Claude 부트스트랩 전량 폐기, 실제 qwen2.5:7b 출력으로 전면 재생성(정확히 일치 5·부족 8·초과 1) — human_label 라벨링 대기 |
| MASKING_GOLDEN | `data/golden/masking_golden.json` | 20개 | 합성. 생기부 모듈 제거 후 `tests/test_masker.py`가 채점(출제 경로가 `mask_pii`를 계속 사용) |
| ~~VIOLATION_GOLDEN~~ | — | — | **2026-08-03 삭제** (생기부 모듈과 함께, 14절) |
| ~~HALLUCINATION_GOLDEN~~ | — | — | **2026-08-03 삭제** (생기부 모듈과 함께, 14절) |
| ITEM_GOLDEN | `data/golden/item_golden.json` | 30개 | human_score 1~5점 분포. 2026-07-09 `evals/eval_exam.py` 하드코딩에서 외부화 |
| example_question_retrieval_test | `data/golden/example_question_retrieval_test.json` | 8개 (reviewed 0개) | 주제어가 아닌 "실제 문제 문장" 스타일 query — standards 컬렉션과의 문체 격차 검증용, 라벨링 대기 |

## 3. 실행 방법

```bash
# 출제 모듈 평가
python evals/eval_exam.py

# 출제 모듈 Agent Trajectory (LangSmith 트레이스 집계 — 11절)
python evals/eval_trajectory.py --since 2026-07-23
```

> `eval_trajectory.py`는 모델을 호출하지 않고 LangSmith API만 읽는다(`LANGCHAIN_API_KEY` 필요).
> 집계할 트레이스를 새로 만들려면 트레이싱을 켠 채로 출제 그래프를 한 번 돌려야 한다:
> `CHROMA_PERSIST_DIR=./chroma_db python scripts/test_exam.py`
> (`.env`에 `LANGCHAIN_TRACING_V2=true`가 이미 있다면 셸에서 다시 줄 필요 없음)

Windows 콘솔에서 실행 시 `cp949` 인코딩 오류(`UnicodeEncodeError`)가 날 수 있음 — 실행 전 `chcp 65001` 또는 `set PYTHONIOENCODING=utf-8` 필요.

## 4. 결과 이력

| 날짜 | 모델 | Recall@5 | MRR | 문항품질 | kappa | ±1일치율 | 세트제약/구조Judge |
|---|---|---|---|---|---|---|---|
| 2025.07 | qwen2.5:1.5b | - | - | - | - | - | - |
| 2025.07 | qwen2.5:7b | 0.679 ❌ | 0.494 | 3.68 ❌ | 0.328 ❌ | 0.800 ✅ | 세트제약 ✅ (리디자인 전) |
| 2025.07 | qwen2.5:7b (생기부) | - | - | - | - | - | FN=0✅ NLI=0.1❌ 위반Recall=0.84❌ |
| 2026.07 | (BGE만, LLM 무관) | 0.905 ✅ | 0.659 | - | - | - | past_exams 8개 제거 후 재측정(n=21) |
| 2026.07.14 | (BGE만, 청킹 재설계 1차 리매핑, n=18) | 0.778 | 0.613 | - | - | - | 신 청킹으로 골든셋 리매핑 직후 측정치 — 이후 아티팩트로 판명(아래 행 참고) |
| 2026.07.14 | (BGE만, 골든셋 앵커 재보정 후, n=18) | 0.944 | 0.780 | - | - | - | ret_003/008/022의 chunk_preview를 새 청크 경계에 맞게 재앵커링 후 재측정 — 구 청킹(0.944)과 동일 수치로 회복. 검색 성능 자체는 청킹 변경 전후로 줄곧 동일했음(둘 다 정답을 rank1로 반환), 위 0.778은 골든셋 채점 기준(80자 완전 포함 매칭)이 청킹 경계 변화에 취약해 생긴 측정 아티팩트였음이 확인됨. 10절 상세 |
| 2026.07.14 | (BGE만, 골든셋 22개 전체 재보정 후) | **0.955** | **0.789** | - | - | - | ret_009/013/019까지 마저 재보정(ret_013은 query 자체를 실제 규정에 맞게 교체) — 22개 중 21개 정합성 확보. 유일한 MISS는 ret_015(청킹 무관, 기존 문제, 의도적으로 미착수). 10절 상세 |
| 2026.07 | qwen2.5:1.5b | - | - | - | - | - | 구조Judge count 0.667/diff 0.667/MAE 1.333 (STRUCTURE_GOLDEN 3개, 부트스트랩) |
| 2026.07.09 | qwen2.5:7b (JUDGE_TPL 5점 앵커 추가 전) | 0.857 | 0.611 | 오답매력도 2.50, 종합 3.70, 합격률 47% | 0.328 | 0.800 | 구조Judge MAE 2.000 (n=3) |
| 2026.07.09 | qwen2.5:7b (JUDGE_TPL 5점 앵커 추가 후) | 0.905 | 0.659 | 오답매력도 2.83(+0.33), 종합 3.79, 합격률 73%(+26%p) | 0.328(변화없음) | 0.800 | 구조Judge MAE 1.667 (n=3) |
| 2026.07.09 | qwen2.5:7b (agent_node 프롬프트 변경 전, `compare_distractor_quality.py`로 실제 생성) | - | - | 오답매력도 2.500 (n=8, 객관식만) | - | - | - |
| 2026.07.09 | qwen2.5:7b (agent_node 프롬프트 변경 후, `compare_distractor_quality.py`로 실제 생성) | - | - | 오답매력도 2.846 (n=13, 객관식만, +0.346) | - | - | - |
| 2026.07.11 | qwen2.5:7b (STRUCTURE_GOLDEN 사람 라벨 20개 완성 후 첫 정식 측정) | - | - | - | - | - | 구조Judge diff 일치율 0.900(κ 0.615✅)/overall MAE 2.150·±1 일치 0.300·이진(≥3) κ **-0.103**❌/type_ratio MAE 0.206(r 0.575) — overall은 Judge가 +1.95 체계적 과대평가, 원인 분석 아래 참고 |
| 2026.07.11 | qwen2.5:7b (STRUCTURE_JUDGE_TPL에 rubric 주입 후 재측정) | - | - | - | - | - | 구조Judge overall MAE 1.800·±1 일치 0.500·이진 κ 0.111(여전히❌)·편향 +1.60 / diff κ 0.474(0.615→하락했으나 목표 유지) — 개선됐지만 미달, 잔여 원인은 "원문 복사 감지 실패"(아래 분석) |
| 2026.07.12 | qwen2.5:14b (STRUCTURE_GOLDEN v3: 유사도 게이트+tool-calling 재시도 로직 적용 후 재생성, 20개 사람 라벨링 완료) | - | - | - | - | - | 구조Judge overall MAE **1.000**·±1 일치 **0.800**·이진(≥3) κ **0.000**(여전히❌, 원인: Judge 점수가 {3,4}에만 몰려 분산 0)·편향 **+0.80**·diff κ 0.474(유지✅) — 연속형 지표는 전부 개선(MAE 2.15→1.80→1.00), human overall 평균도 1.95→2.85로 상승(생성 품질 실제 개선). 이진 kappa만 역설적으로 하락 — 5절 분석 참고 |
| 2026.07.12 | qwen2.5:14b (STRUCTURE_JUDGE_TPL 저점 few-shot 2개 추가) | - | - | - | - | - | 구조Judge overall MAE 0.850(개선)·편향 -0.15(거의 해소)했으나 **Pearson r 0.305→0.097·이진 κ 0.000→-0.099로 상관관계 붕괴**(부작용, 시행착오) — 5절 참고 |
| 2026.07.12 | qwen2.5:14b (저점 1개+균형 문구/예시로 재조정, 채택) | - | - | - | - | - | 구조Judge overall MAE 0.900·±1 일치 0.800·이진(≥3) κ **0.178**(개선, 여전히❌)·Pearson r 0.306(A 수준 회복)·diff κ **0.687**(뚜렷 개선) — 3라운드 중 최선, 5절 참고 |
| 2026.07.12 | qwen2.5:14b (human_label 일관성 재검토 후 str_010/047 재라벨링 → 동일 C안 프롬프트로 재측정) | - | - | - | - | - | 구조Judge overall MAE 1.300·이진(≥3) κ **0.000**·Pearson r **-0.073**·diff κ 0.231 — 전 지표 악화했으나 측정이 더 정확해진 것(Judge가 str_010/047을 재라벨링 전후 동일하게 관대히 채점 — "유의어 치환 반복" 미탐지가 진짜 사각지대임을 확인). 추가 few-shot 튜닝은 n=20 과적합 위험으로 중단, 골든셋 40~50개로 확대 후 재검증 예정(5절 참고) |
| 2026.07.12 | qwen2.5:7b (생기부, 규정 위반 Recall 개선 전) | - | - | - | - | - | 위반Recall=0.840(기존 측정치, 반복 없음) |
| 2026.07.12 | qwen2.5:7b (생기부, 규칙기반 키워드 확장+VALIDATE_TPL 위반유형 한정 후, 3회 반복) | - | - | - | - | - | 위반Recall 3회 측정 **0.920 / 0.960 / 1.000**(평균 0.927, 이전 0.840 대비 개선), Precision 0.958~1.000 — 5절 참고 |
| 2026.07.12 | qwen2.5:7b (STRUCTURE_GOLDEN 20개 → 45개로 확대, 생성만·라벨링 없음) | - | - | - | - | - | `gen_structure_golden.py`로 신규 주제 25개(str_052~076) 생성. budget=1 최초 시도에서 0문항 7건 발생(28%) → budget=3 재시도로 5건 회복 → 남은 2건은 다른 주제로 교체(1건은 재교체까지 2회 필요) 해 **최종 45개 전부 1개 이상 문항 확보**(0문항 없음). 사람 라벨링은 다음 단계(제가 아님) — 5절 참고 |
| 2026.07.12 | qwen2.5:14b (n=45 전량 사람 라벨링 완료, 동일 C안 프롬프트로 첫 재측정) | - | - | - | - | - | 구조Judge overall MAE 1.356·±1 일치 0.556·편향 **+1.178**(n=20 때보다 더 과대평가)·이진(≥3) κ 0.126·Pearson r 0.143·Spearman ρ 0.181·**diff κ 0.509**(목표 0.4 최초 달성) — n=20 대비 diff κ·상관관계는 개선(표본 확대로 노이즈 감소 시사)됐으나 overall 이진 κ·MAE·편향은 여전히 미달/악화, 5절 참고 |
| 2026.07.12 | qwen2.5:14b (STRUCTURE_JUDGE_TPL에 3점 앵커 few-shot 추가 후, n=45, 3회 반복) | - | - | - | - | - | 편향 원인 분석 결과 few-shot 점수 분포가 {1,1,2,4,5}로 3점 미앵커링 → Judge가 애매한 사례에서 3점으로 회피 수렴하는 패턴 확인(judge 분포 58%가 3점, human=1 항목 14개 중 judge=1은 단 1개). 3점 앵커(유의어 치환+정상 문항 혼합) 추가 후 3회 평균: MAE 1.356→**1.185**, ±1 0.556→**0.659**, 편향 +1.178→**+0.978**, 이진 κ 0.126→**0.167**, Pearson r 0.143→**0.248**, Spearman ρ 0.181→**0.269** — 전 지표 개선, 저점 few-shot 시행착오 때와 같은 붕괴 없음. diff κ만 0.509→평균 0.424로 변동폭 커짐(3회 중 1회 0.4 미달, 평균은 유지) — 5절 참고 |
| 2026.07.17 | qwen2.5:7b (생기부, regulations RAG 검색 Recall@5 신규 계측 + 전체 재측정) | - | - | - | - | - | `eval_record.py`에 `regulations` 컬렉션만 분리한 Recall@5/MRR 추가(기존 `eval_exam.py`는 standards+regulations 22건을 합산해서만 보고 — 22건 중 regulations 10건만 따로 떼어 측정한 건 이번이 처음). **regulations Recall@5=0.900, MRR=0.667(n=10, 참고용)** — 정답을 찾긴 하지만(9/10) 1순위로 못 올리는 경우가 잦음(MRR 0.667≈평균 1.5위). 같은 실행에서 PII 마스킹 Recall=1.000·FN=0, NLI 사실추가율=0.050(1/20, 기존 관측 범위 0~0.05 내), **위반 Recall=1.000/F1=1.000**(n=50, 기존 3회 평균 0.927보다 높음 — 단일 실행이라 상한 노이즈 가능성 있고 3회 반복 재확인은 안 함). **참고**: `CHROMA_PERSIST_DIR` 기본값을 `./chroma_db_record_eval`(격리용, 실제로는 비어있어 regulations 검색이 항상 스킵됐을 가능성)에서 다른 eval 스크립트와 동일한 `./chroma_db`(실 인덱싱 데이터, regulations 472건)로 변경 — 로컬에서는 `.env`의 `CHROMA_PERSIST_DIR=/data/chroma_db`가 `load_dotenv()`에 의해 먼저 세팅돼 `setdefault`가 무시되므로, 실행 시 `CHROMA_PERSIST_DIR=./chroma_db` 쉘 오버라이드 필요(다른 eval 스크립트도 동일 이슈) |
| 2026.07.17 | **judge 모델 비교**: qwen2.5:7b vs gpt-5.6-luna (동일 생성물 재채점만, `scripts/compare_judge_models.py` 신규) | - | - | - | - | - | `get_judge_backend()`가 항상 Ollama로 고정돼 있던 걸 `JUDGE_BACKEND=openai` 분기로 확장(기본값은 기존과 동일 유지). ITEM_GOLDEN(n=30, human_avg 3.33)·STRUCTURE_GOLDEN(n=45)을 두 judge로 각각 재채점해 비교(raw: `data/golden/_judge_comparison_results.json`). **문항품질**: kappa 0.328→**0.595**, exact 0.267→0.300, llm_avg 3.83→**4.27**(편향 +0.50→**+0.94**, GPT가 더 후하게 채점), ±1일치율은 qwen이 더 높음(0.800→0.733). **구조유사도**: difficulty_match 0.867→0.933, **overall MAE 1.689→0.600** — GPT용 프롬프트 튜닝을 전혀 안 했는데도(qwen 버릇 교정용 3점 앵커 few-shot 그대로 재사용) 몇 달간 튜닝한 qwen2.5:14B 최고 기록(MAE 1.185, 위 2026.07.12 행 참고)보다 낮음. 해석: 로컬 오픈모델의 judge 신뢰도 한계를 뒷받침하는 결과이나, GPT의 절대 점수 편향이 더 크다는 상충 신호 있어 "확실히 더 나은 judge" 정도로 해석 권장(n=30/45로 표본 작음). 실제 교체는 과거 kappa/MAE 히스토리 단절·재보정 필요가 트레이드오프 — 사용자 결정 대기(`bunpil_roadmap.md` "Judge 모델 비교 실험" 절 참고) |
| 2026.07.24 | 생성 qwen2.5:14b(local) / Judge gpt-5.6-luna(OpenAI, `JUDGE_BACKEND=openai`) — **judge 분리 후 `evals/eval_exam.py`·`evals/eval_record.py` 정기 파이프라인 첫 정식 재측정** | **0.955** ✅ | 0.789 | 종합평균 **4.06** ✅(첫 목표 달성, 이전 최고 3.79)·합격률 80% | **0.468** ✅(첫 목표 달성) | 0.700 ✅ | diff_match 일치율 0.933·overall MAE **0.644**(역대 최저, 이전 최고 0.850~0.900) — 2026.07.17 `compare_judge_models.py`(위 행)는 별도 임시 스크립트로 비교한 것이었지만, 이번은 **런타임 `judge` 노드와 동일한 `judge_structure()`를 그대로 호출한 정식 실행**이라는 점이 다름. 같은 실행에서 생기부도 재측정: PII FN=0✅, 키워드/NLI 사실추가 0건✅, **위반Recall=1.000/F1=0.962**✅(n=50, 이전 3회 평균 0.927보다 높음 — 단일 실행 상한 노이즈 가능성 있어 3회 반복 재확인은 안 함), regulations Recall@5=0.900/MRR=0.667(n=10, 참고용, 변화없음). **로컬 실행 주의**: `.env`의 `CHROMA_PERSIST_DIR=/data/chroma_db`가 로컬에 없는 프로덕션 경로라 `load_dotenv()`가 먼저 세팅해버려 두 스크립트의 `setdefault("./chroma_db")`가 무시됨(2026.07.17 행에서도 같은 이슈 기록됨) — `CHROMA_PERSIST_DIR=./chroma_db` 쉘 오버라이드로 실행함 |

| 2026.08.03 | **하이브리드 검색(BM25+dense, RRF) 도입 A/B** — 검색만 측정(LLM 미사용), 생성·Judge 모델 무관 | 0.955 (변화 없음) | **0.814** (dense-only 0.789) | — | — | — | standards MRR 0.892→**0.938**, regulations MRR 0.667→**0.667(변화 없음)**. 리랭커 이전 후보 포함률 9/10→**10/10**(`ret_015`를 BM25가 후보 12위로 처음 찾아냄) — 12절 참고 |
| 2026.08.03 | **리랭커 조사 → `n_candidates` 20→10 변경** — 검색만 측정(LLM 미사용) | **1.000** ✅(첫 만점) | **0.854** | — | — | — | regulations Recall@5 0.900→**1.000**, MRR 0.667→**0.753**(12절 착수 시 주 목표 달성). 골든 22건 전수 대조에서 개선 3건·**악화 0건**, 리랭커 처리 쌍이 절반이라 **속도 2배**. 리랭커 ablation도 함께 측정(기여도 MRR +0.034~0.045, Recall엔 영향 없음) — 13절 참고 |

> 모델 교체 또는 프롬프트 튜닝 시마다 행 추가. 2026.07 Recall@5/MRR은 passage_text 리디자인으로 past_exams golden 항목이 제거되며 n이 28→21로 줄어 재측정한 값(검색은 LLM과 무관하므로 모델 열은 해당 없음).
>
> **2026.07.09 전/후 비교 주의사항**: 위 두 행은 20분 간격으로 연속 실행한 것으로, `JUDGE_TPL`(`정답유일성`·`오답매력도`·`근거성`)에 오답매력도=5점 few-shot 예시 1개만 추가한 차이만 있음(생성 프롬프트는 미변경). Recall@5(0.857→0.905)와 구조Judge MAE(2.000→1.667)도 이 변경과 무관한데 함께 흔들려서, 이 실행 간 약 ±0.05~0.3 수준의 자연 노이즈(HNSW 근사검색·LLM 샘플링 변동)가 있는 것으로 보임 — 오답매력도 +0.33도 전부가 few-shot 효과라고 단정하기보다는 방향성 신호로 해석 권장. 합격률이 47%→73%로 크게 뛴 건 여러 문항의 overall이 4.0 문턱을 살짝 넘었기 때문(경계 근처 문항이 많았다는 뜻).
>
> **방법론 오류 정정(2026.07.09)**: 이후 `graph.py` agent_node 프롬프트(생성 측)에도 오답 매력도 지시를 추가하고 같은 방식(`eval_exam.py` 전/후 재실행)으로 검증하려 했으나, `eval_item_quality()`가 채점하는 `ITEM_GOLDEN`은 **스크립트에 하드코딩된 고정 30개 문항**이라 agent_node를 전혀 호출하지 않는다 — 즉 생성 프롬프트를 바꿔도 이 지표엔 원리적으로 반영될 수 없다(실제로 전/후 평균이 2.815로 완전히 동일하게 나와서 발견). 생성 프롬프트 변경 효과는 `scripts/compare_distractor_quality.py`로 별도 검증함(아래 결과 이력 참고).

## 5. 진행 중인 조사

### 구조 유사도 Judge 신뢰도 첫 정식 측정 결과 분석 (2026-07-11, n=20)

사람 라벨 20개 완성 후 `judge_structure_one()`(qwen2.5:7b)을 각 엔트리에 재실행해 대조.
raw 결과는 `data/golden/_structure_judge_eval_results.json`.

| 항목 | 결과 | 해석 |
|---|---|---|
| difficulty_match 일치율 / κ | 0.900 / **0.615** | 목표(0.4) 초과 — 난이도 판단은 신뢰 가능 |
| type_ratio_score MAE / r | 0.206 / 0.575 | 방향은 맞으나 Judge가 관대한 편 |
| overall_score MAE | 2.150 | 매우 나쁨 |
| overall_score 이진(≥3) κ | **-0.103** | 우연 이하 — 현재 형태로는 사용 불가 |
| Judge 편향 | +1.95 | 20건 중 19건에서 human보다 높거나 같게 채점 |

**원인(핵심)**: 사람 라벨은 이번에 신설된 rubric(`_schema.overall_score_rubric`)에 따라
**중복 문항·원문 복사·주제 이탈·환각을 감점**하는데, Judge 프롬프트(`STRUCTURE_JUDGE_TPL`)는
"유형 비율·난이도 구성의 구조적 유사도"만 물어봄 — 즉 **사람과 Judge가 서로 다른 것을
채점하고 있음**. 최대 불일치 사례가 전부 이 패턴: str_029/041/025/047(human 1: 완전 중복
문항들, judge 4~5: 유형·난이도는 실제로 일치), str_037(human 0: 언어 오염, judge 3).
Judge가 무능하다기보다 질문이 rubric과 정렬되지 않은 것이 1차 원인 — **다음 단계는
STRUCTURE_JUDGE_TPL에 rubric(중복·복사·주제 이탈 감점)을 주입하고 재측정**. 그래도 κ가
낮으면 그때 Judge 모델 크기(14B) 문제로 넘어감.

**rubric 주입 후 재측정 (2026-07-11, 같은 날)**: `STRUCTURE_JUDGE_TPL`에 0~5 rubric 전문과
"세트 내부 완전 중복 → overall 1" few-shot을 추가하고 20개 재실행.

| 항목 | 주입 전 | 주입 후 | 판정 |
|---|---|---|---|
| overall MAE | 2.150 | **1.800** | 개선 |
| overall ±1 일치 | 0.300 | **0.500** | 개선 |
| overall 이진(≥3) κ | -0.103 | **0.111** | 개선됐으나 목표(0.4) 미달 ❌ |
| Judge 편향 | +1.95 | +1.60 | 개선 |
| difficulty κ | 0.615 | 0.474 | 소폭 하락(목표는 유지 ✅, n=20 노이즈 범위) |

일부 케이스는 정확해짐(str_014: judge 3→0 정답, str_041: 5→3, str_037: 3→2). 그러나
**잔여 최대 불일치 6건(str_012/025/029/047/043/049, human 0~1 vs judge 3~5)은 전부
"생성 문항이 예시 문제의 원문 복사"인 케이스** — rubric에 "원문 단순 복사=1점"이 명시돼
있어도 7B Judge는 생성 문항과 예시 문제 텍스트가 사실상 동일하다는 대조를 해내지 못함.

**다음 단계 제안**: 원문 복사·세트 내 중복은 정성 판단이 아니라 **결정론적 텍스트 유사도
검사로 코드가 감지 가능** — "LLM judges, code decides" 원칙대로 이 감점 요소를 Judge에서
떼어내 코드 게이트(예: `save_item`에 예시 문제·기존 저장 문항과의 유사도 검사)로 옮기면,
Judge는 남은 정성 판단(주제·형식 유사성)만 담당하게 되어 정렬이 쉬워짐. 부수적으로 생성
품질 문제(중복 생성이 최다 감점 사유)도 동시에 개선됨. 그 후에도 κ가 낮으면 Judge 14B 검토.

**→ 유사도 게이트 적용됨 (2026-07-11)**: `save_item`에 문자 bigram 기반 결정론적 검사 추가
(`_check_similarity`, tools.py) — 예시 문제 대비 containment ≥0.90이면 "원문 복사" 거부,
기존 저장 문항 대비 jaccard ≥0.80이면 "세트 내 중복" 거부. 임계값은 라벨링된 골든셋 실측
분포로 결정(완전 복사 1.00 vs 정상 주제-유사 변형 ≤0.73 / 진짜 중복 0.86~1.00 vs 정상 변형
≤0.67). 소급 검증: human 0~1점 복사 케이스(str_012/025/029/047/043) 전부 차단, 중복 세트
(str_035/036/041/049) 전부 차단, 정상 세트 중복 오탐 0. 흥미롭게도 human 4~5점 세트에도
원문 복사 문항이 1개씩 있었는데(라벨링 시 세트 단위 관대 평가) rubric상 감점 대상이므로
차단이 설계에 부합. e2e 확인: 게이트가 복사·오염 문항을 실제로 거부하고 에이전트가 재작성을
시도함. **단, 게이트로 인해 7B의 "가짜 성공"(복사본 반환)이 명시적 실패/재시도로 바뀌면서
생성 성공률이 하락** — 시스템 프롬프트에 복사 금지 명시로 일부 완화했으나, 근본적으로 7B가
"새 문항 창작"을 어려워하는 문제가 드러남(이전엔 복사본이 성공으로 집계돼 가려져 있었음).
구조 Judge 재측정은 게이트 적용 후 생성된 새 골든셋(라벨링 필요)에서 의미가 있음.

**→ 게이트 적용 + qwen2.5:14b로 골든셋 v2 재생성 (2026-07-11)**: 라벨링된 v1과 동일한
20개 passage로 14B(budget=1, 게이트 전부 활성) 재생성. v1(사람 라벨 포함)은
`structure_golden_v1_labeled.json`으로 동결 보관.

| 항목 | v1 (7B, 게이트 전) | v2 (14B, 게이트 후) |
|---|---|---|
| 정확 일치 | 8/20 (복사본이 성공으로 집계된 수치) | 5/20 |
| 부족 / 0개 / 초과 | 9 / 0 / 3 | 9 / 6 / **0** |
| 원문 복사·세트 중복 문항 | 다수 (human 최다 감점 사유) | **0** (게이트 소급 재검으로 확인) |
| 문항 전체 중국어 오염 | 9/29개 세트가 오염으로 분리됐던 이력 | 0 (단, str_035에 '합憲'식 한자 13자 잔존 — 5% 문턱 아래 경계 누출) |

해석: v1의 "정확 일치 8"은 복사본 포함 수치라 실질 품질은 v2가 우위일 것으로 예상
(확정은 사람 라벨링 후). 게이트가 복사·중복을 원천 차단하면서 개수 처리량은 하락
(0개 6건) — budget=1 기준이므로 프로덕션(budget=5+부분 보존)에서는 완화될 것.
**다음: v2 20개 사람 라벨링 → Judge κ 재측정** (복사·중복이 없는 셋에서는 Judge가
정성 판단만 하면 되므로 정렬 기대).

**→ tool-calling 형식 오류 재시도 로직 적용 (2026-07-11)**: v2 재생성에서 0문항으로
끝난 6건을 LangSmith 트레이스로 조사한 결과, 모델이 도구를 부르려는 의도는 있었으나
`tool_calls` 필드가 아닌 깨진 텍스트로 출력해 `agent_node`가 "정상 종료"로 오판하고
루프를 조기 종료한 것이 원인으로 확인됨(7B/14B 공통, 상세는 TROUBLESHOOTING.md).
형식 오류 감지 시 종료 대신 최대 2회 재시도하는 로직을 `graph.py`에 추가(로컬·프로덕션
공통 코드경로). 0문항이었던 6건 재실행 결과 6/6 전부 1개 이상 생성으로 개선됐으나,
재시도 로직이 실제로 발동한 횟수는 0회 — qwen 계열의 실행별 자연 변동(run-to-run
variance)일 가능성이 있어 이 로직의 인과 효과는 확정하지 못함(로직 자체는 최악의 경우도
이전과 동일하므로 무해함, TROUBLESHOOTING.md 참고).

**→ STRUCTURE_GOLDEN v3 재생성 (2026-07-11, 게이트+재시도 로직 전부 적용)**: v1/v2와
동일한 20개 passage를 14B(budget=1)로 재생성. v2(재시도 로직 적용 전)는
`structure_golden_v2_pre_retry_fix.json`으로 동결 보관.

| 항목 | v1 (7B, 게이트 전) | v2 (14B, 게이트만) | v3 (14B, 게이트+재시도) |
|---|---|---|---|
| 정확 일치 | 8/20 (복사본 포함 수치) | 5/20 | **8/20** |
| 0개 | 0 | 6 | **2** |
| 부족 / 초과 | 9 / 3 | 9 / 0 | 10 / 0 |
| 복사·중복·언어 오염 | 다수(human 최다 감점 사유) | 0 | 0 |

해석: v3는 v1과 정확 일치 개수가 같지만(8/20) v1은 복사본이 섞여 있던 수치이고 v3는
게이트 소급 재검에서 위반 0건 확인된 진짜 신규 문항이라 실질 품질은 v3가 우위. v2(게이트
직후, 재시도 로직 전)와 비교하면 0개가 6→2로 줄어 재시도 로직 도입 후 첫 대규모(20개) 실행
에서는 개선이 뚜렷함(이전 6개 소표본 재현 테스트에서는 로직 미발동으로 인과관계 불확실
했던 것과 달리, 이번 20개 전체 비교에서는 방향 일치). 다만 재시도 로직의 실제 발동 횟수는
gen_structure_golden.py가 agent_messages를 골든셋에 저장하지 않아 이번에도 직접 확인은
못 함 — 인과 확정에는 별도 계측이 필요.

**→ 잔여 0개 2건 대체 (2026-07-11)**: v3의 0개 2건(str_025/049)은 골든셋에서 제외하고
신규 passage(str_050/051)로 대체 생성(같은 14B, budget=1, 게이트+재시도 로직 적용
코드). 2건 모두 1회차에 0개가 아닌 결과 확보(3/5, 5/7) — 재시도 없이 통과, 게이트
소급 재검에서도 위반 0건. **최종 20/20 전부 1개 이상 생성, 정확 일치 8/20, 0개 0/20.**

**→ v3(최종 20개) 사람 라벨링 완료 → Judge κ 재측정 (2026-07-12, Judge=14B)**:

| 항목 | v1 (7B, rubric 전) | v1 (7B, rubric 후) | v3 (14B, 게이트+재시도, rubric 유지) |
|---|---|---|---|
| overall MAE | 2.150 | 1.800 | **1.000** |
| overall ±1 일치 | 0.300 | 0.500 | **0.800** |
| overall 이진(≥3) κ | -0.103 | 0.111 | **0.000** |
| Judge 편향 | +1.95 | +1.60 | **+0.80** |
| difficulty κ | 0.615 | 0.474 | 0.474 |
| type_ratio MAE / r | 0.206 / 0.575 | — | 0.225 / 0.383 |
| human overall 평균 | 1.95 | 1.95 | **2.85** |

연속형 지표(MAE, ±1 일치, 편향)는 전부 v1→v3로 꾸준히 개선됨 — 생성 품질 자체가
좋아지면서(게이트로 복사·중복 차단) human 평균도 1.95→2.85로 올라갔고 Judge도 이를
따라감. **그런데 이진(≥3) κ는 오히려 0.000으로 떨어짐. 원인을 확인한 결과, 이번 라운드에서
Judge의 overall_score가 {3, 4} 두 값에만 몰려 있었다**(20개 중 0개가 3점 미만 — 분포:
`[3×7, 4×13]`). human은 1~5점에 고르게 퍼져 있고 7/20이 3점 미만인데, Judge가 이
저점 구간을 한 번도 주지 않으니 이진 분류에서 Judge 쪽 분산이 0이 되어 **kappa가 수학적으로
0으로 강제됨**(Judge가 전부 "합격" 쪽으로만 응답하면, 우연히 맞을 확률과 실제 맞힌 확률이
같아짐). 즉 "Judge가 무작위로 틀렸다"가 아니라 **"Judge가 낮은 점수 자체를 잘 안 준다"**는
구체적 편향 — 생성 품질이 실제로 좋아지긴 했으나(human 평균 상승), 여전히 존재하는
결함(예: str_048 human 1 vs judge 4, str_037 human 2 vs judge 4)을 Judge가 과소평가.

**→ 저점 few-shot 2개 추가 후 재측정 (2026-07-12, 시행착오 A)**: str_048류(패러프레이즈
반복)와 환각 사례를 새 few-shot 2개로 추가. 결과: MAE 1.000→**0.850**, 편향
+0.80→**-0.15**(체계적 과대평가 거의 해소, Judge 점수 분포도 {3,4}→{2,3,4}로 분산돼
의도대로 저점을 주기 시작함)로 평균 지표는 개선. **그러나 Pearson r 0.305→0.097,
Spearman rho 0.083, 이진 κ 0.000→-0.099로 상관관계는 오히려 붕괴.** 원인: str_035·
str_039는 문항이 **1개뿐**이라 애초에 반복·중복이 성립할 수 없는 케이스인데도 Judge가
human 4~5점짜리를 2~3점으로 깎음 — 저점 few-shot이 "패러프레이즈 반복을 정확히 감지"가
아니라 **Judge 전체를 무차별적으로 더 엄격하게 만드는 부작용**을 낸 것으로 판단(평균은
맞아졌지만 개별 항목의 좋고 나쁨 순위가 뒤섞임).

**→ 균형 문구 추가로 재조정 (2026-07-12, 시행착오 B, 채택)**: 환각 few-shot은 제거(1개만
유지: 패러프레이즈 반복), 시스템 프롬프트에 "같은 주제·개념 범주 안에서도 서로 다른 지점
(정의·사례 적용·원인·결과·비교 등)을 묻는 문항은 표현이 비슷해도 반복이 아니므로 감점
금지" 문구 + 이를 보여주는 균형 few-shot(같은 주제, 다른 지점 → 높은 점수) 1개 추가.

| 지표 | A: 추가 전 | B: 저점 2개(부작용) | C: 저점 1+균형(채택) |
|---|---|---|---|
| overall MAE | 1.000 | 0.850 | 0.900 |
| ±1 일치 | 0.800 | 0.800 | 0.800 |
| 편향 | +0.80 | -0.15 | +0.50 |
| Pearson r | 0.305 | 0.097 | **0.306** |
| Spearman rho | — | 0.083 | 0.247 |
| 이진(≥3) κ | 0.000 | -0.099 | **0.178** |
| difficulty κ | 0.474 | 0.400 | **0.687** |

C안이 세 라운드 중 가장 균형 잡힌 결과: MAE·±1 일치는 A와 비슷하지만 상관관계(r)는
A 수준을 유지·소폭 개선했고, 이진 κ(0.000→0.178)와 difficulty κ(0.474→0.687)는 뚜렷이
개선. **다만 overall 이진 κ 0.178은 여전히 목표(0.4) 미달**. Judge 최대 불일치는 여전히
str_001(human 2 vs judge 4)·str_037(human 2 vs judge 4)·str_039(human 5 vs judge 3) —
남은 원인은 아직 특정 못함, 표본 20개로는 few-shot 미세조정의 효과가 노이즈에 묻히기
쉬운 한계도 있음(교훈: 저점 예시를 넣을 땐 반드시 균형 예시를 같이 넣을 것 — 편향 감소가
곧 신뢰도 개선은 아니며, 상관관계·kappa를 반드시 같이 확인해야 함).

**→ str_010/047 라벨 일관성 재검토 → C안으로 재측정 (2026-07-12)**: 자동 스캔(느슨한
bigram 유사도, 게이트 0.80보다 낮은 패러프레이즈까지 포착)으로 human_label 자체의
일관성을 점검한 결과, str_010(문항 간 유사도 0.75, 게이트 차단선 바로 아래)과
str_047(0.56)이 str_048(0.48, overall=1)보다 텍스트 유사도가 더 높은데도 overall=4로
훨씬 후하게 라벨링돼 있던 것을 발견 — 셋 다 "같은 질문을 유의어만 바꿔 반복"하는
동일 유형 결함인데 채점 기준이 사례마다 흔들린 것. 사람이 str_010/047을 overall=1로
재라벨링 후 C안 프롬프트로 재측정:

| 지표 | C안(재라벨링 전) | C안(재라벨링 후) |
|---|---|---|
| overall MAE | 0.900 | 1.300 |
| ±1 일치 | 0.800 | 0.650 |
| 편향 | +0.50 | +1.00 |
| Pearson r | 0.306 | **-0.073** |
| Spearman rho | 0.247 | 0.016 |
| 이진(≥3) κ | 0.178 | 0.000 |
| difficulty κ | 0.687 | 0.231 |

전 지표가 나빠졌지만 **이건 측정이 부정확해진 게 아니라 더 정확해진 것** — 이전엔 human이
str_010/047에 준 관대한 점수(4)가 Judge의 관대한 점수(4~5)와 우연히 맞아떨어져 좋아
보였을 뿐, Judge는 재라벨링 전후로 **이 두 항목에 대해 똑같이 5점·4점을 그대로 유지**함
(진짜로 태도가 안 바뀜). 즉 Judge는 "유의어 치환 반복"(배경/효과/장점/변화, 역할/목적/관계
처럼 다른 단어를 쓰지만 실질적으로 같은 질문)을 여전히 못 잡는 구체적 사각지대가 있고,
이번 재측정이 그걸 정확히 드러낸 것.

**결정(2026-07-12)**: 여기서 추가 few-shot 튜닝은 중단. n=20에 맞춰 프롬프트를 계속
패치하면 이 20개에는 좋아 보여도 새 데이터에 일반화된다는 보장이 없음(A→B 시행착오에서
이미 확인된 위험). Cohen's kappa류 지표는 표본이 작을수록 신뢰구간이 넓어 20개로는
파일럿 수준 판단만 가능 — 다음 단계로 **골든셋을 40~50개 규모로 확대**한 뒤, 지금
프롬프트(C안)를 그 새 데이터에 그대로 돌려 일반화 여부를 확인하는 것을 권장. "유의어
치환 반복 미탐지"는 현재 Judge의 알려진 한계로 기록하고 당장은 손대지 않음.

**진행(2026-07-12 야간)**: `gen_structure_golden.py`로 새 주제 25개(str_052~076)를 생성해
STRUCTURE_GOLDEN을 20개 → **45개**로 확대 완료(0문항 항목 없음, `human_label`은 전부
`null`로 비워둠 — 라벨링은 사람이 직접 진행 예정). budget=1로는 0문항 실패율이 28%(7/25)로
높게 나와 budget=3 재시도 + 실패 지속 항목 2개는 다른 주제로 교체하는 과정을 거침(기존
str_050/051 대체 방식과 동일). **다음 단계**: 사람이 신규 25개에 human_label을 채운 뒤
동일 C안 프롬프트로 재측정 → n=45에서 kappa/상관관계가 n=20 때와 비슷하게 나오는지(일반화
여부) 확인.

**사람 라벨링 완료 + 일관성 검토(2026-07-12)**: 신규 25개(str_052~076) human_label 전부
채움(45개 전량 라벨링 완료). 완료 직후 bigram jaccard 기반 반복도 proxy(str_010/047 감사 때와
동일 방법론)로 전수 재검토해 3건 수정:
- `str_076`: 문항 1·4가 표현만 다를 뿐 사실상 동일 질문(반복 결함)인데 3점으로 관대하게
  채점됐던 것을 발견 → **overall_score 3→2**로 재조정
- `str_072`: `item_type`/`difficulty` 필드에 언어 오염("서술型", "中")이 있었지만 실제 문항
  구조 자체는 서술형·중 난이도로 식별 가능 → 언어 오염은 overall_score에서 감점하고
  type_ratio_score/difficulty_match은 실제 구조 기준으로 판단하도록 **0.0/false/0 →
  1.0/true/1**로 재조정
- `str_052`: `item_type` 표기("객观式")만 오염, 실제로는 두 문항 모두 객관식 → **type_ratio_score
  0.5→1.0**(overall_score는 1로 유지)

이 과정에서 단일 문항 항목의 `type_ratio_score` 채점 기준도 명확히 함: 예시가 1문항이면
비율이 모호하지 않으므로 "예시 유형과 생성 유형이 그대로 일치=1.0 / 유형이 불필요하게
혼합=0.5 / 완전히 반대 유형=0.0"의 3단계 루브릭으로 판단(소수점 정밀 계산 아님). 언어 오염
문자열은 실제 문항 형식이 식별되는 한 type_ratio_score가 아니라 overall_score에서 감점.

**부수 발견(v1 라벨링 기준, 참고용)**: v1 human 라벨 분포는 낮은 쪽에 몰려있었음(평균 1.95/5, 0~2점이 20개 중 14개) —
rubric 기준으로 보면 현재 7B 생성 품질 자체가 낮다는 뜻이기도 함(중복 생성이 가장 흔한 감점
사유). 구조 Judge 신뢰도와 별개로 "중복 문항 생성" 자체를 줄이는 개입(예: 부분 진행 보존
프롬프트에 기존 문항과의 중복 금지 강화, 또는 save_item에 중복 검사 게이트)이 필요해 보임.

### 예시 문제 문장 → standards 검색 정합성 (2026.07.09)
retrieval_golden_final.json의 쿼리는 성취기준 해설 문체(주제어/서술형)로 만들어졌는데, 실제로는 교사가
시험 문제 문장(passage_text)을 그대로 검색에 쓸 수 있어 문체 격차가 있는지 검증되지 않은 상태였음.

- `scripts/eval_example_retrieval.py` + `data/golden/example_question_retrieval_test.json`(8개, 실제 문제 문장 스타일) 작성
- 라벨링(`expected_chunk_id`/`chunk_preview`/`reviewed`)은 사람이 직접 할 것 — 현재 0/8 라벨링 완료, Recall@5/MRR 정량 비교는 라벨링 후 가능
- 기존 골든셋 재확인 기준값: Recall@5=0.905, MRR=0.659 (n=21)
- 예비 관찰(정량 아님, top-5 reranker score 기준): "누진세에 대한 설명으로 옳은 것은?" 같은 구체적 문제 문장은 상위 후보 score가 -10대까지 낮게 나오는 반면, "소선거구제와 비례대표제의 차이점을 서술하시오." 같은 서술형은 -0.6대로 상대적으로 높음 — 문체보다 "핵심 개념어 포함 여부"가 더 큰 영향일 가능성. 라벨링 후 Recall 격차로 확인 필요
- **다음 실행**: `data/golden/example_question_retrieval_test.json`의 8개 항목에 `chunk_preview`(정답 청크 원문 앞부분)와 `expected_chunk_id`를 채우고 `reviewed: true`로 바꾼 뒤 `python scripts/eval_example_retrieval.py` 재실행 → 기존 골든셋과 Recall@5/MRR 나란히 비교됨

### 출제 에이전트 안정성 개선 (컨텍스트/tool-calling, 2026-07-10 야간 자율 세션)
TROUBLESHOOTING.md의 num_ctx 발견 이후 남은 잔여 tool-calling 실패율(~35~40%)을 줄이기 위한 실험.

- **설명텍스트 금지 지시 위치/강조**: 문장 끝에 한 번만 넣은 baseline은 효과 없었음(temperature A/B에서 0.70으로 무변화 확인). 정체성 선언 직후(초두 효과) + 끝(최신 효과) 두 번, "**매우 중요한 규칙**" 강조로 바꾼 strong 변형은 소표본(n=8, temperature=0.2)에서 턴당 설명텍스트 0.38→0.00으로 완전 제거 — 프로덕션(`graph.py`)에 strong 변형 채택
- **temperature 0.7 vs 0.2**: strong 프롬프트 기준 n=28 A/B. exact_match_rate(생성 개수==num_items) 0.7→14%, 0.2→21%(+7%p) — n=28에서 표준오차 약 ±7%p라 노이즈 범위. 초과생성 비율은 0.7→18%(5/28), 0.2→46%(13/28)로 뚜렷이 악화. **판단: temperature 기본값 0.7 유지**(정확도 개선은 불확실한데 초과생성 부작용은 명확) — 코드에는 파라미터만 추가, 즉시 전환 가능
- **재시도 시 부분 진행 보존**: 기존엔 재시도마다 `init_session()`으로 전체 초기화(이전 시도 문항 폐기). `plan_node`가 요청당 1회만 초기화하고, `agent_node`는 `reset_judge()`만 호출해 문항을 유지, "나머지 N개만 작성" 프롬프트로 이어서 생성하도록 개선. `test_exam.py`로 정성 확인 완료(재시도 후에도 이전 시도 문항이 최종 결과에 유지됨).
  - **원인 조사 결과(2026-07-11)**: `scripts/test_retry_preservation.py`의 old 시뮬레이션이 배치 실행에서 첫 샘플부터 30분+ 걸리는 현상을 `ollama.log`(`/opt/homebrew/var/log/ollama.log`)를 직접 보며 재조사. 프로세스/커넥션 누적, GPU 경합, `_invoke_with_retry()` 무한 재시도 전부 배제됨(로그상 매 턴 10~12초로 정상 처리, task ID 계속 증가) — **버그가 아니라 old 방식(budget=3, 전체 재시도) 자체가 `num_items`가 크고 잘 안 맞는 샘플에서 구조적으로 오래 걸리는 것**으로 결론. 상세 조사 과정은 TROUBLESHOOTING.md 참고
  - **정식 old vs new 정량 비교(n=8)는 시간 제약으로 중단**. 대신 실제 STRUCTURE_GOLDEN 생성 이력을 비공식 비교 근거로 사용: 개선 **이전** 코드로 생성된 14개(str_001~032, 2026-07-10 01:20 이전)는 문항 0개/부족 실패가 다수(8/14 부족), 개선 **이후** 코드로 생성된 6개(str_035~040, 재시도 시 부분 진행 보존 포함 전체 반영)는 **6/6 전부 0개 없이 성공**(정확히 일치 4·근접 초과 1·근접 부족 1) — 통제된 A/B는 아니지만 방향성은 뚜렷함. 정식 비교는 다음 세션에서 old 조건 샘플 수를 줄이거나(예: n=3) budget을 낮춰(예: budget=1) 재시도 권장
- **RAG top_k 3→2 실험**: Recall 0.810→0.762(-0.048, n=21), 기준(0.05) 경계선이라 top_k=3 유지
- **안정성 보강**: 장시간 세션에서 간헐적으로 발생하는 Ollama 스트림 오류에 `_invoke_with_retry()`(graph.py, 최대 2회 재시도) 추가

### 생기부 규정 위반 Recall 개선 (2026-07-12, 야간 자율 세션)

기존 `violation_golden.json`(50개, 위반 25/정상 25)으로 `_step_validate()`(`app/modules/record/chain.py`)를
FN/FP 항목까지 개별 출력하도록 진단 스크립트로 재실행해 원인을 특정.

**원인 분석**: FN 3건은 `_rule_violations()`의 결정론적 키워드 목록에 "가정환경"·"종교/정치성향"
카테고리 자체가 아예 없었고("불성실/부족/비교/추측/PII" 4종만 존재), "다른 학생보다 ... 느린 편"처럼
비교 표현과 서술어가 떨어져 있는 문장은 `_RULE_COMPARE`의 인접 문자열 매칭("보다 낮"처럼 붙어있어야 함)에
걸리지 않았음. FP 2건은 `search_regulations` RAG 검색이 짧은 합성 문장에 대해 무관한 행정 규정(성적일람표,
봉사활동 서식 요건 등)을 상위로 반환하고, `VALIDATE_TPL`이 위반 유형을 한정하지 않은 채 "규정에 비추어"만
지시해 LLM이 그 무관한 규정 기준으로 서식 미비를 위반으로 오판. **직접 쿼리해 확인한 결과 실제
regulations 컬렉션에서 가정환경/종교/비교 편향 금지를 명확히 검색 가능한 형태로 담은 청크가 상위에
전혀 잡히지 않아** RAG 검색 보강보다 탐지 로직(규칙+프롬프트) 강화가 근본적인 방향이라고 판단.

**적용한 변경**:
- `_rule_violations()`에 `_RULE_BACKGROUND`(가정형편/가정환경/편부모 등), `_RULE_RELIGION_POLITICS`(종교적/정치적 등),
  `_RULE_APPEARANCE`(외모/키가 작/체격 등) 키워드 목록 3종 신설 — golden set 스키마에 이미 문서화된
  위반 유형(`_schema.provenance`)을 그대로 코드화. label=0(정상) 25개 전체에 대해 신규 키워드 오탐 없음을 확인 후 반영
- `_RULE_COMPARE`에 근접 매칭 정규식(`_RULE_COMPARE_RE`, `보다` 뒤 25자 이내에 열등 서술어) 추가 —
  최초 구현에서 "느리다"의 활용형 "느린"이 문자열 "느리"를 포함하지 않는다는 점(한국어 활용은 단순
  이어붙이기가 아님)을 놓쳐 정규식이 매칭 안 되는 버그가 있었고, 재현 테스트로 발견해 즉시 수정
- `VALIDATE_TPL`(`app/modules/record/prompts.py`) 시스템 프롬프트에 (a) 위반으로 판단할 유형을
  8종으로 명시적으로 한정, (b) "정보 누락·서식 불완전은 [규정]에 조항이 있어도 위반 아님"을 최상단에
  배치(초두 강조 — 이전 세션 tool-calling 재시도 실험에서 확인된 위치 효과 재사용), (c) 봉사활동
  서식 사례(OK)·비교/종교/부정적 낙인 사례(VIOLATION) few-shot 4개 추가

**재측정(동일 조건 3회 반복)**: Recall **0.920 / 0.960 / 1.000**(평균 0.927), Precision
0.958~1.000 — 이전 단일 측정치 0.840 대비 뚜렷한 개선. 3회 중 2회는 목표(≥0.95) 달성, 1회는 근접
미달. FP는 매 실행 1건으로 일정하게 남음("봉사활동에서 대상자의 필요에 맞게 활동 방식을 조율함." —
프롬프트에 명시적 금지 문구를 넣었음에도 7B 모델이 간헐적으로 서식 관련 규정 텍스트에 이끌려 오판).
FN 잔여 원인도 "지도하기 어려운 학생임"/"학습 의욕이 전혀 없어" 같은 **부정적 낙인** 뉘앙스 판단은
의도적으로 규칙화하지 않고 LLM 판단에 맡긴 영역이라, 같은 입력에도 실행마다 다른 판단이 나오는
LLM 샘플링 변동이 그대로 노출됨 — 한 재현 테스트에서는 이 카테고리의 판단 응답에 중국어 문자가
섞여 나오는 현상도 확인(출제 모듈에서 이미 문서화한 언어 오염 버그, TROUBLESHOOTING.md 참고, 같은
모델의 동일한 근본 원인이 다른 모듈에서도 재현됨을 시사).

**결정**: 목표(0.95) 완전 달성은 아니지만 개선 방향과 원인은 명확히 검증됐으므로 이 상태로 유지.
남은 갭(간헐적 FP 1건, 부정적 낙인 카테고리 LLM 판단 변동)은 규칙 기반으로 억지로 틀어막기보다
알려진 한계로 기록 — 무리하게 n=50 골든셋에 맞춰 계속 패치하면 과적합 위험(구조 Judge few-shot
튜닝에서 이미 한 번 확인된 패턴, 5절 상단 참고)이 있다고 판단.

**⚠️ 알려진 리스크(2026-07-12, 사용자 검토 후 명시)**: 이번 개선은 `search_regulations`
RAG 검색 자체의 약점(가정환경/종교/비교 편향 금지를 명확히 담은 규정 청크가 검색 상위에
전혀 안 잡히는 문제, 위 원인 분석 참고)을 고친 게 아니라 **규칙 기반(`_rule_violations`)
키워드 목록과 `VALIDATE_TPL` few-shot으로 우회한 것**이다. `VIOLATION_GOLDEN` 50개
안에서는 이 키워드 목록이 문서화된 위반 유형을 그대로 코드화한 것이라 잘 커버하지만,
**골든셋에 없는 새로운 표현(다른 키워드로 가정환경/종교/정치성향을 언급하는 문장 등)이
실제 서비스에서 들어오면 규칙도 못 잡고 RAG도 관련 규정을 못 찾아 그대로 놓칠 가능성이
있다.** RAG 검색 자체(청크 분할·임베딩 매칭 방식 등)를 개선하는 건 현재 범위 밖으로 보류하되,
이 갭은 그대로 알려진 리스크로 남겨둠 — 향후 규정 위반 오탐지 사례가 실사용에서 보고되면
가장 먼저 의심할 지점.

### NLI 사실추가율 오탐 원인 분석 (2026-07-12, 야간 자율 세션)

로드맵에 남아있던 "오탐 2건"을 특정하기 위해 `eval_hallucination()`을 개별 항목 로깅하도록
재현 스크립트로 2회 반복 실행(`HALLUCINATION_GOLDEN` 20개, `chain.run()`으로 실제 매 실행 새로
생성). **먼저 확인한 사실**: 골든셋의 `forbidden` 키워드(수상/1등/영재 등 명백한 과장 키워드)는
두 실행 모두 0건 — 즉 `keyword_hallucination_rate`는 0으로 안정적이고, 문제는 전적으로 NLI Judge
쪽(`nli_hallucination_rate`)에서만 발생.

**재현 결과**: 실행마다 플래그되는 항목의 개수(1~4개)와 대상이 달라짐 — 즉 로드맵의 "오탐 2건"은
골든셋의 특정 2개 항목이 고정적으로 잘못 라벨된 게 아니라, **매 실행 확률적으로 몇 개씩 걸리는
더 넓은 후보군(경계 사례) 중 그때그때의 표본**이었던 것으로 보임. 두 실행에서 걸린 사례:

| memo | polished | NLI 판단 |
|---|---|---|
| 수학 시간에 발표를 잘 함. | ...적극적으로 발표를 진행하였으며, 내용을 명확하게 전달하여 이해를 도모함. | YES |
| 친구들 의견 듣고 조율함. | ...효과적으로 조율하여 협업 활동에 성공적으로 참여함. | YES |
| 협력 학습에서 팀원 도움. | ...팀 작업을 원활히 진행시키는 역할을 함. | YES |
| 체육대회에서 팀원과 함께 경기 준비함. | ...협력적인 태도와 책임감을 표현함. | YES |
| 조별 과제에서 리더 역할. | ...조업을 관리하고 팀원들과 협력하여 성공적으로 완수함. | YES |

**원인**: 골든셋 라벨 오류도, NLI 프롬프트의 명백한 버그도 아님 — **"문체 다듬기"와 "사실 추가"의
경계가 정의상 모호한 지점**을 매번 다르게 건드리는 것으로 판단. `POLISH_TPL`의 few-shot 자체가 이미
"친구들이 이해못할 때 도와줌" → "협력적인 태도를 보임"처럼 행동 서술에 일반적 미덕 프레이밍을
더하는 것을 정상 문체 변환으로 가르치고 있고, `NLI_TPL`의 few-shot도 "잘 함" → "적극 참여하였음"을
NO(추가 아님)로 규정한다 — 단발성 프레이밍 추가는 두 프롬프트 모두 정상으로 취급. 그런데 실제
`polish()` 출력은 종종 "효과적으로 조율" + "성공적으로 참여" + "협력적인 태도와 책임감"처럼 여러
평가적 수식어를 한 문장에 누적시키고, NLI Judge는 이 **누적된 함의**(원문에 없는 "효과"·"성공"이라는
결과 판단이 실질적으로 새로 추가됨)를 위반으로 판단하는 것으로 보임 — 근거 없는 판단은 아니고,
오히려 CLAUDE.md 하드룰 5("메모에 없는 사실 추가 금지")를 엄격히 적용하면 Judge 쪽이 더 원칙에
가깝다고도 볼 수 있음.

**코드 수정을 보류한 이유**: 이건 단순 버그가 아니라 "생기부 문체 관행상 허용되는 일반적 미덕
프레이밍"과 "사실 추가"의 경계를 어디에 그을지에 대한 **정책적 판단**이 필요한 지점이며, 하드룰
5(보안 하드룰 중 가장 엄격한 조항)에 직접 걸쳐 있음. `POLISH_TPL`을 더 보수적으로(원문 표현에
최대한 밀착) 바꾸면 이 오탐은 줄지만 문체 다듬기 품질(생기부다운 표현력)이 희생될 수 있고, 반대로
`NLI_TPL`을 더 관대하게 하면 실제 사실 추가를 놓칠 위험이 커짐 — 어느 쪽이 맞는지는 제가 임의로
결정하기보다 사용자 확인이 필요하다고 판단해 코드는 그대로 두고 분석만 보고함.

**사용자 판단 및 결정(2026-07-12)**: 위 경계 사례들을 원문으로 직접 검토한 결과 —
"정도부사·평가어만 중첩된 경우"(예: 위 표의 "친구들 의견 듣고 조율함" 사례, 효과적으로/
성공적으로가 붙어도 원문 행위 자체는 그대로)는 **NO(문체 다듬기)**, "원문에 없던 구체적
행위나 결과가 새로 서술된 경우"(예: 위 표의 "조별 과제에서 리더 역할" 사례, "조업을 관리"라는
새 행위 + "완수"라는 결과 주장)는 **YES(사실 추가)**로 판단. `POLISH_TPL`이 아니라 `NLI_TPL`을
보강하는 방향으로 결정 — 지금 few-shot 2개로는 이 두 패턴을 구분할 기준이 없었음.

**적용한 변경**: `NLI_TPL`(`scripts/eval_record.py`) 시스템 프롬프트에 "정도부사·평가어만
붙고 원문 행위가 그대로면 NO, 원문에 없던 구체적 행위·결과가 새로 서술되면 YES"를 명시하고,
few-shot에 위 두 유형(경계 사례 B=NO, C=YES)을 실제 사례 그대로 추가(4개로 확장).

**재측정(동일 조건 3회 반복)**: nli_hallucination_rate **0.050 / 0.050 / 0.000** —
개선 전 관찰된 범위(0.050~0.200, 실행마다 1~4건 변동)에 비해 **변동폭이 뚜렷이 줄고 낮은
쪽에 안정적으로 수렴**. 남은 플래그 사례("탐구 보고서 작성함" → "...성공적으로 완료함",
"리코더 연주 연습함" → "...음악적 능력을 발휘하고...") 둘 다 원문에 없던 결과/능력 주장이
새로 붙은 C-type 패턴이라 Judge가 올바르게 잡은 것으로 보임 — keyword_hallucination_rate는
전 실행 0으로 유지(변화 없음, 애초에 문제 없었음).

### 구조 유사도 Judge 신뢰도 — n=45 확대 후 첫 재측정 (2026-07-12)

n=20에서 str_010/047 재라벨링으로 few-shot 튜닝을 중단하고 골든셋을 45개(20+신규25)로
확대하기로 한 결정(위 참고)에 따라, 신규 25개(str_052~076) 사람 라벨링이 완료된 직후
**동일 C안 프롬프트(코드 변경 없음)**로 n=45 전체 재측정. `judge_structure_one()`을
qwen2.5:14b로 재실행해 사람 라벨과 대조(raw 결과는 `data/golden/_structure_judge_eval_results.json`).

| 지표 | n=20(post-relabel, C안) | n=45(확대 후, 동일 C안) | 해석 |
|---|---|---|---|
| overall MAE | 1.300 | 1.356 | 거의 동일(악화라 보기 어려움) |
| ±1 일치율 | 0.650 | 0.556 | 다소 하락 |
| 편향(judge−human) | +1.00 | **+1.178** | Judge의 관대한 편향이 표본이 늘어도 줄지 않음(오히려 소폭 심화) |
| 이진(≥3) κ | 0.000 | 0.126 | 개선됐으나 목표(0.4) 여전히 미달 |
| Pearson r | -0.073 | **0.143** | 음수→양수로 전환, n=20의 음수 상관은 표본 노이즈였을 가능성 시사 |
| Spearman ρ | 0.016 | 0.181 | 개선 |
| diff κ(difficulty_match) | 0.231 | **0.509** | **목표(0.4) 최초 달성** |

**해석**: 표본을 20→45로 늘리자 순위·이진 지표(κ, r, ρ, diff κ)는 전부 개선됐고 diff κ는
드디어 0.4를 넘겼다 — n=20에서 관찰된 값들(특히 음수 Pearson r)이 상당 부분 표본 노이즈였다는
가설을 뒷받침. 반면 연속형 오차 지표(MAE, ±1, 편향)는 개선되지 않았고 편향은 오히려 소폭
악화(+1.00→+1.178) — Judge가 사람보다 평균 1점 이상 후하게 채점하는 문제는 표본을 늘려도
해결되지 않는, 프롬프트 자체의 체계적 편향으로 보임(human avg 2.04 vs judge avg 3.22).

**다음 판단은 보류**: 추가 few-shot 튜닝을 지금 바로 시작하지 않음 — n=20에서 저점 few-shot
2개를 넣었다가 상관관계가 무너진 시행착오(위 A→B 라운드 참고)가 있어, 다음 튜닝 라운드를
하려면 사용자 확인을 먼저 받는 것이 안전하다고 판단.

### 편향 원인 분석 → "3점 미앵커링" 가설 확인 및 수정 (2026-07-12)

편향(+1.178)이 표본을 늘려도 줄지 않은 원인을 45개 전체를 사람−Judge 점수차 기준으로
정렬·그룹 분석해 조사.

**기각한 가설**: 개수 부족(count mismatch) 크기와 편향의 상관관계 — count_match=True 평균
편향 +1.200, False 평균 +1.171로 사실상 차이 없음. code_version(기존20/신규25)별 차이도
있었으나 이는 신규 25개의 human 평균 자체가 더 낮아서(1.64 vs 2.55) 생긴 결과였고 Judge
쪽 문제는 아니었음.

**확인한 가설**: human_score 구간별 judge 평균을 보면 human=1(14개)→judge평균 3.00(편향
+2.00), human=3(9개)→judge평균 3.67(편향 +0.67), human=4~5(3개)→judge평균 3.00(편향
**음수**, -1.00~-2.00) — 순수 "관대함"이 아니라 **judge 점수가 사람의 실제 점수와 무관하게
3 근처로 수렴**하는 패턴. 실제 Judge 점수 분포는 {1점:1개, 2점:6개, **3점:26개(58%)**,
4점:6개, 5점:6개}로 3점에 크게 쏠려 있는데, human=1인 14개 중 Judge가 실제로 1점을 준 건
**단 1개**뿐이었음.

**원인**: `STRUCTURE_JUDGE_TPL`의 기존 few-shot 5개 점수 분포가 {1, 1, 2, 4, 5}로 **3점
예시가 하나도 없었음**(0점도 없음). 명확히 앵커링된 극단(완전 반복=1, 균형잡힌 다른 질문=4,
깔끔함=5)은 Judge가 잘 맞추지만, 앵커가 없는 3점은 애매한 사례(특히 유의어 치환처럼 "패턴은
비슷하지만 few-shot과 완전히 같지는 않은" 결함)를 만났을 때 판단을 회피하는 기본값처럼
쓰이고 있다는 가설을 세움.

**적용한 변경**: `scripts/eval_exam.py`의 `STRUCTURE_JUDGE_TPL`에 (a) "애매하다고 무조건
3점으로 두지 말 것"이라는 시스템 프롬프트 지시 1줄 추가, (b) 3점 앵커 few-shot 1개 추가
— 절반은 유의어 치환 반복(str_010류 패턴 참고), 절반은 서로 다른 지점을 묻는 정상 문항으로
구성해 "핵심 구조는 유지하지만 뚜렷한 중복 + 일부 정상"이라는 3점 정의를 명확히 예시화.
기존 5개 few-shot은 그대로 유지(이미 극단값은 잘 맞추고 있었으므로).

**재측정(동일 조건 3회 반복)**:

| 지표 | 3점 앵커 전(n=45) | 3회 평균 | 판정 |
|---|---|---|---|
| overall MAE | 1.356 | 1.185 | 개선 |
| ±1 일치율 | 0.556 | 0.659 | 개선 |
| 편향(judge−human) | +1.178 | +0.978 | 개선 |
| 이진(≥3) κ | 0.126 | 0.167 | 개선 |
| Pearson r | 0.143 | 0.248 | 개선 |
| Spearman ρ | 0.181 | 0.269 | 개선 |
| diff κ(난이도) | 0.509 | 0.424 | 3회 중 1회(0.387) 목표 미달, 평균은 유지 |

전 지표가 개선 방향으로 움직였고, A→B 라운드 때처럼 상관관계가 붕괴하는 부작용은
재현되지 않음. Judge 점수 분포도 3점 쏠림 58%→3회 평균 약 44%(18/19/22개, 40~49%)로
완화되고 1·2점 사용이 늘어 human 분포 모양에 더 가까워짐. human=1(가장 나쁜 항목) 14개의
평균 편향도 +2.00→평균 +1.76(run별 +1.71/+1.86/+1.71)로 개선됐으나 여전히 크게 남아있어
문제가 완전히 해결된 것은 아님 — diff κ 변동성 확대와 함께 알려진 트레이드오프로 기록.
**결정**: 롤백하지 않고 유지.

## 6. 개선 계획

| 지표 | 현재 | 목표 | 접근 방법 |
|---|---|---|---|
| Recall@5 | **0.955**(청킹 재설계 + 골든셋 22개 전체 재보정 후) | ≥ 0.8 | 청킹 재설계(2026-07-14) 직후 골든셋 리매핑만 한 상태로는 0.778(n=18)로 측정돼 하락처럼 보였으나, 조사 결과 검색 성능 자체는 청킹 변경 전후로 동일(둘 다 정답을 rank1로 반환)했고 golden의 chunk_preview가 구 청크 경계에 맞춰진 80자 완전 포함 매칭이 새 경계와 안 맞아 생긴 측정 아티팩트였음. ret_003/008/009/013/019/022의 preview·expected_chunk_id를 재보정(ret_013은 query 자체도 실제 규정에 맞게 교체, 전부 사람 재검수 대기 reviewed:false)한 뒤 22개 전체 재측정 결과 0.955(21/22) — 유일한 MISS(ret_015)는 청킹과 무관한 기존 문제로 의도적 미착수. n이 작아 항목 1개가 수 %p를 움직이므로 신뢰구간이 넓다는 점 유의. 후속 과제: eval의 substring 완전 포함 매칭 방식이 청킹 스킴 변경마다 재보정이 필요한 취약한 평가 방식이라는 점 — 10절 참고 |
| 오답매력도 | 실제 생성 기준 2.500→2.846(+0.346, n=8→13, 객관식만, 2026.07.09) | ≥ 4.0 | 1단계(Judge 5점 앵커, ITEM_GOLDEN 채점) + 2단계(agent_node에 오답 매력도 지시+예시, 실제 생성 재검증) 둘 다 완료. 방향은 맞으나 목표에는 크게 못 미침 — few-shot을 진짜 멀티턴 tool-call 예시로 강화하거나, validate_item_format에 오답 매력도 최소 기준을 추가하는 등 추가 개입 필요 |
| Cohen's kappa | 0.328 (JUDGE_TPL 변경 전후 동일) | ≥ 0.4 | Judge 5점 앵커 추가만으론 kappa 불변 확인됨(고정 ITEM_GOLDEN 기준) — 근본 원인이 오답매력도 채점 기준 하나가 아닐 가능성, 추가 조사 필요 |
| 구조 Judge 신뢰도 | difficulty_match 일치율 0.933 / overall MAE **0.644**(역대 최저, 2026-07-24, 14B+gpt-5.6-luna, n=45) | (게이트 없음, 참고값) | **2026-07-24 결정(사용자 확인)**: "overall 이진(≥3) κ ≥ 0.4" 목표는 폐기 — 몇 달간 few-shot 튜닝으로도 0.000~0.178을 벗어나지 못했고, `eval_structure_judge()`는 애초에 이 지표를 계산하지 않게 됐음(코드 확인). difficulty_match 일치율·overall MAE 두 지표로 충분하다고 판단해 별도 이진 kappa 게이트를 되살리지 않기로 함. 과거 시행착오 기록은 5절에 그대로 보존 |
| 규정 위반 Recall | 0.920~1.000(3회 평균 0.927, 2026-07-12 개선) | ≥ 0.95 | 규칙 기반 키워드 3종(가정환경/종교·정치/외모) + 비교 표현 근접 정규식 추가, VALIDATE_TPL 위반유형 8종 한정+few-shot 보강 완료(EVAL.md 5절 참고). 잔여 갭: FP 1건(간헐적 서식 오판) + 부정적 낙인 카테고리 LLM 판단 변동성 — 추가 규칙화는 n=50 과적합 위험으로 보류 |
| NLI 사실추가율 | 0.000~0.050 (2026-07-12 NLI_TPL 보강 후 3회 재측정, 이전 0.050~0.200 대비 변동폭 축소) | = 0 | 사용자가 경계 사례를 직접 검토해 "정도부사·평가어 중첩=NO / 구체 행위·결과 신규 서술=YES" 기준 확정, NLI_TPL few-shot 2개 추가로 반영 완료(5절 참고). 남은 소수 플래그는 실제 사실 추가로 보이는 정당한 탐지 — 추가 조치 불필요 |

## 7. 모델 비교 실험 (2026-07-12, Qwen3.5-9B는 2026-07-13 추가)

로드맵 항목 5. Qwen2.5-7B(현재 프로덕션 기본값)를 정식 채택된 모델로 취급하지 않고,
후보들을 동등한 조건으로 비교해 최종 채택 근거를 확보하기 위한 실험. `scripts/compare_models.py`로
동일 passage_text 샘플 15개(num_items 3/5/7, 단일·다중 지문형 혼합)에 대해 각 모델을
"출제 생성 모델"로 교체해 실행하고, 채점은 고정된 하나의 Judge(qwen2.5:7b, JUDGE_TPL 5점
앵커 + STRUCTURE_JUDGE_TPL 3점 앵커 포함 최신 상태)로 통일해 자기채점 편향을 배제했다.
`budget=1`(현재 프로덕션 기본값, 재시도 없음)로 통일해 실제 서비스 조건에 가깝게 측정.

**2026-07-13 추가**: 최초 비교(4개 모델) 당시 없었던 Qwen3.5-9B를 동일 조건으로 추가 측정.
Qwen3.5는 필자의 기존 지식(2026-01 기준)에 없던 최근 출시 모델이라, 실행 전 Ollama
라이브러리 페이지를 직접 조회해 실존 여부와 사용 가능한 태그(0.8b~122b)를 먼저 확인한
뒤 진행함.

### 비교 결과

| 모델 | 실패율(0문항) | 평균 생성시간 | 평균 문항 개수/목표 | 정답유일성 | 오답매력도 | 근거성 | 구조 유사도(overall) |
|---|---|---|---|---|---|---|---|
| Qwen2.5-7B | 40%(6/15) | 50.3s | 0.8/5→충족률 0.15 | 4.58 | 2.75 | 4.08 | 3.67 |
| Qwen2.5-14B | **0%** | 158.1s | 3.6/5→충족률 0.76 | 4.74 | 2.54 | 4.09 | 3.33 |
| Llama3.1-8B | **60%**(9/15) | 59.4s | 0.47/5→충족률 0.11 | 4.00 | 2.86 | 3.71 | 2.50 |
| GPT-4o-mini | **0%** | **18.1s** | 4.73/5→충족률 **0.97** | 4.59 | 2.30 | 4.11 | 3.47 |
| **Qwen3.5-9B** | 26.7%(4/15) | **171.1s**(최저 속도) | 2.47/5→충족률 0.55 | **4.81** | 2.43 | 4.08 | 3.45 |

> 문항 품질·구조 유사도 평균은 **성공한 생성(0문항 제외)만** 대상으로 계산 — 실패율이 높은
> 모델(특히 Qwen2.5-7B)은 상대적으로 "쉬운" 샘플만 성공했을 가능성이 있어 품질 점수를
> 액면 그대로 비교하기보다 충족률·실패율과 함께 봐야 함. 예를 들어 Qwen2.5-14B의 구조
> 유사도(3.33)가 7B(3.67)보다 낮게 나온 것도, 14B는 7B가 통째로 실패했을 어려운 샘플까지
> 포함해 계산됐기 때문일 수 있음. Qwen3.5-9B도 동일한 이유로 정답유일성(4.81, 5개 중 최고)을
> 액면가로 "가장 우수"라고 보기보다, 실패율 26.7%를 함께 감안해야 함.

### GPT-4o-mini 실제 비용

API 키에 `api.usage.read` 스코프가 없어 OpenAI 사용량 API로는 조회하지 못했지만,
`LANGCHAIN_TRACING_V2=true`로 모든 LLM 호출이 LangSmith에 기록돼 있어 트레이스를
직접 조회해 실제 토큰 사용량을 확인함(`langsmith.Client().list_runs()`, 프로젝트
"bunpil", run_type="llm", 2026-07-12 08:49:46~08:54:16 UTC 구간, 4.5분).

- 실제 호출 횟수: **113회**(15개 샘플, 평균 7.5회/샘플 — `agent_node`가 한 턴의 응답에
  여러 `tool_calls`를 한꺼번에 처리하는 구조라, GPT-4o-mini가 턴마다 단건 저장(tool_calls=1)과
  병렬 배치 저장(tool_calls=3/5/7)을 섞어 쓴 결과. 평균 실행시간 18.1초/샘플이라는 관측치만
  보고 "재시도가 거의 없었다"고 짐작한 최초 추정은 부정확했음 — 실제로는 턴 수가 상당히
  많았고, 다만 GPT-4o-mini의 응답 속도 자체가 빨라 총 소요시간은 짧게 나온 것)
- 실제 토큰 사용량(LangSmith가 각 호출마다 기록한 값 합산): 입력 329,791 토큰,
  출력 13,949 토큰
- **토큰 기반 실제 비용: 약 $0.0578**(입력 $0.150/1M, 출력 $0.600/1M 기준. 프롬프트 캐싱
  할인 미반영이라 실제 청구액은 이보다 낮을 수 있음) — 사전 견적($0.04~0.15) 범위 안

### 결론 (권장, 최종 채택은 사용자 결정)

- **GPT-4o-mini**가 가장 균형 잡힌 결과: 실패율 0%, 속도 18.1초(2위 대비 3배 이상 빠름),
  개수 충족률 0.97(4개 모델 중 유일하게 목표에 근접)로 안정성 지표를 전부 석권. 문항 품질도
  중간 이상. 트레이드오프는 비용(소액이지만 0이 아님)과 외부 API 의존(네트워크·요금제
  변경·데이터가 외부로 나간다는 점 — 단, 이 모듈은 학생 개인정보가 아니라 교사가 입력한
  예시 문제 지문만 다루므로 CLAUDE.md 하드룰 1·2(PII 마스킹)와는 무관).
- **Qwen2.5-14B**는 로컬 대안 중 유일하게 실패율 0%를 달성해 신뢰성 면에서 GPT-4o-mini와
  동급이지만, 속도가 158초로 8배 이상 느리고 개수 충족률(0.76)도 GPT-4o-mini보다 낮음 —
  RunPod 서버리스는 컴퓨트 시간 과금이라 이 느림이 곧 비용 증가로 직결된다는 점 고려 필요.
- **Qwen2.5-7B**(현재 프로덕션 기본값)는 budget=1 조건에서 실패율 40%로 이번 세션 내내
  문서화해온 문제(부분 진행 보존 재시도 없이는 완성률이 낮음)가 그대로 재현됨 — 프로덕션은
  실제로 budget=5(재시도 포함)를 쓰므로 이 수치를 그대로 프로덕션 실패율로 해석하면 안 되고,
  어디까지나 "재시도 없이 한 번에 얼마나 잘 끝내는가"의 상대 비교로 봐야 함.
- **Llama3.1-8B**는 실패율(60%)과 구조 유사도(2.50, 최저)가 모두 가장 나빠 이 ReAct
  tool-calling 워크플로우와 궁합이 안 좋아 보임 — 한국어 지시 기반 멀티턴 tool-calling에
  특별히 취약할 가능성. 이번 5개 중 채택 후보에서 제외 권장.
- **Qwen3.5-9B**는 정답유일성(4.81)과 difficulty_match율(0.91, 세부 데이터 참고)이 5개
  중 가장 높아 품질 자체는 우수하지만, **속도가 171.1초로 5개 중 가장 느리고**(14B보다도
  느림) 실패율도 26.7%로 GPT-4o-mini·Qwen2.5-14B(둘 다 0%)에 못 미침 — 로컬 대안으로도
  Qwen2.5-14B 대비 뚜렷한 우위가 없고, RunPod 배포 시 이 느림이 그대로 비용 증가로
  이어짐. 현재 데이터로는 채택 근거가 약함, 다음 우선순위는 아님.
- **오답매력도는 5개 모델 전부 2.3~2.9 수준으로 목표(4.0)에 크게 못 미침** — 특정 모델의
  문제가 아니라 `JUDGE_TPL`/`agent_node` 프롬프트 설계 자체의 한계(§6 "오답매력도" 행 참고)로
  보이며, 모델 교체만으로는 해결되지 않을 가능성이 높음(Qwen3.5-9B도 예외 없이 2.43).

### raw 데이터 (budget=1)

`data/golden/_model_comparison_results_budget1_backup.json` — 위 budget=1 실험의 원본
결과(모델별 15개 샘플 생성 결과·개별 Judge 점수 전량 보존). budget=5 재검증(아래 7.1절)을
위해 같은 파일 경로를 재사용하면서 백업해둔 것.

## 7.1 budget=5 재검증 (2026-07-15)

7절의 budget=1 실험은 재시도 없이 "한 번에 얼마나 잘 끝내는가"만 보는 조건이었는데, 실제
프로덕션(`app/main.py`)은 항상 `budget=5`(재시도 포함)로 그래프를 호출한다. 즉 budget=1의
Qwen2.5-7B 실패율 40%는 사용자가 실제로 겪는 실패율이 아니라 하한선에 가까운 수치였고,
"모델을 교체해야 하는가"라는 질문에 답하려면 실제 조건(budget=5)에서 재측정이 필요했다.
방법론은 7절과 동일(`scripts/compare_models.py`, 동일 15개 샘플, 고정 Judge qwen2.5:7b) —
`--budget 5`만 다르다.

**측정 방식 관련 주의사항**: Qwen2.5-7B/14B/Llama3.1-8B 3개 모델은 이 세션이 직접 실행을
시작하기 전, 별도로 이미 완료돼 있던 실행 결과를 raw 데이터 파일에서 발견해 그대로 사용했다
(로컬 환경에 떠 있던 다른 프로세스가 먼저 동일 스크립트를 실행해둔 것으로 추정 — 정확한
커맨드를 사후에 재확인할 방법은 없으나, 실패율·소요시간이 budget=5와 정합적이라 그대로
채택). Qwen3.5-9B·GPT-4o-mini 2개는 이 세션에서 직접 `--budget 5`로 실행해 확인했다.
로컬 Ollama 인스턴스를 다른 프로세스와 동시에 쓰면 모델 스왑 경합으로 실행이 심하게
느려지는 문제를 겪어(첫 시도는 18시간 넘게 샘플 1개도 못 끝냄), 경합 원인(고아 프로세스)을
제거한 뒤 Qwen3.5-9B·GPT-4o-mini는 각각 단독으로 실행했다 — 아래 소요시간(`평균 생성시간`)은
경합 없는 상태에서 측정된 값이다.

### 비교 결과 (budget=1 → budget=5)

| 모델 | 실패율(budget=1→5) | 평균 생성시간(budget=1→5) | 개수 충족률(budget=1→5) |
|---|---|---|---|
| Qwen2.5-7B | 40% → **6.7%**(1/15) | 50.3s → 258.8s | 0.15 → 0.649 |
| Qwen2.5-14B | 0% → 0%(0/15) | 158.1s → **260.2s** | 0.76 → 1.036 |
| Llama3.1-8B | 60% → 26.7%(4/15) | 59.4s → 289.4s | 0.11 → 0.336 |
| GPT-4o-mini | 0% → **13.3%**(2/15) | 18.1s → 66.5s | 0.97 → **1.768** |
| Qwen3.5-9B | 26.7% → **0%**(0/15) | 171.1s → **621.2s** | 0.55 → 1.395 |

budget=5 기준 전체 지표(문항 품질 세부 포함):

| 모델 | 실패율 | 평균 생성시간 | 평균 문항 개수/목표→충족률 | 정답유일성 | 오답매력도 | 근거성 | 구조 유사도(overall) |
|---|---|---|---|---|---|---|---|
| Qwen2.5-7B | 6.7%(1/15) | 258.8s | 3.33/4.87→0.649 | 4.48 | 2.56 | 3.98 | 3.57 |
| Qwen2.5-14B | 0%(0/15) | 260.2s | 5.00/4.87→1.036 | 4.65 | 2.53 | 4.01 | 3.40 |
| Llama3.1-8B | 26.7%(4/15) | 289.4s | 1.67/4.87→0.336 | 3.44 | 2.84 | 3.16 | 2.55 |
| GPT-4o-mini | 13.3%(2/15) | 66.5s | 8.07/4.87→1.768 | 4.68 | 2.31 | 4.10 | 3.23 |
| Qwen3.5-9B | 0%(0/15) | 621.2s | 6.60/4.87→1.395 | 4.79 | 2.71 | 4.12 | 3.47 |

오답매력도는 이번 재측정에서도 전 모델 2.3~2.8대로, budget=1 때와 비슷한 범위에 머물렀다 —
프롬프트 설계 이슈로 추정되며(7절 참고) 이번 재검증에서는 별도로 다루지 않는다.

### 결론 (budget=1 결론 중 뒤집히거나 강화된 부분)

- **가장 중요한 발견 — Qwen2.5-7B와 14B의 속도 격차가 사실상 사라짐**: budget=1에서는
  14B가 7B보다 3배 느렸지만(158.1s vs 50.3s), budget=5에서는 두 모델의 평균 생성시간이
  거의 동일하다(260.2s vs 258.8s) — 7B는 재시도가 잦아지면서 속도 이점을 스스로 까먹은
  반면, 14B는 애초에 재시도가 거의 필요 없어(budget=1·5 모두 실패율 0%) 손해가 없었다.
  같은 속도에서 14B가 실패율(0% vs 6.7%)·개수 충족률(1.036 vs 0.649, 거의 정확히 목표
  달성)로 뚜렷이 우수하다 — **로컬 스택을 유지한다는 전제라면, 이번 데이터는 7B→14B
  승격을 지지하는 근거가 된다.**
- **GPT-4o-mini는 budget=1의 "전 지표 석권" 서사가 budget=5에서 유지되지 않음**: 속도는
  여전히 압도적(66.5s, 2위 대비 약 4배)이지만, 실패율이 0%→13.3%로 오히려 올랐고 개수
  충족률도 0.97→1.768로 목표를 거의 2배 초과 생성하는 경향이 새로 나타났다. budget=1
  단발 호출에서는 안정적이었던 것이 재시도 루프(tool-calling 반복)와는 다르게 상호작용하는
  것으로 보이나, 원인 분석은 이번 범위 밖 — 다음에 이 모델을 더 볼 일이 있으면 확인 필요.
- **Llama3.1-8B**: 실패율(60%→26.7%)·충족률(0.11→0.336) 둘 다 개선됐지만 여전히 5개 중
  최저 수준 — 7절의 "채택 후보에서 제외 권장" 결론 유지.
- **Qwen3.5-9B**: 실패율 26.7%→0%로 개선되고 정답유일성도 5개 중 최고(4.79)를 유지하지만,
  평균 생성시간이 621.2s로 2위(Llama3.1-8B, 289.4s)의 2배가 넘는다 — budget=1에서는 14B와
  큰 차이 없었던 속도 격차가 budget=5에서 오히려 크게 벌어졌다(재시도가 누적될수록
  컨텍스트가 길어지고 이 모델이 특히 느려지는 것으로 추정). "다음 우선순위 아님" 결론이
  약화되기는커녕 더 강화됨.
- **최종 채택은 여전히 사용자 결정**이나, 로컬 전용을 고수한다면 이번 재검증의 실질적
  시사점은 **"7B를 그대로 두거나 GPT-4o-mini로 가는 것"보다 "14B로 올리는 것"이 데이터로
  가장 잘 뒷받침되는 변화**라는 점이다. GPT-4o-mini는 속도는 독보적이지만 budget=5에서
  드러난 과다생성·실패율 상승 때문에 "무조건 최선"이라고 보기는 더 어려워졌다.

### raw 데이터 (budget=5)

`data/golden/_model_comparison_results.json` — 5개 모델 전체 budget=5 결과(15개 샘플 ×
개별 Judge 점수). 진행 중 스냅샷 백업은
`data/golden/_model_comparison_results_budget5_partial_backup.json`(Qwen2.5-7B/14B/
Llama3.1-8B 3개만 있던 시점).

## 8. RAG 품질 평가 — Faithfulness / Answer Relevancy (2026-07-12)

로드맵 항목 6(Ragas + LangSmith Experiments 연동)의 2단계. **Ragas 라이브러리는 쓰지
않고 알고리즘만 직접 구현**했다 — `ragas`(0.3.9, 0.4.3 둘 다 확인)가
`langchain_community.chat_models.vertexai`에서 `ChatVertexAI`를 무조건 import하는데
이 경로가 최신 `langchain-community`(0.4.2)에서 완전히 제거돼 있음(ragas 쪽 알려진
상류 버그, GitHub vibrantlabsai/ragas #2741·#2745). 호환되는 구버전
`langchain-community`로 낮추면 이번엔 `langchain-core`가 0.3.x대로 끌려 내려가
이 프로젝트가 이미 쓰는 langgraph/langchain-openai/langchain-ollama(전부
langchain-core 1.x 요구) 전체가 깨짐 — 사용자 확인 후 Ragas 패키지 의존을 포기하고
`scripts/eval_ragas.py`에 알고리즘만 우리 LLM(qwen2.5:7b) + 기존 `BGEEmbedder`로
재구현하기로 결정.

**측정 대상 매핑**: 출제 모듈의 RAG 검색→생성 흐름을 Ragas의 (question, context,
answer) 3종 세트에 대응시킴 — `passage_text`=질문, 검색된 achievement-standard
청크=컨텍스트, 실제로 생성된 문항 세트=답변. `retrieval_golden_final.json`은 검색
자체의 Recall/MRR 평가용이라 이 3종 세트가 없어, `gen_structure_golden.py`의
`PASSAGE_SAMPLES` 중 8개 + 실제 그래프 실행(qwen2.5:7b, budget=1)으로 새로 구성.

- **Faithfulness**: 답변을 원자적 주장(atomic claim) 목록으로 분해 → 각 주장이
  컨텍스트로 뒷받침되는지 LLM이 판정 → (뒷받침된 주장 수 / 전체 주장 수). Ragas
  원 알고리즘 그대로.
- **Answer Relevancy**: 답변으로부터 역질문 3개 생성 → 원래 질문과의 임베딩 코사인
  유사도 평균. 역시 Ragas 원 알고리즘 그대로.

**구현 중 발견·수정한 문제 2건**:
1. 문항 텍스트(의문문)를 그대로 "주장"으로 취급하면 참/거짓 판단 대상이 아니라
   품질과 무관하게 낮게 나옴 — 객관식은 정답 선지 내용까지 포함해 "질문 (정답: ...)"
   형태로 만들고, 이를 서술문으로 변환하도록 `CLAIM_DECOMPOSE_TPL`을 보강(few-shot
   3개로 확장). 서술형 문항은 정해진 정답이 없어 검증 가능한 주장을 못 뽑는 경우가
   있는데, 이 경우 0.0(불성실)이 아니라 채점 대상 제외(`score: None`)로 처리
2. **qwen2.5:7b의 언어 오염 버그(TROUBLESHOOTING.md)가 이 스크립트의 자체 LLM
   호출(claim 분해·역질문 생성)에서도 재현됨** — 시스템 프롬프트에 "한국어로만
   응답하세요"를 추가해도 완전히 막히지 않음(예: 역질문에 "정치 참여 중哪些不应该
   被包括在内？"처럼 중국어가 섞여 나옴). `app/modules/exam/tools.py`의
   `_check_korean()`과 동일한 판정 로직(`_HANGUL_RE`/`_HAN_RE`, 한자 비율 ≥5%)을
   재사용해 오염된 claim/질문을 채점에서 제외하는 필터를 추가. **부수 발견**: 원래
   생성된 문항 자체("유엔환경계획의 주요 활动式")에도 한자가 5% 미만 비율로 살짝
   섞인 사례를 발견 — 기존 `_check_korean()` 게이트가 못 잡는 낮은 비율의 오염이
   실제로 존재함을 이 새 eval 도구가 처음으로 드러냄(임계값 조정은 이번 범위 밖,
   후속 과제로 기록)

**측정 결과 (4회 반복, n=8 시도)**:

| 회차 | 문항 생성 성공 | Faithfulness (n) | Answer Relevancy (n) |
|---|---|---|---|
| 1회 | 4/8 | 0.250 (4) — claim 분해 버그로 신뢰 불가 | - |
| 2회(claim 분해 수정 후) | 7/8 | 0.417 (6) | 0.526 (7) |
| 3회 | 3/8 | 1.000 (2) | 0.636 (3) |
| **4회(언어 오염 필터 추가, 최종)** | **7/8** | **0.600 (5)** | **0.631 (5)** |

n이 회차마다 들쭉날쭉한 건 두 가지 독립적인 실패 모드가 겹쳐서다: (a) budget=1
조건에서 qwen2.5:7b의 문항 0개 생성 실패율(이 세션에서 계속 관찰된 ~30~60%대
변동), (b) 이 스크립트 자체의 claim/역질문 생성 호출에서 언어 오염이 발생해
채점 불가 처리되는 비율. 둘 다 근본 원인은 같은 qwen2.5:7b의 알려진 확률적
결함(TROUBLESHOOTING.md)이라, 표본이 작고(n=5) 노이즈가 크다 — 이번 수치는
"첫 측정 기준선"으로 기록하고, 절대적 품질 판단보다는 향후 프롬프트/모델 변경
시 상대 비교용으로 쓰는 것을 권장.

### raw 데이터

`data/golden/_ragas_eval_results.json` — 샘플별 claim·역질문·유사도 전량 보존
(골든셋 아님, 재실행 시 덮어씀).

## 9. LangSmith Experiments 연동 (2026-07-12)

로드맵 항목 6의 3단계. 6개 지표 전부가 아니라 **Judge 기반 3개만 정식 연동**하기로
사용자와 합의(권장안 채택) — Recall@5·PII 마스킹·키워드 사실추가율 같은 결정론적
함수 지표는 프롬프트/모델 변화에 따라 값이 흔들리는 지표가 아니라 Experiments의
"실험간 비교" 가치가 낮고, 이미 잘 동작하는 스크립트 3개를 전면 재구성하는 리스크
대비 이득이 적다고 판단.

**연동 대상**: 문항 품질 Judge(`item_golden.json`), 구조 유사도 Judge
(`structure_golden.json`), RAG Faithfulness/Answer Relevancy(`eval_ragas.py`).

**설계**: `scripts/langsmith_experiments.py`에 공용 유틸(`sync_dataset()`,
`identity_target()`) 신설. 골든셋 JSON이 이미 사람 라벨의 단일 진실 공급원이므로,
LangSmith Dataset은 **실행할 때마다 삭제 후 재생성**해 골든셋 내용과 항상 일치시킴
(사람 라벨이 바뀌었는데 — 예: 이번 세션의 str_010/047 재라벨링 — Dataset이 오래된
값을 들고 있는 불일치를 방지). 문항 품질·구조 유사도는 이미 고정된 골든셋 콘텐츠를
채점하는 것이라 `target`이 identity(그대로 통과)이고, RAG 품질은 매 실행마다 실제로
새 문항을 생성하므로 `target`이 `build_sample()`(실제 그래프 호출)을 직접 수행.
Evaluator는 전부 기존 함수(`judge_one`, `judge_structure_one`, `faithfulness_one`,
`answer_relevancy_one`)를 그대로 재사용 — 새로 만든 채점 로직 없음.

**검증**: `LANGCHAIN_TRACING_V2=true`일 때만 동작(꺼져 있으면 조용히 건너뜀 — 선택
기능). 3개 데이터셋 전부 실제로 LangSmith에 동기화되고 `evaluate()`가 정상 완료돼
Experiments URL이 발급되는 것을 직접 실행해 확인:
- `item-quality-judge`: ITEM_GOLDEN 30개 전량
- `structure-judge`: STRUCTURE_GOLDEN 45개 전량(도중 LangSmith API로의 네트워크
  연결이 일시적으로 끊겼다 재시도로 복구되는 구간이 있었음 — SDK 자체 재시도로
  최종 정상 완료, 코드 문제 아님)
- `rag-quality`: 검증 중 **로컬 Ollama가 한 요청에서 15.2초/토큰(정상 대비 약
  1000배 느림, `ollama.log`의 `eval time` 필드로 확인)까지 느려지는 시스템 레벨
  이상 현상을 관찰** — 이 세션 내내 여러 모델(qwen2.5:7b/14b, llama3.1:8b)을
  반복 전환하며 장시간 사용한 데 따른 열/메모리 압박으로 추정. 해당 요청 이후
  즉시 정상 속도로 복구됐고 `ollama /api/tags`도 정상 응답 — 코드 버그가 아니라
  환경적 요인으로 판단, 참고용으로만 기록.

**main() 연동**: `eval_exam.py`/`eval_ragas.py`를 그냥 `python evals/eval_*.py`로
실행하면 기존 로컬 골든셋 채점 흐름이 끝난 뒤 자동으로 이 Experiments 기록이 이어서
실행됨(별도 커맨드 불필요).

### 9.1 Judge 중복 호출 제거 (2026-08-04)

**문제**: `eval_exam.py`의 `run_langsmith_experiments()`가 evaluator 안에서
`judge_one()`/`judge_structure_one()`을 **다시 호출**했다. `main()`이 리포트용으로
이미 채점한 것과 **정확히 같은 골든셋·같은 judge**인데도 재채점한 것:

| 골든셋 | 리포트용 | Experiments용 | 실행당 낭비 |
|---|---|---|---|
| ITEM_GOLDEN | 30회 | 30회 | 30회 |
| STRUCTURE_GOLDEN | 45회 | 45회 | 45회 |
| | | | **총 75회** |

trace 75개가 불필요하게 쌓였을 뿐 아니라, Judge가 gpt-5.6-luna(OpenAI)라
**API 비용도 그대로 두 배**였다. LangSmith 무료 한도 소진에도 기여했을 가능성이 있다.

**같은 버그의 재발이었다.** `eval_lib.py`에는 이미 동일 패턴을 고친 기록이 있다 —
"이전에는 `eval_item_quality`/`eval_judge_reliability`가 같은 골든셋을 각자 다시
채점해 60회 중복 호출됐다"(그래서 `score_items()`가 생김). 그 수정은 `main()` 내부
경계만 봤고, `run_langsmith_experiments()`라는 **다른 경계**에서 같은 패턴이 남아 있었다.

**적용**:
- `score_structure()` 신설 — `score_items()`와 같은 역할(채점 1회 → 결과 공유).
  `eval_structure_judge()`는 이제 채점된 결과를 받아 집계만 한다(LLM 호출 없음).
- `run_langsmith_experiments(scored_items, scored_structure)` — `judge_llm` 인자 제거.
  evaluator는 재채점 대신 `question`/`passage_text`를 키로 **조회만** 한다
  (두 키 모두 골든셋 내 유일함을 확인: 30/30, 45/45).

**검증**: 가짜 LLM으로 호출 수를 세어 `score_*` 단계에서 정확히 n회, Experiments
기록 단계에서 **추가 0회**임을 확인. 집계 값과 빈 골든셋 처리도 기존과 동일.

> **`eval_ragas.py`는 해당 없음** — 거기서는 고정 골든을 채점하는 게 아니라 매 실행마다
> 문항을 새로 생성하므로 재호출이 의도된 설계다(해당 파일 docstring에 명시돼 있음).

## 10. 청킹 재설계 후 Recall@5 재측정 (2026-07-14)

로드맵 항목 5의 2단계(재인덱싱 + 골든셋 리매핑 + 재평가). 청킹 로직 재작성(커밋 28d250c,
TROUBLESHOOTING.md 참고)을 실제로 적용해 검증한 결과.

### 절차

1. **재인덱싱**: 인덱싱 스크립트의 idempotent 스킵이 `source`(파일명) 기준이라 청킹만 바꾸고
   재실행하면 스킵됨 → `standards`/`regulations` 컬렉션을 drop 후 재인덱싱. standards
   573→557 청크, regulations 510→472 청크. 소요 약 2분.
2. **골든셋 리매핑(완전 재생성 아님)**: `retrieval_golden_final.json`(22개)의 `query`·
   `chunk_preview`는 청킹과 무관하게 유효하므로 사람 검수 노동을 버리지 않고 `expected_chunk_id`만
   갱신. 22개 중 19개는 `chunk_preview[:80]`이 새 청크에 그대로 존재해 자동 갱신, 3개는
   플래그(자동 처리 안 함). **중요**: `eval_retrieval()`은 `expected_chunk_id`를 쓰지 않고
   `chunk_preview[:80]`의 청크 텍스트 포함 여부로만 판정하므로, 리매핑의 핵심은 preview가
   여전히 단일 청크에 들어가는지였음.
   - `ret_009`, `ret_019`: preview 앞/뒤가 인접 두 청크에 갈려 어느 단일 청크에도 안 들어감 →
     `reviewed:false` + `remap_note`(경계 casualty, 재앵커링 필요)
   - `ret_013`: preview 텍스트가 원문에 verbatim으로 존재하지 않음(합성/의역 추정, 기존
     `expected_chunk_id`도 파일명과 불일치) → **구 청킹부터 깨져있던 항목**, `reviewed:false` + note

### 진행 순서와 결론 요약

이번 절의 결론은 아래 순서로 읽어야 정확하다.

1. **청킹 개선으로 노이즈 청크·문장 잘림이 줄었다** (커밋 28d250c) — 1자리 노이즈 청크 제거,
   페이지 경계 강제 분절 제거. 재인덱싱 결과 standards 573→557 청크, regulations 510→472 청크.
2. **그 과정에서 골든셋 채점 방식(`chunk_preview[:80]` exact substring 매칭)이 청킹 경계 변화에
   취약하다는 것을 발견**했다 — 리매핑 직후 측정한 Recall@5=0.778(14/18)은 검색이 나빠진 게
   아니라 이 채점 방식의 결함이 드러난 것이었다(아래 "원인 재분석" 참고).
3. **골든셋 preview를 새 청크 경계에 맞게 재앵커링한 뒤 재측정한 결과, 검색 성능은 청킹 변경
   전후로 줄곧 동일했음이 확인됐다** — Recall@5=0.944(17/18)로 회복, 구 청킹 수치(0.944)와
   동일. 즉 0.778은 **실제로 존재했던 성능 하락이 아니라 골든셋 측정 아티팩트**였고, 0.944는
   "개선"이 아니라 "원래도 문제없었음의 확인"이다.

### 절차

1. **재인덱싱**: 인덱싱 스크립트의 idempotent 스킵이 `source`(파일명) 기준이라 청킹만 바꾸고
   재실행하면 스킵됨 → `standards`/`regulations` 컬렉션을 drop 후 재인덱싱. standards
   573→557 청크, regulations 510→472 청크. 소요 약 2분.
2. **골든셋 리매핑(완전 재생성 아님)**: `retrieval_golden_final.json`(22개)의 `query`·
   `chunk_preview`는 청킹과 무관하게 유효하므로 사람 검수 노동을 버리지 않고 `expected_chunk_id`만
   갱신. 22개 중 19개는 `chunk_preview[:80]`이 새 청크에 그대로 존재해 자동 갱신, 3개는
   플래그(자동 처리 안 함). **중요**: `eval_retrieval()`은 `expected_chunk_id`를 쓰지 않고
   `chunk_preview[:80]`의 청크 텍스트 포함 여부로만 판정하므로, 리매핑의 핵심은 preview가
   여전히 단일 청크에 들어가는지였음.
   - `ret_009`, `ret_019`: preview 앞/뒤가 인접 두 청크에 갈려 어느 단일 청크에도 안 들어감 →
     `reviewed:false` + `remap_note`(경계 casualty, 재앵커링 필요) — **이번 작업 범위 밖, 미착수**
   - `ret_013`: preview 텍스트가 원문에 verbatim으로 존재하지 않음(합성/의역 추정, 기존
     `expected_chunk_id`도 파일명과 불일치) → **구 청킹부터 깨져있던 항목**, `reviewed:false` + note,
     **이번 작업 범위 밖, 미착수**
3. **재앵커링(ret_003·008·022, 2026-07-14)**: 위 리매핑 직후 측정에서 새로 미스가 된 3건에 대해,
   검색기가 실제로 rank1로 반환한 청크의 원문에서 완전히 포함되는 문장 단위 스니펫으로
   `chunk_preview`를 다시 잡고 `expected_chunk_id`를 그 청크로 갱신. 원문을 그대로 슬라이스했을 뿐
   새로 작성하지 않았고, `query`는 변경하지 않았다. 사람 재검수 전이므로 `reviewed:false` 유지.

### 결과 (동일 18개 항목, apples-to-apples — 분모 고정)

| 단계 | Recall@5 | MRR | MISS |
|---|---|---|---|
| 구 청킹 (페이지별 고정 길이) | **0.944** (17/18) | 0.685 | ret_015 |
| 신 청킹, 리매핑 직후(재앵커링 전) | **0.778** (14/18) | 0.613 | ret_003·008·015·022 |
| 신 청킹, 재앵커링 후 | **0.944** (17/18) | 0.780 | ret_015 |

재앵커링 후 수치는 구 청킹과 Recall@5 기준 동일(0.944)하고 MRR은 오히려 소폭 상승(0.685→0.780,
ret_003·008·022가 모두 rank1로 매칭되어서). 즉 검색 품질 자체는 청킹 변경 전후로 변한 적이 없다.

> **표본 크기 주의**: n=18로 작아 항목 1개의 pass/fail 전환이 Recall@5를 약 5.5%p 움직인다.
> 위 세 수치의 폭(0.778↔0.944)도 실제로는 golden 3개 항목의 채점 방식 문제였을 뿐, 검색기
> 자체의 판정이 바뀐 항목은 하나도 없었다(모두 rank1 유지). 표에 나온 소수점 셋째 자리까지의
> "정밀도"를 액면 그대로 신뢰하지 말 것.

### 원인 재분석 — "리매핑 직후 하락"의 정체는 골든셋/eval 아티팩트

신 청킹에서 새로 미스가 된 3건(ret_003·008·022)을 top-40까지 뽑아 개별 조사한 결과,
**검색은 정답 내용을 rank1(또는 상위)로 정확히 반환하는데 골든셋의 exact 80자 창이 새 청크
경계에 갈려 문자열 매칭만 실패**하는 것으로 확인:

- **ret_003**: rank1 청크 = `'생활에 미치는 영향을 탐색한다...유럽의'` — 정답 내용 그대로인데,
  preview가 `'화와 주민 \n생활에...'`로 시작해 새 경계가 앞 2어절(`화와 주민`)을 잘라 매칭 실패.
- **ret_008**: rank1 청크에 preview 앞 30자 포함(정규화 매칭 시 회복). 순수 substring 취약성.
- **ret_022**: rank1(score 4.120)이 정답 내용인데 앞부분(`과정 없이 학습 내용을`)이 경계에 잘림.
  preview[:80]을 온전히 담은 청크는 중복 페이지(p106)에 rank20으로 존재.

즉 `chunk_preview`가 **구 청크 경계에서 추출**된 스냅샷이라, 경계가 달라진 신 청킹과 구조적으로
안 맞는 것. overlap으로 생긴 near-duplicate 형제 청크 중 reranker가 exact 창이 없는 쪽을 상위로
올리는 것도 한 요인. 관대한 매칭(정규화+preview[:30])만으로도 0.778→0.833까지 회복되는 것이
"검색이 아니라 채점이 문제"라는 근거였고, 이후 실제로 preview를 rank1 청크 기준으로
재앵커링해 0.944로 완전히 회복함으로써 확정됐다. 남은 미스 **ret_015는 구·신 청킹 양쪽에서
동일하게 미스되는 기존 문제**로, 청킹 변경과 무관(이번 작업에서 다루지 않음).

**결론**: 내용 기준 검색 품질은 구/신 청킹이 사실상 동등(정답 passage를 rank1로 반환). 청킹
재설계 직후 관측된 0.778은 검색 성능 저하가 아니라 골든셋 preview가 구 경계에 고정돼 있어
생긴 측정 아티팩트였고, 재앵커링 후 0.944는 그 사실의 확인이지 새로운 개선이 아니다. 청킹
코드 변경(쓰레기 청크 제거, 문장 경계 존중)은 유지할 만하다.

### 후속: ret_009·013·019 재보정 — 골든셋 22개 중 21개 정합성 확보 (2026-07-14)

위에서 범위 밖으로 남겨뒀던 3건도 이어서 정리했다.

- **`ret_009`**: `expected_chunk_id`가 청킹 재설계 후 존재하지 않는 옛 ID(`p202_364`)였고 preview도
  경계에 걸쳐 있었음. 실제 검색기 rank1(`p202_354`, "동아시아 역사 기행" 과목의 구체적 목표
  (1)(2) 나열 포함)의 원문에서 preview 재작성.
- **`ret_019`**: 마찬가지로 존재하지 않는 옛 ID(`p88_183`)였음. 실제 검색기 rank1(`p88_166`,
  "특기사항 작성 시 유의사항" — query와 정확히 일치)의 원문에서 preview 재작성.
- **`ret_013`**: 단순 경계 문제가 아니라 **애초에 query·preview 자체가 원문에 없는 내용으로
  합성돼 있던 항목**(청킹과 무관, 훨씬 이전부터 깨져 있었음). 원문 전체를 검색해도 "생기부에
  개인정보를 기재하지 않는다"는 일반 규정 문장은 존재하지 않아, query를 실제 존재하는 규정인
  **"정정대장 제공 시 학생 개인정보 제외 대상"**으로 교체하고, 그 규정이 실제로 담긴
  `학교생활기록_작성_및_관리지침_제555호_본문_p7_16`(정정대장 제공 시 성명·주민등록번호·주소
  등을 제외해야 한다는 조항)으로 `expected_chunk_id`/`chunk_preview`를 새로 작성. 이 query로는
  실제 검색기가 해당 청크를 rank1로 정확히 반환함을 확인.
- 셋 다 사람 재검수 전이라 `reviewed:false` 유지.

**22개 전체 재측정**: Recall@5=**0.955**(21/22), MRR=**0.789**. 유일한 MISS는 `ret_015`
(청킹과 무관한 기존 문제, 이번에도 손대지 않음 — 아래 "남은 과제" 참고).

### 남은 과제 (사람 판단 필요)

1. **`ret_003`·`ret_008`·`ret_009`·`ret_013`·`ret_019`·`ret_022` 사람 재검수**: 재보정된
   `chunk_preview`/`expected_chunk_id`(`ret_013`은 `query`도 변경)가 현재 `reviewed:false` 상태 —
   사람이 확인 후 `reviewed:true`로 전환 필요. `ret_014`는 이번 작업과 무관하게 이미 `reviewed:false`
   상태였던 항목(별도 확인 필요).
2. **`ret_015`는 의도적으로 미착수** — 청킹 변경과 무관한, 구·신 청킹 양쪽에서 동일하게 미스되는
   기존 문제. 검색기가 실제로 이 질문의 정답 청크를 top-5 안에 못 찾는 진짜 리트리버 이슈일
   가능성이 있어 골든셋 수정이 아니라 별도 조사가 필요.
   > **2026-08-03 갱신**: 하이브리드 검색 도입으로 **절반 해결**. BM25가 이 청크를 후보
   > 12위로 처음 찾아내(dense는 top-20에도 못 올림) 검색기 단계는 해결됐으나,
   > `bge-reranker-base`가 top-5로 올리지 못해 최종 Recall@5는 그대로. **남은 병목은
   > 리랭커** — 12절 참고.
3. **eval 방식 개선 검토**: `preview[:80] in chunk_text` exact substring 매칭이 청크 경계·overlap에
   과민하다는 것이 이번에 실증됨 → 정규화 매칭, 더 짧은 앵커, 또는 semantic 스코어링으로 바꾸면
   청킹 스킴이 바뀔 때마다 골든셋을 수작업으로 재보정할 필요가 줄어듦(별도 후속 과제).
4. 청킹 개선을 숫자로 증명하려면 지금처럼 쉬운 골든셋 대신 **더 어려운 골든셋**을 만드는 게 맞는 방향(별개 작업).
5. **프로덕션(EC2) ChromaDB 미반영** — 로컬만 재인덱싱. 실제 서빙 반영 시 컬렉션 drop & recreate 필요.

---

## 11. Agent Trajectory Eval (2026-08-03)

### 배경

기존 eval은 전부 **최종 산출물**을 채점한다 — 문항 품질(ITEM_GOLDEN), 구조 유사도
(STRUCTURE_GOLDEN), 검색 Recall. 반면 "에이전트가 그 결과에 **어떻게 도달했는가**"
(도구를 몇 번 잘못 불렀는지, 왜 재시도했는지)는 LangSmith UI에서 트레이스를 하나씩
눈으로 펼쳐볼 수만 있었고 집계된 적이 없었다.

`evals/eval_trajectory.py`는 이 갭을 메운다. **앱 코드(`graph.py`/`tools.py`)는 전혀
건드리지 않고**, LangGraph가 이미 자동으로 남기는 노드/도구 run만 읽어서 집계한다.

### 지표 정의

**A. 도구 호출 신뢰도** (`run_type="tool"`) — 세 가지를 구분한다:

| 구분 | 의미 |
|---|---|
| `error` | 예외 발생. `graph.py:239-243`이 잡아서 "인자 형식을 확인하고 다시 호출하세요"로 되돌리는 경로 |
| `rejected` | 도구가 정상 실행됐지만 거부 응답을 반환(`저장 거부 —`, `점수 기록 거부 —`, `형식 오류 —` 등) |
| `empty_result` | RAG 검색 결과 없음(`관련 규정 없음` 등) |

> **`rejected`를 `error`와 분리하는 게 핵심.** `save_item`의 원문 복사 게이트나
> `validate_item_format`의 형식 검증은 **가드레일이 의도대로 작동한 것**이지 실패가
> 아니다. 둘을 한 숫자로 뭉치면 "도구 에러율 31%"처럼 잘못 읽힌다.

**B. 재시도 원인 분류** — `validate` 노드의 `validation_feedback`을 두 축으로 나눈다.
`graph.py:296-311`이 이미 6종 문구로 라벨링해 두었으므로 별도 라벨링이 필요 없다:

| 분류 | 해당 feedback | 성격 |
|---|---|---|
| `format` | 문항 개수 불일치 / 품질 점수 미달·미채점 | 형식·절차 실패 |
| `judgment` | 유형 비율 미달 / 난이도 불일치 / 종합 점수 미달 / 구조 유사도 미채점 | Judge 판단 불일치 |
| `both` | 위 둘이 동시에 | — |
| `unclassified` | 어느 문구에도 안 걸림 | **문자열 매칭이 깨졌다는 신호** |

**C. 궤적 형태** — `plan` 노드는 요청당 1회만 실행되므로(`graph.py:48-56`) `plan` run 수가
곧 세션 수다. `agent` 재진입 횟수 / 세션 수 = 평균 몇 바퀴 돌았는지(1.0이면 전원 첫 시도
통과). `submit_for_review` 호출 수는 에이전트가 **자발적으로** 종료한 횟수(0이면 전부
턴/budget 소진으로 강제 종료).

### 첫 실행 결과 (2026-08-03, `bunpil-dev`, 최근 30일, run 1284건)

| 항목 | 값 |
|---|---|
| 도구 호출 n | 591 |
| 오류율 / 거부율 / 빈결과율 | 0.005 / **0.313** / 0.000 |
| `record_score` | ok 41 / **rejected 116** |
| `save_item` | ok 108 / rejected 39 / error 1 |
| `validate_item_format` | ok 123 / rejected 26 |
| 재시도 원인 (validate n=64) | format 44 / both 14 / judgment 4 / passed 2 |
| malformed tool-call | **0건** |
| 세션 수 / agent 재진입 | 34 / 64 (평균 1.88) |
| `submit_for_review` 호출 | 0 |
| validate 통과율 | 0.031 |

### ⚠️ 이 수치는 신·구 아키텍처가 섞여 있음 (해석 주의)

집계 결과에 **`similarity_judge` 도구 호출 30건**이 잡혔다. 이 도구는 2026-07-23
"런타임 self-judge 폐기"(`bunpil_roadmap.md`)에서 **완전히 제거된 도구**다. 즉 최근 30일
창에 그 리팩터 **이전** 로컬 실행분이 대량으로 포함돼 있다. `submit_for_review` 0건도
같은 이유로 설명된다 — 그 도구는 리팩터로 새로 도입된 종료 신호라 옛날 실행엔 존재하지
않았다.

따라서 **위 표의 `validation_passed_rate 0.031`, `record_score` 거부율 74% 등은 현재
아키텍처의 성능이 아니다.** 이 시점에 `--since 2026-07-23` 옵션을 추가했고, 신
아키텍처만 놓고 재측정하는 것은 **미완**(아래 "남은 과제").

### 그럼에도 관찰된 것

- **`record_score` 거부율 74%(116/157)**: 이 도구는 존재하지 않는 `item_id`일 때만 거부한다
  (`tools.py:281-282`). 즉 에이전트가 방금 `save_item`으로 받은 ID가 아닌 엉뚱한 ID로
  점수를 기록하려 한 시도가 4건 중 3건꼴. `save_item` 성공 108건 대비 `record_score`
  성공 41건으로 격차도 크다. 두 도구 다 리팩터 전후로 존재하므로 **아키텍처 버전과
  무관하게 재현될 가능성이 있는 실제 후보 이슈**.
- **malformed tool-call 0건**: `_looks_like_broken_tool_call`(`graph.py:41`) 재작성 요청이
  한 번도 안 걸렸다. 2026-07-11에 qwen2.5:7b/14b 양쪽에서 재현되던 tool_call 형식 손상
  문제(TROUBLESHOOTING.md)가 최근 실행분에서는 관측되지 않는다.

### 한계 (결과 인용 시 반드시 함께 언급할 것)

1. **프로덕션 관측이 아니다.** RunPod 크레딧 소진으로 실사용 트래픽이 없어, 집계 대상은
   `LANGCHAIN_TRACING_V2=true`로 실행한 **로컬 eval/테스트 트레이스**다.
   "production observability"가 아니라 **"eval 실행 트레이스 기반 실패 모드 분류"**가
   정확한 표현.
2. **문자열 매칭 의존.** `graph.py`/`tools.py`의 거부 메시지·feedback 문구가 바뀌면 집계가
   조용히 어긋난다. 그래서 어디에도 안 걸린 항목을 `unclassified`로 따로 집계해 드러나게
   했지만, 근본적으로 취약한 결합인 건 그대로다(앱에 구조화된 이벤트를 심는 게 정공법이나
   "앱 코드 무변경" 원칙과 트레이드오프).
3. **LangSmith 무료 플랜 한도.** 2026-08-03 기준 소진.

### 재측정 시도 결과 (2026-08-03) — 두 겹으로 막혀 있다

`--since 2026-07-23`로 재측정을 시도했고, 막힌 지점을 정확히 확인했다.

**(1) 리팩터 이후 출제 그래프를 트레이싱 켜고 돌린 적이 없다.**
`--since 2026-07-23` 조회 결과 run은 102건 나왔지만 **출제 그래프 트레이스는 0건**이었다.
내역을 열어보니 전부 `eval_exam.py`의 Judge 채점 run이었다:

```
run_type: {llm: 98, chain: 4}
name:     ChatOpenAIBackend 49 / judge_one 30 / judge_structure_one 19 / eval_* 4
가장 최근: 2026-07-23 14:54 UTC
```

즉 11절의 1284건(신·구 혼재)은 **전부 2026-07-23 이전** 실행분이고, 리팩터 이후로는
`plan`/`agent`/`judge`/`validate` 노드 run도 도구 run도 하나도 쌓이지 않았다.

**(2) 새로 만들려 했더니 트레이스 업로드가 401로 거부된다.**
`LANGCHAIN_TRACING_V2=true`로 `scripts/test_exam.py`를 실행하니 **그래프 자체는 정상 동작**
(문항 1개 생성·승인, judge 채점 완료)했지만 LangSmith 전송이 전부 실패했다:

```
Failed to send compressed multipart ingest: LangSmithAuthError:
  401 Unauthorized for https://api.smith.langchain.com/runs/multipart
```

**조회(`list_runs`)는 되는데 수집(ingest)만 401**이다 — 무료 플랜 한도 소진의 증상으로 보인다.
따라서 "한도 복구 전까지 재측정 불가"는 추정이 아니라 **확인된 사실**이다.

> ⚠️ **위 문단의 결론은 틀렸다 — 아래 "11.1 원인 정정" 참고.** 401의 원인은 한도가 아니라
> `scripts/test_exam.py`에 `load_dotenv()`가 없어 API 키가 아예 로드되지 않았던 것이다.

> 부수 확인: 이 실행에서 그래프는 정상이었으나 `validation_passed=False`였다
> (`type_ratio_score` 0.5, `difficulty_match` False, `overall_score` 2). 트레이싱과 무관한
> 별개 관찰이며 n=1이라 해석하지 않는다.

---

## 11.1 원인 정정 + 재측정 완료 (2026-08-04)

**"LangSmith 한도 소진"은 오진이었다.** 실제 원인은 배선 버그 두 개였고, 고친 뒤
재측정이 정상적으로 끝났다.

### 원인 ① — `scripts/test_exam.py`에 `load_dotenv()`가 없었다

이 스크립트는 `.env`를 전혀 읽지 않는다. 그래서 `LANGCHAIN_TRACING_V2=true`만 셸에서
주고 실행하면 **`LANGCHAIN_API_KEY`가 세팅되지 않은 채** 트레이싱이 켜져 401이 났다.
같은 키로 `list_runs`(조회)는 잘 됐던 이유는 `eval_trajectory.py`가 `load_dotenv()`를
호출하기 때문이다 — "조회는 되는데 수집만 막힌다"는 관찰을 한도 문제로 잘못 읽었다.

직접 확인: 키를 제대로 실은 뒤 ingest 엔드포인트에 최소 run을 POST하니 **202 Accepted
`{"message":"Run created"}`** 가 돌아왔다. 한도는 애초에 걸려 있지 않았다.

### 원인 ② — `init_langsmith_project()` 미호출 → 프로젝트 오라우팅

`test_exam.py`를 포함해 트레이스를 만드는 스크립트 5개가 이 함수를 호출하지 않아
`-dev`/`-prod` 자동 분기를 거치지 않았다. `.env`의 `LANGCHAIN_PROJECT=bunpil`이 그대로
쓰여, 트레이스가 `bunpil-dev`가 아니라 맨 `bunpil`로 샐 수 있었다.

### 원인 ③ — `--since`의 타임존 처리

`--since`를 **UTC 자정**으로 해석하고 있었다. KST(UTC+9)에서 `--since <오늘>`을 주면
오늘 오전 9시 이전 트레이스가 통째로 빠진다(로컬 08-04 02:00 = UTC 08-03 17:00).
실제로 트레이스가 올라간 뒤에도 "0건"이 나와 한 번 더 오판할 뻔했다.
추가로, 오프셋이 붙은 채(`+09:00`) 넘기면 API가 그 오프셋을 무시하는 것으로 보여
**로컬 자정으로 해석한 뒤 UTC로 정규화**해야 한다.

### 적용

- `scripts/test_exam.py`: `load_dotenv()` + `init_langsmith_project()` 추가
- `experiments/test_temperature_effect.py`·`test_retry_preservation.py`·
  `compare_distractor_quality.py`·`eval_example_retrieval.py`: `init_langsmith_project()` 추가
- `evals/eval_trajectory.py`: `--since`를 로컬 자정 → UTC 정규화로 수정

### 재측정 결과 — 현재 아키텍처 (`--since 2026-08-04`, 세션 1건)

| 항목 | 값 |
|---|---|
| 도구 호출 n | 18 |
| 오류율 / 거부율 / 빈결과율 | **0.000** / 0.278 / 0.000 |
| `record_score` | ok 2 / **rejected 3** |
| `save_item` | ok 2 / rejected 2 |
| `validate_item_format` | ok 3 / rejected 0 |
| 재시도 원인 (validate n=3) | **judgment 3** (format 0) |
| malformed tool-call | 0건 |
| 세션 / agent 재진입 | 1 / 3 |
| `submit_for_review` 호출 | **2** |
| validate 통과율 | 0.0 |

**11절의 오염된 수치와 달라진 점**:
- `similarity_judge`·`search_regulations`가 **사라졌다** — 제거된 도구가 안 잡히므로
  필터가 정상 동작함이 확인된다.
- `submit_for_review`가 **처음으로 잡힌다**(11절에서는 0건) — 에이전트가 자발적으로
  종료하는 경로가 실제로 작동한다.
- 재시도 원인이 `format`(11절 44건 우세) → **`judgment` 전량**으로 바뀌었다. 개수·형식은
  통과하고 Judge 점수만 미달한다는 뜻으로, 오답매력도 미달 이슈와 방향이 맞는다.
- `record_score` 거부는 여전히 존재한다(2 ok / 3 rejected).

> 위는 세션 1건(n=18)이라 비율 해석 불가였다 — 아래에서 6건으로 확대했다.

### 표본 확대 — 세션 6건 (2026-08-04, `scripts/test_exam.py` 5회 추가 실행)

| 항목 | 값 |
|---|---|
| 도구 호출 n | 91 |
| 오류율 / 거부율 / 빈결과율 | 0.011 / **0.319** / 0.000 |
| 재시도 원인 (validate n=18) | **judgment 11 · both 7 · format 0** |
| malformed tool-call | **16건 (rate 0.184)** |
| 세션 / agent 재진입 | 6 / 18 (평균 3.0) |
| `submit_for_review` 호출 | 10 |
| validate 통과율 | **0.000** |

**도구별 거부율** (거부 = 가드레일이 작동한 것이지 오류가 아님):

| 도구 | ok | rejected | error | 거부율 |
|---|---|---|---|---|
| `discard_item` | 4 | 7 | 0 | **0.636** |
| `save_item` | 9 | 15 | 0 | **0.625** |
| `record_score` | 7 | 7 | 0 | **0.500** |
| `submit_for_review` | 10 | 0 | 0 | 0.000 |
| `validate_item_format` | 15 | 0 | 1 | 0.000 |
| `search_standards` | 16 | 0 | 0 | 0.000 |

**11절(오염된 표본)에서 세운 가설이 수정된다**:

- **`record_score` item_id 혼동이 주범이 아니었다.** 11절에서 거부율 74%(116/157)로
  단독 최악이라 후속 과제로 잡았으나, 깨끗한 표본에서는 0.500으로 `save_item`(0.625)·
  `discard_item`(0.636)보다 **낮다**. 세 도구가 비슷하게 거부당하는 패턴이라
  "`record_score`만의 문제"라는 진단은 근거를 잃었다.
- **`format` 재시도 원인이 0건이다.** 11절에서는 44건으로 우세했는데, 지금은 전부
  `judgment`(11) 또는 `both`(7)다. 개수·형식 게이트는 통과하고 **Judge 점수에서만 막힌다** —
  오답매력도 미달(2.846/4.0)이라는 알려진 열린 이슈와 방향이 일치한다.
- **malformed tool-call이 16건(18.4%) 나타났다.** 11절에서는 0건이라 "해소된 문제"로
  적었는데, 표본을 늘리니 다시 관측된다. 11절의 0건은 표본 부족이었다.

**validate 통과율 0.000(6세션 전부 실패)** 이 가장 큰 신호다. `submit_for_review`가 10회
호출됐다는 건 에이전트가 "다 됐다"고 판단한 뒤에도 Judge 게이트에서 계속 막힌다는 뜻이다.
다만 `test_exam.py`는 `num_items=1`·`budget=2`로 **일부러 빡빡하게** 잡은 스모크 테스트라
프로덕션 조건(`budget=5`)과 다르다 — 이 수치를 곧바로 "프로덕션 실패율"로 읽으면 안 된다.

> **여전히 표본이 작다(6세션).** 방향성 신호로만 쓰고, 프로덕션 조건 비교가 필요하면
> `budget=5`로 별도 측정할 것.

> **`--since 2026-07-23`은 여전히 오염된다**: 리팩터가 그날 **중에** 배포돼서, 당일 이른
> 실행분(`similarity_judge` 16건)이 포함된다. 날짜 경계가 하루 단위라 같은 날 배포된
> 변경은 분리할 수 없다 — 깨끗한 비교가 필요하면 배포 **다음 날**을 기준으로 줄 것.

### 남은 과제

1. **표본 확대** — 위는 세션 1건이다. `scripts/test_exam.py`를 여러 번 돌려 n을 늘린 뒤
   비율을 해석할 것. 이제 배선이 고쳐졌으므로 그냥 실행만 하면 트레이스가 쌓인다.
2. ~~**`record_score` item_id 혼동 조사**~~ — **표본 확대로 기각.** 깨끗한 6세션에서
   `record_score` 거부율은 0.500으로 `save_item`(0.625)·`discard_item`(0.636)보다 낮다.
   11절의 74%는 오염된 표본의 산물이었다. 세 도구가 고르게 거부당하는 것이 실제 패턴이므로
   "특정 도구의 item_id 혼동"이 아니라 **저장/폐기/채점 사이클 전반의 시행착오**로 봐야 한다.
3. **Judge 게이트 단독 병목 확인** — 재시도 원인이 `judgment`/`both`뿐이고 `format`이 0건이다.
   개수·형식은 통과하는데 Judge 점수에서만 막힌다는 뜻이라, 오답매력도 미달(2.846/4.0)
   개선이 곧 통과율 개선으로 이어질 가능성이 있다. 두 지표를 함께 추적할 것.
4. **`budget=5`(프로덕션 조건) 측정** — 위는 `test_exam.py`의 `num_items=1`·`budget=2`
   스모크 조건이라 통과율 0.000을 프로덕션 실패율로 읽으면 안 된다.

---

## 12. 하이브리드 검색 도입 (BM25 + dense, 2026-08-03)

### 배경

`app/common/rag/retriever.py`는 도입 이래 **dense 단독**이었다 — BGE-M3 임베딩으로
top-20 후보를 뽑아 리랭커에 넘기는 구조. dense-only로 하겠다는 결정 자체가 문서화된
적이 없었고(MODEL_SELECTION.md 5절 "그냥 그렇게 구현됨"), 생기부 규정 위반 탐지에서
**"RAG 검색이 가정환경·종교 규정 청크를 상위로 못 올린다"**는 약점이 열린 이슈로
남아 있었다. 그동안은 검색을 고치는 대신 `chain.py`의 키워드 규칙으로 **우회**해왔다
(5절 "알려진 리스크").

### 구현

`app/common/rag/lexical.py` 신규 — BM25 역색인. **새 모델도, 재인덱싱도, 스키마 변경도,
새 의존성도 없다**:

- **토큰화**: 이미 로드된 BGE-M3의 토크나이저(XLM-R sentencepiece)를 빌려 씀. 한국어
  조사가 별도 토큰으로 분리돼(`'가정환경이' → ['▁가정','환경','이']`) 형태소 분석기
  (kiwipiepy/mecab) 없이도 어간이 매칭된다. `'가정형편이'`도 `▁가정`을 공유
- **인덱스**: ChromaDB에 이미 있는 청크 텍스트를 읽어 메모리에 구축(1029개 기준 1~2초,
  컬렉션별 lazy 캐시). 디스크에 남는 것이 없어 프로덕션 반영도 코드 배포만으로 끝남
- **융합**: dense 순위와 BM25 순위를 **RRF**(k=60)로 합침. 두 점수의 스케일이 전혀
  달라 직접 더할 수 없으므로 **등수의 역수만** 더한다
- **BM25는 컬렉션 전체를 독립적으로 훑는다** — dense 후보 20개를 재정렬하는 게 아니다.
  해결하려는 문제가 "정답 청크가 dense 후보에 아예 안 들어오는 것"이라, dense 결과
  안에서만 순위를 바꿔서는 의미가 없기 때문

`BGE-M3`의 `sparse_vecs`(모델이 학습한 어휘 가중치)를 쓰는 대안도 검토했으나 재인덱싱 +
메타데이터 스키마 변경 + 모델 버전 결합이 필요해 미채택(MODEL_SELECTION.md 5절 "대안").

### A/B 측정 결과 (n=22, LLM 미사용 결정론적)

| 구간 | dense-only | hybrid | 변화 |
|---|---|---|---|
| 전체 Recall@5 | 0.955 | 0.955 | — |
| 전체 MRR | 0.789 | **0.814** | +0.025 |
| standards MRR (n=12) | 0.892 | **0.938** | +0.046 |
| standards Recall@5 (n=12) | 1.000 | 1.000 | — |
| **regulations MRR (n=10)** | 0.667 | 0.667 | **변화 없음** |
| regulations Recall@5 (n=10) | 0.900 | 0.900 | — |

> dense-only 수치가 2026-07-24 기록치(0.955/0.789, 0.900/0.667)와 정확히 재현돼
> 측정 자체의 신뢰성은 확인됨.

### ⚠️ 주 목표는 달성하지 못함 — 병목이 리랭커로 이동

착수 시 세운 주 목표는 **regulations MRR 0.667 개선**이었는데 **한 자리도 움직이지
않았다.** 개선은 전부 standards에서 나왔다.

리랭커 이전 단계(후보 20개)에서 정답 청크의 순위를 직접 재보니 원인이 드러났다:

```
ret_013: dense=1     hybrid=3
ret_015: dense=None  hybrid=12    ← BM25가 처음 찾아냄
ret_019: dense=2     hybrid=1
ret_022: dense=2     hybrid=1
후보 포함률: dense 9/10 → hybrid 10/10
```

- **`ret_015`는 10절 "남은 과제" 2번에서 "구·신 청킹 양쪽에서 동일하게 미스되는,
  진짜 리트리버 이슈일 가능성"으로 열려 있던 항목이다.** BM25가 후보 12위로 끌어올려
  **검색기 단계의 후보 포함률은 9/10 → 10/10으로 완결**됐다.
- 그런데 최종 Recall@5는 그대로다 — **`bge-reranker-base`가 이걸 top-5로 올리지
  못했기 때문**이다.
- 나머지 9건은 이미 dense 후보에 1~3위로 들어와 있었다. 후보 순서를 바꿔도 최종
  순위는 리랭커가 다시 정하므로 MRR이 안 바뀐다.

**결론: regulations의 병목은 검색기가 아니라 리랭커였다.** MODEL_SELECTION.md 4절에
"리랭커의 실제 기여도가 한 번도 정량 측정된 적 없음"이라고 적혀 있었는데, 이번 측정이
그 지점을 처음으로 가리켰다.

### 채택 결정

회귀 없이 개선만 확인돼 **`RAG_HYBRID` 기본값을 `true`로 채택**(사용자 결정).
되돌리려면 `RAG_HYBRID=false`.

### 남은 과제

1. **리랭커 조사** — `bge-reranker-base` → `bge-reranker-v2-m3` 교체나 `n_candidates`
   조정으로 `ret_015`가 top-5에 들어오는지. 리랭커 유무 ablation(§4 미해결 항목)도 함께.
2. **가정환경·종교 유형 골든 항목 부재** — 열린 이슈 2번의 실제 실패 사례(골든셋에 없는
   새 표현으로 가정환경/종교를 언급하는 문장)가 `retrieval_golden_final.json`에 없어,
   하이브리드가 그 유형을 실제로 잡는지는 **측정되지 않았다.** 해당 유형 골든 항목을
   추가하려면 사람 라벨링이 필요(이 프로젝트 관례상 라벨링은 대행하지 않음).
3. **n=10 표본 한계** — regulations 골든이 10건뿐이라 항목 1건이 10%p를 움직인다.
   위 "변화 없음"도 개선이 없다는 증명이라기보다 **이 표본으로는 검출 못 했다**는 뜻.

> **13절에서 1·3번 해결**: 리랭커를 조사하다 `n_candidates`가 진짜 원인이었음이
> 드러나 regulations MRR 0.667 → **0.753**, 전체 Recall@5 **1.000** 달성.

---

## 13. 리랭커 조사 — `n_candidates`가 진짜 병목이었다 (2026-08-03)

### 출발점

12절에서 "병목이 검색기 → 리랭커로 이동했다"고 결론 내린 뒤, 리랭커 모델 교체
(`bge-reranker-v2-m3`)를 검토하기 전에 **비용 0인 것부터** 측정했다:
리랭커 ablation과 `n_candidates` 스윕. 스크립트는 `experiments/compare_reranker.py`.

### A. 리랭커 ablation — MODEL_SELECTION.md 4절의 열린 질문에 대한 답

"리랭커의 실제 기여도가 한 번도 정량 측정된 적 없음"으로 열려 있던 항목을 이번에 측정했다
(n=22, `n_candidates=20` 기준):

| 구성 | 전체 Recall@5 | 전체 MRR |
|---|---|---|
| dense, 리랭커 없음 | 0.955 | 0.744 |
| dense + 리랭커 | 0.955 | 0.789 |
| hybrid, 리랭커 없음 | 0.955 | 0.780 |
| hybrid + 리랭커 | 0.955 | 0.814 |

**리랭커의 기여는 MRR +0.034~0.045, Recall@5에는 영향 없음.** 즉 "정답을 찾느냐"가 아니라
"찾은 걸 몇 위에 놓느냐"에만 기여한다. 빼면 확실히 나빠지므로 유지가 맞다.

### B. n_candidates 스윕 — 예상과 반대 결과

| n_candidates | 전체 Recall@5 | 전체 MRR | regulations MRR | `ret_015` 최종 순위 |
|---|---|---|---|---|
| **10** | **1.000** | **0.854** | **0.753** | **5위** |
| 20 (기존 기본값) | 0.955 | 0.814 | 0.667 | top-5 밖 |
| 30 | 0.955 | 0.789 | 0.667 | top-5 밖 |
| 50 | 0.909 | 0.780 | 0.667 | top-5 밖 |

**후보를 늘릴수록 나빠진다.** 리랭커에 후보를 많이 줄수록 좋을 것이라는 통념과 반대다 —
`bge-reranker-base`가 후보가 많아질수록 오답을 상위로 잘못 올리는 것으로 보인다.
후보가 적으면 (품질이 괜찮은) 검색 순서가 더 많이 보존된다.

**골든 22건 전수 대조(10 vs 20)**: 개선 3건 · **악화 0건** · 동일 19건.
한 항목도 나빠지지 않아, 단순 표본 요동이 아니라 방향성 있는 차이로 판단했다.

```
ret_014  3위 → 2위
ret_015  없음 → 5위    ← 10·12절에서 계속 열려 있던 항목
ret_017  2위 → 1위
```

실제 도구 설정(`top_k=3`)에서도 같은 방향: 전체 MRR 0.803→**0.833**,
regulations MRR 0.667→**0.733**, Recall@3는 양쪽 0.909로 동일.

**속도도 2배 빠르다** — 리랭커가 채점할 (query, passage) 쌍이 절반이라
골든 22건 처리 기준 25초 → 13초.

### 적용

- `RAGRetriever.retrieve()`의 `n_candidates` 기본값 **20 → 10**.
  생기부(`chain.py`)는 원래부터 10을 명시해 쓰고 있었으므로 **실질 변경은 출제 모듈**
  (`tools.py`의 `search_standards`/`search_regulations`가 기본값을 쓰고 있었음).
- `evals/eval_lib.py`의 `eval_retrieval()`이 하드코딩하던 `n_candidates=20`을 제거해
  **기본값을 따르도록** 수정 — 그대로 뒀다면 eval이 프로덕션과 다른 설정을 측정하는
  검증-배포 불일치가 생긴다(2026-07-23 judge에서 이미 겪은 문제).

### 최종 수치 (프로덕션 기본값 그대로)

| 지표 | 2026-07-24 | 현재 | 목표 |
|---|---|---|---|
| 전체 Recall@5 (n=22) | 0.955 | **1.000** | ≥ 0.8 ✅ |
| 전체 MRR (n=22) | 0.789 | **0.854** | 참고값 |
| regulations Recall@5 (n=10) | 0.900 | **1.000** | 참고값 |
| **regulations MRR (n=10)** | 0.667 | **0.753** | 참고값 |

12절 착수 시 세운 주 목표(regulations MRR 개선)를 **하이브리드 검색이 아니라
`n_candidates` 조정으로 달성**했다. 하이브리드가 `ret_015`를 후보에 올려놓지 않았다면
`n_candidates=10`으로도 못 찾았을 것이므로, 두 변경이 함께 작용한 결과다.

### 한계

- **n=22 과적합 위험**: 악화 0건이라 방향성은 신뢰할 만하지만, Recall@5 1.000은
  "이 골든셋에서 전부 찾았다"는 뜻이지 검색이 완벽하다는 뜻이 아니다. 더 어려운
  골든셋이 필요하다는 10절 "남은 과제" 4번은 그대로 유효하다.
- **리랭커 모델 교체는 미측정** — `bge-reranker-v2-m3`는 ~2.3GB 다운로드가 필요하고
  CPU 추론이 더 느려져, `n_candidates` 조정만으로 목표를 넘긴 시점에서 보류했다.
  필요해지면 `BGE_RERANK_MODEL` 환경변수만 바꿔 같은 스크립트로 재측정할 수 있다.

---

## 14. 코퍼스 커버리지 갭 — "검색이 못 찾은 게 아니라 없었다" (2026-08-03)

### 발견

13절 이후 "더 어려운 골든셋"을 만들려고 regulations 코퍼스 **472청크를 전수 스캔**하다,
그동안의 진단이 틀렸다는 것이 드러났다.

열린 이슈 2번과 5절은 이렇게 기록해왔다 — *"`search_regulations` RAG 검색 자체
(가정환경·종교 규정 청크가 검색 상위에 안 잡히는 문제)를 고친 게 아니라 키워드 목록으로
우회한 것"*. 즉 **검색 실패**로 진단해왔다.

실제로 코퍼스에 각 표현이 몇 번 나오는지 직접 세어보니:

| 검색어 | regulations 472청크 중 |
|---|---|
| 종교 | **0건** |
| 정치적 성향 / 정치성향 | **0건** |
| 외모 / 용모 / 신체적 | **0건** |
| 추측 | **0건** |
| 가정환경 / 가정형편 | **0건** |
| 경제적 지위 | 1건 ✅ |

**검색이 못 찾은 게 아니라 조항 자체가 인덱싱된 코퍼스에 없다.** 인덱싱된 문서는
`기재요령_고등학교_2024`(452청크)와 `학교생활기록_작성_및_관리지침_제555호_본문`(20청크)
둘뿐이고, 여기엔 종교·외모·추측 관련 금지 조항이 들어있지 않다.

즉 **하이브리드 검색도 리랭커 교체도 이 문제를 고칠 수 없었다** — 없는 것을 검색할 수는 없다.
`_rule_violations`의 키워드 규칙은 "검색 약점을 우회한 것"이 아니라, **애초에 코퍼스에 없는
내용을 대신 담고 있던 것**이다. 우회가 아니라 유일한 근거였다.

### 단, '가정환경'은 존재한다 — 다른 표현으로

`기재요령_고등학교_2024_p24_40`에 실재한다:

> 차. **부모(친인척 포함)의 사회･경제적 지위**(직종명, 직업명, 직장명, 직위명 등) 암시 내용

교사가 "가정형편"이라고 쓰는 것을 규정은 "부모의 사회·경제적 지위"라고 쓴다. **공유 토큰이
전혀 없어** BM25로도 잡히지 않고, dense도 이 격차를 못 넘는다(아래 후보셋 `cand_001` 실패).
하이브리드 검색이 이 유형에 무력했던 이유가 여기서 설명된다.

### 후보 골든셋

`data/golden/regulations_retrieval_candidates.json` — 코퍼스에 **실재하는 조항**만 근거로
10건 작성(사람 검수 대기, `reviewed: false`). expected_chunk_id는 검색기 출력이 아니라
코퍼스를 직접 훑어 지정했다 — 검색기로 정답을 정하면 "현재 시스템이 찾는 것"만 골든이 되는
순환 오류가 생기기 때문.

**현재 검색기 Recall@5 = 0.600(6/10)** — 기존 골든셋(1.000)과 대비되어 변별력이 있다.
미스: `cand_001`(가정형편→부모 사회경제적 지위), `cand_003`, `cand_009`, `cand_010`.

> 작성 중 `chunk_preview`를 공백 정규화된 출력에서 복사해 첫 측정이 0/10으로 나온 실수가
> 있었다. 원본은 줄바꿈을 포함하는데 substring 매칭이 이를 못 넘긴 것 — 10절 "남은 과제"
> 3번이 지적한 *"substring 완전 포함 매칭이 취약하다"*가 그대로 재현된 사례다.

### 남은 과제

1. **사람 검수** — 10건의 query↔expected_chunk_id 짝이 타당한지 확인 후 `reviewed: true`.
   (미스했다는 것이 짝이 틀렸다는 뜻은 아니므로, 검수는 "이 질문의 근거 조항이 이 청크가
   맞는가"만 볼 것.)
2. **코퍼스 보강 여부 결정** — 종교·외모·추측 조항이 담긴 문서(시도교육청 기재 도움자료 등)를
   `data/regulations/`에 추가해 인덱싱할 것인가, 아니면 현행처럼 규칙 기반으로만 처리하고
   **"이 항목들은 RAG 근거 없이 규칙으로만 판정된다"고 명시**할 것인가. 후자를 택하면
   `VALIDATE_TPL`이 8종 위반 유형을 나열하면서 정작 그 근거를 검색에서 못 받는 현 구조를
   문서에 정직히 남겨야 한다.
3. **5절·열린 이슈 2번의 진단 문구 수정** — "검색이 상위로 못 올림"이 아니라
   "코퍼스에 조항이 없음"이 정확하다.

### 후속: 규칙 위반을 차단 → 경고로 강등 (2026-08-03, 사용자 결정)

위 발견에 이어, 키워드 규칙이 **서술 대상을 구분하지 못한다**는 것이 실측으로 확인됐다.
분필은 사회 교사용 도구인데 정치·종교·가정환경은 **사회과가 가르치는 주제어**이기도 하다:

| 현재 동작 | 문장 | 실제로는 |
|---|---|---|
| 🚫 차단 | 사회 수업에서 **정치적** 다원주의 개념을 조사해 발표함 | 교과 내용 — 통과해야 함 |
| 🚫 차단 | **가정환경**에 따른 교육 격차를 주제로 보고서를 작성함 | 탐구 주제 — 통과해야 함 |
| 🚫 차단 | **저소득** 국가의 빈곤 문제를 탐구해 발표함 | 국가 얘기 — 통과해야 함 |
| ✅ 통과 | 아버지가 대기업 임원이라 경제에 관심이 많음 | **기재요령 p24 위반 — 잡아야 함** |

**잡아야 할 건 놓치고, 놓아줘야 할 건 잡는다.** 주목할 점은 규정 근거가 있는
`_RULE_BACKGROUND`도 똑같이 오작동한다는 것 — 즉 문제는 "규정 근거 유무"가 아니라
**키워드 매칭이 "학생 본인의 속성"과 "학생이 탐구한 주제"를 구분할 수 없다**는 데 있다.
둘은 같은 단어를 쓴다.

**적용**: `_rule_violations()` → `_rule_warnings()`로 강등(차단하지 않음), PII 검사만
`_pii_violations()`로 분리해 차단 유지. LLM 규정 판정(`VALIDATE_TPL`)도 경고로 강등.

| 항목 | 처리 | 근거 |
|---|---|---|
| PII 생성·잔존 | **차단** | 하드룰 2·4 — 문맥에 따라 괜찮아지는 종류가 아님 |
| 메모에 없는 사실 추가 | **차단** | 하드룰 5 명시 |
| 검증 시스템 실패 | **차단** | fail-closed 유지 |
| 키워드 6종 + LLM 규정 판정 | **경고** | 오탐이 잦고, 최종 기재 책임은 어차피 교사 |

경고는 재시도를 유발하지 않는다 — 오탐 문장을 다시 윤문시키면 정상 문장을 망가뜨린다.

**eval 처리**: `eval_violation_detection()`은 violations + warnings를 **합쳐서** 센다.
이 지표가 재는 것은 "탐지했는가"이지 "차단했는가"가 아니므로, 차단 동작이 바뀌었다고
탐지 성능 히스토리가 끊기면 안 된다.

**남은 과제**: `VIOLATION_GOLDEN` 50건에는 위 오탐 유형(교과 주제로서의 정치·가정환경)이
한 건도 없다. 그래서 Recall 0.927이라는 수치가 이 문제를 가려왔다 — **오탐 사례를 골든셋에
추가해 Precision을 제대로 측정하는 것**이 다음 과제다(사람 검수 필요).

#### 보강 후보 17건 작성 (`data/golden/violation_golden_candidates.json`)

두 종류의 실패 사례를 모았다. 전 항목을 `_rule_warnings()`에 실제로 통과시켜
**현재 오작동이 재현되는 것만** 수록했다 — 이미 올바르게 동작하는 문장은 골든셋에 넣어도
변별력이 없으므로 제외했다(작성 중 2건이 여기 걸려 교체됨).

| 종류 | 건수 | 내용 |
|---|---|---|
| 오탐(`label=0`인데 걸림) | 14 | "정치적 다원주의 개념을 조사해 발표함", "가정환경에 따른 교육 격차 보고서", "학급 회의에서 정치적 중립을 지키며 사회를 진행함" 등 6개 규칙 전부 |
| 미탐(`label=1`인데 통과) | 3 | "아버지가 대기업 임원이라…", "어머니가 운영하는 병원에서 봉사활동…" — **기재요령 p24에 근거가 있는 유일한 유형** |

이 17건만으로 규칙을 단독 채점하면 **TP=0 / FP=14 / FN=3 / TN=0**(Precision·Recall 모두
0.000). 실패 사례만 모은 집합이라 이 수치가 시스템 전반 성능은 아니지만, 기존 50건에서
Recall 0.927이 나왔던 것과 대비하면 **그 수치가 무엇을 못 보고 있었는지**가 드러난다.

`label`은 제안이며 사람 검수 대기(`reviewed: false`)다. 특히 오탐 항목의 `label=0`은
"교과 주제로서의 언급은 기재 가능하다"는 전제에 기대는데, **이 전제 자체가 규정으로 확인된
것이 아니다**(위 코퍼스 갭 참고 — 종교·외모·추측 조항은 기재요령에 없음). 교사 검수 필수.

#### LLM(`VALIDATE_TPL`)도 같은 방식으로 틀린다 — 측정 결과

미탐 3건을 few-shot으로 넣어 고치려다, **"프롬프트에 지엽적인 예시를 넣는 것 아니냐"**는
지적을 받고 먼저 측정했다. `VALIDATE_TPL` 시스템 프롬프트에는 이미 "가정환경(경제력·가족형태
등)"이 위반 유형으로 들어가 있으므로, 카테고리를 몰라서 틀리는 것인지부터 확인이 필요했다.

실제 런타임 경로(regulations RAG 검색 → `VALIDATE_TPL`, 생성 모델 qwen2.5:14b)로 측정:

| 기대 | LLM 판정 | 문장 |
|---|---|---|
| 위반 | ✅ VIOLATION | 아버지가 대기업 임원이라 경제 분야에 관심이 많음 |
| 위반 | ✅ VIOLATION | 어머니가 운영하는 병원에서 봉사활동을 하며 진로를 구체화함 |
| 위반 | ❌ **OK** | 부모님이 모두 교직에 계셔서 교육 문제에 관심이 깊음 |
| 정상 | ❌ **VIOLATION** | 부모의 경제적 지위 남용을 방지하는 프로젝트를 진행하는 등… |
| 정상 | ❌ **VIOLATION** | 가정환경에 따른 교육 격차를 주제로 보고서를 작성함 |

**5건 중 2건만 맞다.** 이때는 "LLM도 키워드와 같은 방식으로 틀린다"고 결론지었는데,
**아래 n=17 전수 측정에서 이 결론이 뒤집혔다.** n=5 단일 실행은 표본이 너무 작았다.

#### 정정 — n=17 전수 3회 측정 (2026-08-03)

후보 17건 전부에 대해 키워드 규칙과 LLM 판정을 나란히 측정했다(3회 반복):

| 판정 주체 | TP | FP | FN | TN | Precision | Recall | 정확도 |
|---|---|---|---|---|---|---|---|
| **키워드 규칙** | 0 | 14 | 3 | 0 | 0.000 | 0.000 | **0.000** |
| **LLM 1회차** | 3 | 2 | 0 | 12 | 0.600 | 1.000 | **0.882** |
| **LLM 2·3회차** | 2 | 2 | 1 | 12 | 0.500 | 0.667 | **0.824** |

**앞선 n=5 결론은 틀렸다.** LLM은 키워드와 "같은 방식으로" 틀리지 않는다 —
17건 중 14~15건을 맞히는 반면(정확도 0.82~0.88), **키워드는 17건 전부를 틀린다**(0.000).
이 17건이 실패 사례만 모은 적대적 집합이라 키워드 0.000은 설계상 당연하지만, 같은 집합에서
LLM은 대부분을 맞힌다는 것이 핵심이다.

n=5에서 LLM이 나빠 보였던 건 그 5건에 오탐 사례가 2건뿐이었고(LLM이 둘 다 틀림) 단일
실행이었기 때문이다. 실행 간 변동도 확인했다 — `vc_011`·`vc_012`가 회차마다 판정이 뒤집혔다.

**함의**: 이 적대적 집합에서 **키워드 규칙은 기여가 없고 오탐만 14건 만든다.** 최종 판정이
둘의 OR라면 키워드는 Precision만 깎는다. 다만 기존 `VIOLATION_GOLDEN` 50건에서는 키워드가
Recall 0.927에 기여했으므로 "쓸모없다"가 아니라 **"쉬운 사례에선 되고 어려운 사례에선 해롭다"**가
정확하다. 키워드 규칙 제거 여부는 두 집합을 합친 뒤 재측정해서 판단할 사안이다.

**그래서 지금 하지 않는 것**:
1. 근거 조항이 확인된 유형은 `부모의 사회·경제적 지위` 하나뿐이고 나머지 5개 규칙은
   규정 근거조차 없다(위 코퍼스 갭). 근거 없는 규칙을 튜닝하는 건 순서가 틀렸다.
2. 오탐 14건의 `label=0`이 **아직 교사 확인 전**이라, 이걸 정답으로 삼아 튜닝하면 또
   합성 라벨에 과적합한다.

다만 **차단 → 경고 강등을 먼저 해둔 덕분에 피해는 제한적**이다. 판정이 틀려도 교사가 문장을
받아보고 무시하면 된다.

**착수 조건**: 지인 교사 확인으로 (a) 종교·외모 등이 실제 현장 규범인지, (b) 교과 주제로서의
언급이 기재 가능한지가 정해지면 → 오탐 14건을 골든셋에 편입 → 키워드 규칙 제거 여부를
50+17건 합산으로 재측정해 결정.

### 결말 — 생기부 모듈 제거 (2026-08-03, 사용자 결정)

위 "착수 조건"을 기다리는 대신 **모듈 자체를 걷어내기로 했다.**

정리하면 이 모듈의 상태는 이랬다:

1. **검증 규칙 6종 중 규정 근거가 확인된 것은 1개**(`_RULE_BACKGROUND` ← 기재요령 p24).
   나머지 5종은 인덱싱된 교육부 문서에 조항이 아예 없다.
2. **그 1개조차 오작동한다** — "가정환경에 따른 교육 격차 보고서"(정상)를 잡고
   "아버지가 대기업 임원이라…"(위반)를 놓친다.
3. **판정 정확도**: 적대적 17건에서 키워드 0.000 / LLM 0.82~0.88.
4. **하드룰 1 때문에 실제 데이터로 검증할 수 없다** — 합성 메모로만 테스트했고 실 현장
   적용은 한 적이 없다.
5. **정답을 정해줄 도메인 근거가 없다** — 교사 확인 없이는 골든셋 라벨조차 확정 불가.

검증할 수 없는 기능을 포트폴리오에 남기는 것보다 걷어내는 쪽이 정직하다고 판단해
출제 단일 모듈로 범위를 좁혔다.

**제거 대상**: `app/modules/record/`, `evals/eval_record.py`, `scripts/test_record.py`,
`tests/test_record_chain_*.py`, VIOLATION/HALLUCINATION 골든셋(+후보 17건),
`/record` 엔드포인트, 프론트 `RecordTab`, 생기부 체인 다이어그램.

**유지 대상**:
- `app/common/privacy.py`(`mask_pii`) — 출제 경로가 쓴다(`_build_spec`이 `passage_text`를
  모델 호출 전 마스킹). MASKING_GOLDEN 20건은 `tests/test_masker.py`가 이어받아 채점한다.
- `regulations` 컬렉션·코퍼스·인덱싱 스크립트 — 런타임에서는 더 이상 조회하지 않지만
  `retrieval_golden_final.json` 22건 중 10건이 이 컬렉션이라, 지우면 Recall@5 히스토리가
  n=22→12로 끊긴다. **검색 eval 전용**으로 남긴다.

> 위 4·5절과 결과 이력의 생기부 수치(위반 Recall 0.927 등)는 **그대로 보존**한다 — 당시
> 의사결정 기록이고, 그 수치가 왜 신뢰할 수 없었는지가 이 절의 요지다.

---

## 15. validate 게이트 임계값 재보정 — "통과할 수 없는 게이트"였다 (2026-08-04)

> 증상→원인 추적 서사는 [TROUBLESHOOTING.md 13번](./TROUBLESHOOTING.md#13-통과할-수-없는-게이트-2026-08-04) 참고.
> 이 절은 측정 데이터와 임계값 근거를 담는다.

### 발단

사용자가 "`submit_for_review`를 호출해 정상적으로 마치는 케이스가 거의 없어 보인다"고
관측. 조사해보니 **`submit_for_review` 자체는 정상**이었다(11.1절 6세션에서 10회 호출,
거부 0건). 문제는 그 다음 단계인 `validate_node` 게이트였다.

11.1절 데이터를 재해석하면 결론이 분명해진다 — validate 18회 중 재시도 사유가
`judgment 11 · both 7 · format 0`이므로, **Judge 지표 게이트가 18/18(100%) 실패**했고
그중 11회는 개수·품질 조건은 통과하고 **Judge 지표만 단독으로** 막았다.

### 원인 — 임계값이 Judge의 실제 점수 분포 위에 있었다

`overall_score >= 4` / `type_ratio_score >= 0.7`은 2026-07-06 도입(`a673d9a`) 이후
**한 번도 재검토된 적이 없고 근거 기록도 없었다**. 그 사이 Judge 프롬프트는 4회 이상
재보정됐다(5절) — 채점자는 계속 바꾸면서 합격선은 한 번도 안 본 셈이다.

"Judge가 실제로 어떤 점수를 주는가"를 측정한 적이 없어서 검증이 불가능했다. 그래서
프로덕션 Judge(gpt-5.6-luna)로 STRUCTURE_GOLDEN 45건을 재채점해 분포를 측정했다
(`experiments/measure_validate_gate.py`, raw `data/golden/_validate_gate_calibration.json`).

**overall_score 분포** (2회 측정, Judge temperature 0.7이라 회차 변동 있음)

| 점수 | 1 | 2 | 3 | 4 | 5 | 평균 |
|---|---|---|---|---|---|---|
| run1 | 10 | 12 | 18 | 5 | 0 | 2.40 |
| run2 | 12 | 13 | 17 | 2 | 1 | 2.27 |
| human | 14 | 19 | 9 | 2 | 1 | 2.04 |

Judge는 4~5점을 거의 주지 않는다. `type_ratio_score`도 0.5에 큰 덩어리가 몰리는데
(45건 중 15~18건) 임계값 0.7이 그 바로 위를 잘라 이 구간을 통째로 탈락시켰다.

**임계값별 실측 통과율** (judge 3개 지표만, count_match·all_approved 제외)

| 기준 | run1 | run2 |
|---|---|---|
| `overall>=4, type>=0.7` (옛 기준) | 0.089 | **0.067** |
| `overall>=3, type>=0.5` (새 기준) | 0.489 | **0.422** |

### 결정적 근거 — 프롬프트와 게이트가 모순이었다

Judge 프롬프트가 "이게 정답"이라고 가르치는 few-shot 6개를 옛 게이트로 판정하면
**통과가 1개뿐**이다. 특히 few-shot 6번은 *"같은 주제라도 다른 지점을 묻는 문항은
반복이 아니니 감점하지 말라"*고 가르치려고 2026-07-12에 추가한 균형 예시인데
(`overall_score` 4), `type_ratio_score`가 0.5라 게이트에서 탈락한다. 통과하는 유일한
1개는 `overall=5`를 요구하는데 Judge는 5점을 거의 주지 않는다.

즉 **Judge에게 "이건 좋은 세트"라고 가르쳐 놓고, 코드는 그 좋은 세트를 반려**하고
있었다.

### 적용

`app/modules/exam/graph.py`의 임계값을 상수로 분리하고 측정 근거를 주석에 남겼다.

| 항목 | 이전 | 이후 |
|---|---|---|
| `_MIN_OVERALL_SCORE` | 4 | **3** |
| `_MIN_TYPE_RATIO_SCORE` | 0.7 | **0.5** |
| `difficulty_match` | True | 변경 없음(실측 통과율 0.911로 병목 아님) |

`type_ratio` 0.5를 허용하는 것은 **`num_items`가 예시 문제의 문항 수와 무관하게
지정된다는 설계(2026-07-09 결정)의 당연한 귀결**이기도 하다 — 예시가 객관식 1문항인데
2문항을 요청하면 유형 구성이 정확히 같을 수 없다.

경계값 회귀 테스트 2건 추가(`tests/test_exam_validation.py`): `overall=3·type=0.5`는
통과, `overall=2·type=0.2`는 여전히 반려 — 임계값을 낮춘 것이지 게이트를 없앤 게 아니다.

### 실사용 영향

게이트 실패 시에도 문항은 그대로 반환되고 UI는 `validation_passed`를 표시하지 않으므로,
교사가 결과를 못 받는 문제는 **아니었다**. 실질 비용은 **통과가 불가능하니 매 요청이
재시도 예산(budget=5)을 전량 소진**한 것 — 생성이 불필요하게 느리고 비쌌다.

### 남은 과제

1. ~~**`record_score`는 여전히 self-judge다.**~~ → **2026-08-05 조치 완료, 16절 참고.**
   문항 품질 점수를 생성 에이전트가 자기 자신에게 매기고 그 점수가 `all_approved`
   조건으로 validate를 막고 있었다. 사람 라벨과 대조된 적이 한 번도 없어,
   게이트에서 제외하고 참고값으로 강등했다.
2. **런타임과 오프라인 eval이 Judge에게 다른 payload를 보낸다.** 오프라인은 5개 필드,
   런타임은 `get_draft_items()`의 9개 필드(에이전트가 스스로 매긴 `judge_score`·`status`
   포함). 독립 채점해야 할 Judge가 자기채점 결과를 보고 있고, 이 문서의 Judge 신뢰도
   수치는 프로덕션이 실제로 보내지 않는 형태로 측정된 값이다.
3. **재시도가 아무것도 바꾸지 않을 수 있다** — 에이전트가 동일한 문항 세트로
   `submit_for_review`만 다시 호출하면 그대로 종료되고 Judge는 같은 것을 재채점한다.
4. 새 임계값의 실제 효과(재시도 횟수·완료율 변화)는 `evals/eval_trajectory.py`로
   재측정해 확인할 것 — 이번 변경은 골든셋 재채점 기반이라 런타임 궤적은 아직 미측정.

---

## 16. 자체 평가 점수를 게이트에서 제외 (2026-08-05)

### 문제

15절 조사 중 드러난 것 — `validate_node`의 5개 통과 조건 중 `all_approved`가
**검증된 적 없는 자기채점에 의존**하고 있었다.

`record_score`는 문항을 생성한 **에이전트 자신이 자기 문항에 매기는 점수**다
(도구 설명에 "에이전트가 직접 평가한 점수"라고 명시). 이 점수가 3 미만이면
`rejected`가 되고 `all_approved`가 깨져 재시도가 걸린다. 그런데 이 자기채점이
정확한지 사람 라벨과 대조된 적이 한 번도 없다.

2026-07-23에 구조 유사도에서 self-judge를 걷어낸 것과 **정확히 같은 결함**이
문항 품질 축에 그대로 남아 있었다. 게다가 EVAL.md에 기록된 "문항 품질 Judge
신뢰도 κ=0.468"은 `evals/eval_lib.py`의 `judge_one`(별도 Judge)이 ITEM_GOLDEN을
채점한 값인데, **`judge_one`은 런타임에서 실행되지 않는다** — 검증한 것과 배포된
것이 또 달랐다.

부수적으로, 점수가 없는 문항(`record_score` 호출 실패, 실측 거부율 0.500)이
기본값 0.0을 받아 **저품질과 구분 없이 `rejected`로 취급**되는 버그도 있었다.

### 적용 (B안)

| 항목 | 이전 | 이후 |
|---|---|---|
| `all_approved` | validate 통과 조건 (AND) | **게이트에서 제외** — 참고값으로만 유지 |
| 미채점 문항 | `judge_score=0.0` → `rejected` | **`status="unscored"`로 분리** |
| UI 표시 | "품질 4.0/5", "✓ 승인 / ✗ 반려" | "AI 자체평가 4.0/5", "✓ 자체평가 통과 / ✗ 미달 / − 없음" |

**게이트에서 뺀 이유**: 문제는 "자기채점이 존재한다"가 아니라 "검증 안 된 자기채점이
프로덕션 통과 여부를 결정한다"였다. 세트 품질은 이미 `judge_node`(별도 Judge)의
`overall_score`가 보고 있고, 그 루브릭에 환각·중복·주제 이탈·언어 오염이 감점
요소로 들어 있어 실질적으로 이중 방어였다.

**별도 Judge 노드 추가(A안)를 택하지 않은 이유**: 문항 개수만큼 LLM 호출이 늘어
비용·지연이 크게 증가하는데, 위와 같이 세트 단위 Judge가 이미 품질을 보고 있다.
RunPod 크레딧이 소진된 상황에서 매 요청 호출을 늘리는 건 우선순위가 맞지 않는다.

**UI 라벨을 바꾼 이유**: 이 점수는 교사 화면에 "품질 4.0/5"로 그대로 노출되고
있었다 — 교사가 객관적 검증 결과로 오해할 수 있어 "AI 자체평가"임을 명시했다.

회귀 테스트 3건 추가(`tests/test_exam_validation.py`): 미채점 문항이 통과를 막지
않을 것, 자체 평가가 낮아도(1점) 막지 않을 것, `unscored`와 `rejected`가 구분될 것.

### 남은 과제

- **자체 평가의 신뢰도는 여전히 미측정이다.** 게이트에서 뺐을 뿐 정확도를 잰 것은
  아니다. 재려면 에이전트 자기채점과 이미 검증된 `judge_one`(κ=0.468)을 같은
  생성물에 대해 비교하면 된다 — 사람 라벨을 새로 만들 필요 없이 self-bias를
  정량화할 수 있다(Judge 모델 교체 때 쓴 `compare_judge_models.py`와 같은 방법론).
- **런타임과 오프라인 eval의 Judge payload 불일치는 아직 그대로다**(15절 남은 과제 2번).
- 15·16절 변경의 실제 효과(재시도 횟수·완료율)는 `evals/eval_trajectory.py`로 재측정 필요.

---

## 17. 자기채점 도구 제거 + Judge 입력 통일 (2026-08-06)

16절에서 자기채점 점수(`record_score`)를 게이트에서 뺐는데, 그러고 나니 이 도구가
**턴만 먹고 아무것도 결정하지 않는 상태**가 됐다. 이번엔 도구 자체를 걷어냈다.

### 왜 표시값으로 남기지 않았나 — 턴 예산이 실제 병목이었다

시스템 프롬프트가 `record_score`를 **"save_item 응답을 받은 다음 턴에"** 부르도록
강제하고 있었다. 즉 문항 하나당 필요한 턴이 이랬다:

| 턴 | 도구 |
|---|---|
| 1 | `search_standards` |
| 2 | `validate_item_format` |
| 3 | `save_item` |
| 4 | `record_score` ← 게이트하는 것 없음 |

`agent_node`의 턴 한도는 **14**다. 문항당 4턴이면 3문항에 13턴으로 빠듯하고,
5문항이면 21턴이라 애초에 불가능하다. 실제로 STRUCTURE_GOLDEN 45건에서 목표 개수별
달성률이 정확히 이 산술을 따랐다 — 3개 0.421 / 5개 0.100 / 7개 0.000, **45회 중 최대
생성량 6개**. 게다가 `record_score`는 실측 거부율이 0.500(주로 존재하지 않는 `item_id`로
호출, 11.1절)이라 실패할 때마다 턴을 더 낭비했다.

정리하면 **게이트하지도 않는 도구가 문항당 턴의 25%를 쓰고 있었다.**

### 함께 고친 것 — Judge 입력 payload 불일치 (15절 남은 과제 2번)

런타임과 오프라인 eval이 Judge에게 서로 다른 필드를 넘기고 있었다:

| | Judge에게 넘기던 것 |
|---|---|
| 오프라인 eval | 골든셋 `generated_items` — 5개 필드 |
| 런타임 | `get_draft_items()` — `item_id`·`standard`·`judge_score`·`status`까지 포함 |

두 가지가 문제였다. ① EVAL.md에 쌓아온 구조 Judge 신뢰도 수치가 **프로덕션이 실제로
보내지 않는 입력**으로 측정된 값이었다(2026-07-23에 해소했다던 검증-배포 불일치가
입력 형태로 재발). ② 독립 채점해야 할 Judge가 **에이전트 자기채점 점수(`judge_score`)를
payload 안에서 보고 있었다** — 앵커링 위험.

정규화를 `judge_node`가 아니라 **공유 함수 `judge_structure()` 안**에 넣었다
(`_JUDGE_ITEM_FIELDS`). 호출부가 늘어도 구조적으로 다시 어긋날 수 없게 하기 위함이다.

### 적용 범위

| 대상 | 변경 |
|---|---|
| `tools.py` | `record_score` 도구 삭제(TOOLS 6→5개), `ctx["scores"]` 제거, `get_draft_items()`가 `judge_score`·`status`를 더 이상 붙이지 않음 |
| `graph.py` | 시스템 프롬프트 4단계 삭제(문항당 4턴→3턴), 진행 요약에서 `score=` 제거 |
| `judge.py` | `_JUDGE_ITEM_FIELDS`로 입력 정규화 |
| `state.py` | `DraftItem`에서 두 필드 제거 |
| 프론트엔드 | "AI 자체평가" 배지·점수 바 제거 — 검증된 적 없는 수치를 교사에게 품질로 보여주지 않는다 |

회귀 테스트 2건 추가: 저장된 문항에 자기채점 흔적이 없을 것 + 런타임·오프라인 payload가
동일하게 정규화될 것.

### 남은 과제

- 이 변경의 효과는 아래 18절에서 런타임으로 측정한다.
- `experiments/`의 3개 스크립트 프롬프트에 `record_score` 지시가 stale하게 남아 있다
  (무시될 뿐 결과를 깨뜨리지 않음, 재실행 시 갱신 — EVAL_MAP.md §5).

---

## 18. 재보정 효과 런타임 측정 — 측정 도구가 측정할 수 없는 것을 재고 있었다 (2026-08-07)

15~17절 변경(게이트 임계값 재보정 + 자기채점 도구 제거)의 실제 효과를 확인하려고
`scripts/test_exam.py`를 6회 실행하고 `evals/eval_trajectory.py`로 집계했다.
11.1절 baseline과 **동일 조건**(num_items=1, budget=2, qwen2.5:14b + gpt-5.6-luna).

### 결과

| 지표 | baseline (08-04) | 이번 (08-07) |
|---|---|---|
| 도구 호출 n | 91 | 100 |
| 세션 / agent 재진입 | 6 / 18 | 6 / 18 (동일) |
| **malformed tool-call** | **16건 (0.184)** | **0건 (0.000)** |
| `record_score` | 7 ok / 7 rejected | — (도구 제거됨) |
| `save_item` | 9 ok / 15 rejected | 6 ok / 22 rejected / 2 error |
| `validate_item_format` | 15 ok / 1 error | 16 ok / 18 rejected / 1 error |
| 재시도 원인 | judgment 11 · both 7 · format 0 | judgment 13 · both 5 · format 0 |
| **validate 통과율** | **0.000** | **0.000** |

### 통과율 0.000은 게이트 문제가 아니었다 — 측정 도구의 문제였다

통과율이 그대로라 재보정이 무효했나 싶었지만, **이 스모크 테스트로는 애초에 통과가
불가능**하다는 것이 드러났다.

`test_exam.py`의 예시 문제는 **객관식 1 + 서술형 1**인데 `num_items=1`을 요청한다.
1문항 세트는 2가지 유형 구성을 재현할 수 없으므로 `type_ratio_score`가 구조적으로
0.5에서 막힌다. 그리고 `STRUCTURE_JUDGE_TPL`의 few-shot 3번이 **정확히 이 상황**을
가르치고 있다:

```
예시: "선거 원칙?(객관식2+서술형1)" / 생성: [객관식 1개]
→ {"type_ratio_score": 0.5, "difficulty_match": false, "overall_score": 2}
```

실제 관측된 4개 세션이 전부 `type_ratio 0.5 / overall 2`로 이 앵커와 일치했다.

**통제 실험으로 확정**(동일한 생성 문항 1개를 예시만 바꿔 재채점):

| 예시 구성 | type_ratio | overall | 게이트 |
|---|---|---|---|
| 객관식1 + 서술형1 (스모크 조건) | 0.5 | **2** | 탈락 |
| 객관식1만 (유형 구성 일치) | 1.0 | **4** | 통과 |

같은 문항인데 예시 구성만 바꾸니 2점에서 4점으로 뛴다. 즉 **스모크 조건은 Judge
루브릭상 "2점짜리 상황"으로 정의돼 있고**, 어떤 임계값을 쓰든(재보정 전 4, 후 3)
통과할 수 없다. baseline의 0.000도 같은 이유였으므로, **이 지표로는 재보정 전후를
비교할 수 없다.**

15절의 재보정 근거(STRUCTURE_GOLDEN 45건, 통과율 6.7%→42.2%)는 여전히 유효하다 —
그쪽은 num_items 3/5/7의 다양한 조합이라 이 구조적 상한에 걸리지 않는다.

> **9절·10절과 같은 계열의 실수다.** 9절은 "생성 프롬프트를 바꿨는데 그 지표는 생성
> 경로를 안 탔다", 10절은 "채점 방식이 청크 경계에 과민했다", 이번은 "테스트 조건이
> 루브릭상 통과 불가였다" — 셋 다 **측정 도구가 내가 재려는 것을 잴 수 없었던** 경우다.

### 유효한 발견 — malformed tool-call이 사라졌다

측정 가능한 축에서는 뚜렷한 변화가 있었다: **malformed tool-call 18.4% → 0%**.

`record_score`는 "save_item이 반환한 item_id를 다음 턴에 다시 넣어 호출"해야 하는
상태 의존적 단계였고, 실측 거부율 0.500의 상당수가 존재하지 않는 item_id 호출이었다.
이 단계를 없애자 그 실패 경로 자체가 사라진 것으로 보인다.

**단, 인과는 확정하지 못한다.** 이번엔 대신 `validate_item_format` 거부가 0→18로
늘었다(형식 자기교정 경로가 작동한 것이라 설계상 정상이지만, 에이전트가 형식을 더
자주 틀렸다는 뜻이기도 하다). 실패 형태가 "도구 호출 문법 깨짐"에서 "문법은 맞지만
내용이 형식 미달"로 **옮겨간 것**일 수 있다. n=6 세션으로는 구분할 수 없다.

### 남은 과제

1. ~~**스모크 테스트 조건 재검토**~~ → **2026-08-07 조치 완료, 19절 참고.**
2. ~~**게이트 통과율의 런타임 측정**~~ → 1번 해소 후 측정, 19절 참고.
3. **0문항 세션 2/6** — `save_item`이 한 번도 성공하지 못한 세션. budget=1~2의
   알려진 실패 모드(과거 7B 28%, 14B+게이트 30%)와 같은 계열로 보이나 재확인 필요.
4. `save_item` 성공 9→6 / 거부 15→22 변화의 원인 — 위 "실패 형태 이동" 가설과
   함께 표본을 늘려 확인할 것.

---

## 19. 스모크 테스트 조건 수정 → 게이트 통과율 첫 런타임 측정 (2026-08-07)

18절에서 `scripts/test_exam.py`가 **통과 불가능한 조건**을 쓰고 있어 회귀 감지 도구로
기능하지 못한다는 것이 드러났다. 조건을 고치고 재측정했다.

### 수정 — 유형만 균일하게, 개수 디커플링은 유지

예시 문항 2개를 **둘 다 객관식**으로 바꿨다(이전: 객관식1 + 서술형1).
`num_items=1`은 그대로 두었으므로 **개수 디커플링(1 ≠ 2) 검증은 유지**된다.

바꾸기 전에 가설을 먼저 실측했다 — 예시=객관식2 / 생성=객관식1로 3회 채점:
`type_ratio 1.0, overall 4~5` 전부 통과. **Judge가 개수를 감점하지 않는다**는
프롬프트 명세(`문항 개수 일치 여부는 판단하지 마세요`)도 이 실측으로 확인됐다.

트레이드오프: 서술형 생성 경로는 이 스모크에서 더 이상 타지 않는다(형식 검증 자체는
`tests/test_exam_tool_gates.py`가 LLM 없이 커버).

**실패 진단도 분리했다.** 이전엔 `[실패] 목표 문항 수·구조 검증 미충족` 한 줄뿐이라
배선이 끊긴 건지 품질이 모자란 건지 알 수 없었다 — 이 모호함 때문에 18절에서 결과를
잘못 읽을 뻔했다. 이제 ① 문항 0개(배선/tool-calling) ② 개수 불일치(턴 예산)
③ Judge 게이트 미달(품질 또는 게이트 과조임)을 구분해 출력한다.

### 결과 — 6세션 (동일 조건, num_items=1 · budget=2)

| 세션 | 생성 | type_ratio | difficulty | overall | 통과 |
|---|---|---|---|---|---|
| 1 | 1개 | 1.0 | ✓ | 3 | ✅ |
| 2 | 1개 | 1.0 | ✓ | 3 | ✅ |
| 3 | 1개 | 1.0 | ✓ | 1 | ❌ |
| 4 | 1개 | 1.0 | ✓ | 1 | ❌ |
| 5 | 1개 | 1.0 | ✓ | 4 | ✅ |
| 6 | 1개 | 1.0 | ✓ | 1 | ❌ |

- **개수 달성 6/6** (18절 조건에서는 2/6이 문항 0개였다)
- **게이트 통과율 3/6 = 0.500** — 이 프로젝트에서 런타임 통과가 관측된 것은 처음이다

### 재보정 효과를 순수하게 분리 — 같은 측정에 두 기준을 적용

통과율만 보면 스모크 조건도 함께 바뀌었으니 재보정 덕인지 알 수 없다. 그래서
**동일한 6회 측정치에 옛 기준과 새 기준을 각각 적용**했다:

| 기준 | 통과 | 비율 |
|---|---|---|
| 옛 기준 (`overall>=4`, `type>=0.7`) | 1/6 | 0.167 |
| **새 기준** (`overall>=3`, `type>=0.5`) | **3/6** | **0.500** |

세 번의 통과 중 **둘이 `overall=3`** — 옛 기준이었다면 탈락했을 값이다. 즉 재보정이
런타임에서도 실제로 작동한다. 15절의 골든셋 근거(6.7%→42.2%)와 방향·규모가 모두 일치한다.

### 게이트가 실제로 변별하고 있다

`overall` 분포가 **{1, 1, 1, 3, 3, 4}로 이봉(bimodal)** 이다. 1점은 루브릭상
*"원문 단순 복사 또는 완전 중복에 의존"* — 예시와 너무 비슷한 문항을 생성한 경우다.
`save_item`의 복사 게이트(bigram containment ≥0.90)는 어휘 기준이라 이런 **의미적
근접 복제**를 못 잡는데, Judge가 그 사각지대를 메우고 있다. 게이트를 낮췄지만
"아무거나 통과"가 아니라 좋고 나쁨을 가르고 있다는 뜻이다.

### 한계

- **n=6, 로컬 스모크 조건**(`num_items=1`, `budget=2`)이다. 프로덕션은 `budget=5`라
  재시도가 실패를 더 회복시킬 여지가 있어 통과율은 더 높을 수 있다 — 여전히 미측정.
- 이번 배치의 트레이스는 18절 배치와 같은 날짜라 `eval_trajectory.py --since`(일 단위)로
  분리 집계할 수 없었다. 도구 단위 지표(malformed 비율 등)는 18절 수치를 참고할 것.
- 통과/실패를 가른 것이 "생성 품질의 자연 변동"인지 다른 요인인지는 n=6으로 단정 불가.

---

## 20. 통과율 개선 방향 진단 — 가설 2개가 모두 반증됨 (2026-08-07)

19절에서 게이트 통과율이 3/6으로 측정됐다. 이를 올릴 방법을 찾으려고 실패 원인을
진단했다(`experiments/diagnose_near_copy.py`, raw: `data/golden/_near_copy_diagnosis.json`).

### 출발 가설

19절 배치의 실패 3건이 **전부 `overall=1`**이었고, 루브릭상 1점은 "원문 단순 복사 또는
완전 중복"이다. 그래서 "실패 = 근접 복사"로 보고 두 가지를 검토했다:

- **A안(어휘)**: `save_item`의 복사 게이트가 bigram containment ≥0.90인데, 기존 실측
  주석에 *"정상적인 주제-유사 변형은 ~0.73 이하"*라 적혀 있으니 **0.73~0.90 구간이 비어
  있다.** 임계값을 0.78~0.80으로 낮추면 비용 0으로 잡을 수 있지 않을까?
- **B안(의미)**: Judge가 잡는 건 의미적 근접 복제인데 게이트는 어휘적이다. 임베딩
  코사인 유사도 게이트를 추가해야 하지 않을까?

### 측정 — 두 가설 모두 근거 없음

생성 문항마다 게이트와 **동일한 계산**의 containment와 BGE-M3 코사인 유사도를 함께 쟀다.

| overall | containment | embed_cosine | 문항 |
|---|---|---|---|
| 3 | 0.262 | 0.530 | 시장 실패의 한 가지 사례로 공공재가 무임승차하는… |
| 2 | 0.286 | 0.519 | 사회주의 경제 체제의 특징으로 맞지 않는 것은? |
| 2 | 0.600 | 0.546 | 시장 실패의 사례 중 하나는? |
| 2 | 0.450 | 0.613 | 대한민국 정치 체제의 주요 특징으로 옳지 않은 것은? |
| 3 | 0.583 | 0.682 | 대한민국 헌법이 규정하는 민주주의 원리 중 하나는? |
| 3 | 0.267 | 0.522 | 대한민국의 정치 체제는 어떤 형태인가? |

- **A안 반증**: containment 최대가 **0.600**으로 전부 0.73 미만이다. 임계값을 0.78로
  낮춰도 **잡히는 게 하나도 없고**, 0.60까지 낮추면 통과권 문항(0.583)이 함께 차단된다.
- **B안 지지 안 됨**: 통과권 평균 코사인 0.578인데 실패권과 구간이 완전히 겹친다
  (0.519~0.682 전 범위에 통과·실패가 섞임). 이 축으로는 분리가 안 된다.

### 더 중요한 발견 — 실패 원인이 배치마다 달랐다

| 배치 | overall 분포 | 통과 | 실패의 성격 |
|---|---|---|---|
| 19절 | {1,1,1,3,3,4} | 3/6 | `overall=1` — 근접 복사 |
| 이번 | {1 없음, 2,2,2,3,3,3} | 3/6 | `overall=2` — 주제 이탈·품질 저하 |

**통과율은 두 배치 모두 정확히 3/6인데 실패 사유는 완전히 다르다.** 합산 n=12에서
통과율 0.500은 안정적이지만, "실패 = 근접 복사"라는 19절 기반 진단은 **재현되지 않았다.**

즉 출발 가설 자체가 **한 배치(n=6)를 과잉 해석한 것**이었다. 11.1절에서 "malformed
0건"을 표본 부족으로 오독했던 것과 정확히 같은 실수다.

**Judge 채점 노이즈도 관측됐다**: `"대한민국의 정치 체제는 어떤 형태인가?"`(overall 3,
통과)와 `"대한민국 정치 체제의 주요 특징으로 옳지 않은 것은?"`(overall 2, 실패)은
사실상 같은 주제인데 점수가 갈렸다. 단일 문항(n=1 세트) 채점은 변동이 크다.

### 결론 — 하지 말아야 할 것과 남은 것

- **A안·B안 모두 착수하지 않는다.** 데이터가 지지하지 않는다. 특히 A안은 "비용 0"이라
  매력적이었지만 실제로는 **잡을 대상이 없다.**
- **실패 원인 특정은 n=6~12로는 불가능하다.** 원인별 개입을 하려면 표본을 먼저 늘려야 한다.
- **다음으로 값싼 것은 `budget=5`(프로덕션 조건) 측정이다.** 코드 변경 없이 현재 상태의
  실제 상한을 알려준다 — 지금까지 측정은 전부 `budget=2` 스모크 조건이었고, 재시도가
  2.5배 더 있으면 통과율이 올라갈 여지가 있다.

---

## 21. 프로덕션 조건(budget=5) 측정 — 개선 작업 없이 통과율 0.833 (2026-08-07)

20절에서 통과율 개선 가설 2건이 모두 반증됐다. 코드를 고치기 전에 **"이미 프로덕션
조건에서는 괜찮은가"**부터 확인했다 — 지금까지 측정은 전부 `budget=2` 스모크 조건이었고
프로덕션은 `budget=5`(`app/main.py`)다. `experiments/measure_budget_effect.py` 신규,
raw는 `data/golden/_budget_effect.json`.

### 결과 — 코드 변경 0줄, 통과율 0.500 → 0.833

| 지표 | budget=2 (n=12) | **budget=5 (n=6)** |
|---|---|---|
| 게이트 통과율 | 0.500 | **0.833** (5/6) |
| 개수 달성 | — | 6/6 |
| 문항 0개 | — | 0/6 |
| 평균 소요 | — | 111초 |

### 재시도가 실제로 값을 했다

"통과했지만 몇 번째 시도였나"를 함께 기록했다. 이게 핵심이다 — 전부 1회차에 통과했다면
budget을 늘린 것과 무관한 우연이기 때문이다.

| agent 실행 횟수 | 건수 | 통과 |
|---|---|---|
| 1회 (재시도 없음) | 2 | 2 |
| 2회 | 1 | 1 |
| 3회 | 1 | 1 |
| 4회 | 1 | 1 |
| 5회 (예산 소진) | 1 | **0** |

**통과 5건 중 3건이 2회차 이상에서 나왔다.** 특히 3회·4회차에서 통과한 2건은
`budget=2`였다면 예산이 모자라 실패했을 세션이다. 부분 진행 보존 설계(2026-07-10)가
의도대로 작동하고 있다는 직접 증거다.

### 교차 검증 — 두 측정이 서로를 확인한다

이 데이터로 `budget=2`를 시뮬레이션하면(agent 실행 2회 이내만 인정) **3/6 = 0.500**이
나오는데, 이는 **실제 budget=2 측정치 0.500(n=12)과 정확히 일치**한다. 두 독립 측정이
서로를 교차 검증한 셈이라 결과 신뢰도가 올라간다.

### 재보정이 없었다면 이 결과도 없었다

통과 5건의 `overall_score`가 **전부 3**이다. 같은 데이터에 옛 임계값을 적용하면:

| 기준 | 통과 |
|---|---|
| 옛 기준 (`overall>=4`) | **0/6 = 0.000** |
| 새 기준 (`overall>=3`) | **5/6 = 0.833** |

즉 **재보정(15절)이 없었다면 budget을 아무리 늘려도 0.000이었다.** 재시도 예산과
임계값 재보정이 **함께** 작동해야 통과가 나온다.

### 결론

**개선 작업은 당장 필요하지 않다.** 20절에서 검토했던 A안(어휘 임계값)·B안(임베딩
게이트)은 근거가 없었고, 실제 프로덕션 조건에서는 이미 5/6이 통과한다. 스모크 조건의
낮은 통과율(0.500)은 **의도적으로 빡빡하게 잡은 테스트 조건의 산물**이지 프로덕션
품질 문제가 아니었다.

### 한계

- **n=6**이다. 0.833은 방향성 신호이지 정밀한 추정치가 아니다.
- `num_items=1`·로컬 14B 조건이다. 문항 수가 늘면 14턴 한도가 다시 병목이 될 수 있다
  (17절 참고 — 목표 5개면 산술적으로 빠듯하다).
- 유일한 실패(1건)는 재시도 5회를 전부 소진하고도 `overall=1`(근접 복사)이었다.
  **budget이 만능은 아니다** — 특정 실행은 반복해도 같은 실패 모드에 갇힌다.
- 실사용 트래픽 기반이 아니다(RunPod 크레딧 소진, 로컬 측정).
