# 분필(Bunpil) — LLM 모델 선정 근거

> 생성(출제 문항 작성) LLM과 평가(Judge) LLM을 각각 어떤 기준으로 후보군에 올렸고,
> 무엇을 근거로 최종 모델을 채택했는지 정리한 문서. 실험 원본 데이터·회차별 로그는
> [EVAL.md](./EVAL.md) 7·7.1·9절, 실험 이력은 [bunpil_roadmap.md](./bunpil_roadmap.md)에 있으며
> 이 문서는 그 결론만 압축한 요약본이다. 실험 스크립트: `scripts/compare_models.py`(생성 모델),
> `scripts/compare_judge_models.py`(Judge 모델).

---

## 1. 생성(Generation) LLM

### 1.1 후보군 선정 기준

- **로컬 서빙 가능**(RunPod 서버리스 vLLM 배포 대상) — Qwen 계열을 우선 후보로 둔 이유.
  `LLM_BACKEND=runpod`으로 그대로 교체 가능해야 함(`app/common/llm/` 추상화 레이어 경유).
- **한국어 지시 기반 멀티턴 tool-calling**을 실제로 수행할 수 있는가 — 출제 모듈은 ReAct
  에이전트(LangGraph)로, 한 요청에 LLM을 여러 번 호출하며 도구 호출 형식을 지켜야 하므로
  이 능력이 필수 전제.
- **비교 가능성**: 동일 조건(같은 15개 `passage_text` 샘플, 동일 Judge, 동일 budget)에서
  실행 가능해야 후보로 인정. GPT 계열은 CLAUDE.md 하드룰상 프로덕션에서 실제 학생 데이터를
  다루지 않으므로(이 모듈은 교사가 붙여넣는 예시 문제 지문만 처리) 비교 실험 자체는 허용되지만,
  채택 시 외부 API 의존·비용이라는 별도 트레이드오프가 생긴다는 점을 함께 고려.
- 후보: **Qwen2.5-7B**(기존 프로덕션 기본값) / **Qwen2.5-14B** / **Llama3.1-8B** /
  **GPT-4o-mini** / **Qwen3.5-9B**(2026-07-13 추가, Ollama 라이브러리에서 실존·태그 확인 후 편입)

### 1.2 비교 방법

- `scripts/compare_models.py`: 동일 15개 `passage_text` 샘플(num_items 3/5/7, 단일·다중
  지문형 혼합)을 각 후보 모델로 "생성 모델"만 교체해 실행.
- 채점은 **고정된 하나의 Judge(qwen2.5:7b)**로 통일 — 모델이 자기 출력을 자기가 채점하는
  self-scoring bias를 배제하기 위함.
- 1차: `budget=1`(재시도 없음, "한 번에 얼마나 잘 끝내는가"). 이후 실제 프로덕션 조건인
  `budget=5`(재시도 포함)로 **재검증** — 1차 결과만으로 결론 내리면 재시도 유무에 따른
  속도·완성률 역전을 놓칠 수 있다고 판단했기 때문(실제로 아래처럼 역전이 일어남).

### 1.3 비교 결과 (budget=1 → budget=5)

| 모델 | 실패율(0문항) | 평균 생성시간 | 개수 충족률 |
|---|---|---|---|
| Qwen2.5-7B | 40% → **6.7%** | 50.3s → 258.8s | 0.15 → 0.649 |
| Qwen2.5-14B | 0% → 0% | 158.1s → **260.2s** | 0.76 → **1.036** |
| Llama3.1-8B | 60% → 26.7% | 59.4s → 289.4s | 0.11 → 0.336 |
| GPT-4o-mini | 0% → 13.3% | **18.1s → 66.5s** | 0.97 → 1.768(과다생성) |
| Qwen3.5-9B | 26.7% → 0% | 171.1s → **621.2s**(최저 속도) | 0.55 → 1.395 |

핵심 발견: **budget=1에서는 14B가 7B보다 3배 느렸지만(158s vs 50s), budget=5에서는 두
모델의 평균 생성시간이 사실상 같아진다(260.2s vs 258.8s)** — 7B는 재시도가 잦아지며 속도
이점을 스스로 까먹고, 14B는 애초에 재시도가 거의 필요 없어 손해가 없었다. 같은 속도라면
실패율(0% vs 6.7%)과 개수 충족률(1.036, 목표에 거의 정확히 도달 vs 0.649)에서 14B가 뚜렷이
우수하다.

GPT-4o-mini는 budget=1에서 전 지표를 석권하는 것처럼 보였으나, budget=5에서는 실패율이
오히려 0%→13.3%로 오르고 개수를 목표의 거의 2배까지 과다생성하는 경향이 새로 나타나
"budget=1 단발 호출에서만 안정적"인 결과였음이 드러남. Llama3.1-8B는 전 지표 최저로
후보에서 제외. Qwen3.5-9B는 정답유일성은 최고(4.79)지만 budget=5에서 속도가 621s로
가장 느려져(재시도가 누적될수록 컨텍스트가 길어지며 특히 느려지는 것으로 추정) 우위가 없음.

오답매력도는 5개 모델 전부 2.3~2.9 수준(목표 4.0)으로 모델 차이가 아니라 Judge/생성
프롬프트 설계 자체의 한계로 판단 — 모델 교체로 해결되는 지표가 아님을 확인.

### 1.4 최종 채택: **Qwen2.5-14B**

- **판단 근거**: 로컬 스택 유지를 전제로 실제 서비스 조건(budget=5)에서 재검증한 결과,
  14B가 7B 대비 "속도 손해 없이"(같은 260초대) 실패율·개수 충족률에서 뚜렷이 우수 —
  가장 데이터로 잘 뒷받침되는 변화. GPT-4o-mini는 속도는 압도적이지만 budget=5에서
  실패율 상승·과다생성이라는 새 결함이 드러나 "무조건 최선"이라 보기 어려워졌고, 외부
  API 의존·비용·데이터 유출 우려(교사 지문 데이터가 외부로 나감)라는 트레이드오프도 있어
  최종 채택에서는 보류.
- **적용**: `.env.example`의 `OLLAMA_MODEL=qwen2.5:14b`, 커밋 `22a2628`(생성 모델을 Qwen2.5
  7B에서 14B로 전환), RunPod은 AWQ 4bit 양자화로 서빙(RTX A5000 24GB 기준 메모리 제약 대응).
- **남은 채택 후보로 보류한 것들**: GPT-4o-mini(속도 이점은 확실하나 budget=5 결함·비용·외부
  의존 트레이드오프로 다음 우선순위), Qwen3.5-9B(품질은 우수하나 속도 열위로 로컬 대안으로도
  14B 대비 우위 없음), Llama3.1-8B(전 지표 최저로 제외).

---

## 2. 평가(Judge) LLM

### 2.1 왜 생성 모델과 Judge 모델을 분리했는가

`OLLAMA_MODEL`(생성)과 `OLLAMA_JUDGE_MODEL`(채점)을 별도 환경변수로 분리(미설정 시
`OLLAMA_MODEL`로 폴백). 이유: ①생성 모델을 바꿀 때마다 채점 기준(Judge)까지 흔들리면
회차 간 비교가 불가능해짐 ②Judge는 "정답을 만드는" 능력보다 "판단·비교" 능력이 중요해
반드시 같은 크기/모델일 필요가 없음.

### 2.2 후보군 선정 기준

- **사람 라벨과의 신뢰도**(Cohen's kappa, MAE, ±1 일치율)로 정량 검증 가능해야 함 —
  DESIGN.md 5절 원칙 "LLM Judge는 사람 라벨과 먼저 일치율 검증"에 따름.
- 로컬 오픈모델(Qwen2.5-7B/14B)로 몇 달간 프롬프트(few-shot 앵커, rubric 주입 등)를
  튜닝했음에도 문항품질 kappa 0.328, 구조유사도 overall 이진 kappa 0.167로 목표(≥0.4)
  미달이 장기간 지속 — "생성 모델을 바꿔도 안 풀리는 문제인가, Judge 자체의 한계인가"를
  가리기 위해 **생성물은 고정하고 Judge만 교체**하는 비교 실험을 별도로 설계.
- 후보: **qwen2.5:7b**(기존 Judge 기본값) vs **gpt-5.6-luna**(OpenAI, Judge 비교 실험 전용).
  합성 데이터(ITEM_GOLDEN·STRUCTURE_GOLDEN, 전부 사람이 만든 가상 시나리오/실제 공개
  성취기준 기반 생성물)만 대상이라 하드룰 1(실제 학생 데이터 미사용)에 저촉되지 않음.

### 2.3 비교 결과 (`scripts/compare_judge_models.py`, 2026-07-17)

같은 생성물(ITEM_GOLDEN n=30, STRUCTURE_GOLDEN n=45)을 두 Judge로 각각 재채점만 실행 —
생성은 재실행하지 않음.

| 지표 | qwen2.5:7b | gpt-5.6-luna |
|---|---|---|
| 문항품질 kappa | 0.328 | **0.595** |
| 문항품질 exact 일치 | 0.267 | 0.300 |
| 문항품질 llm_avg (편향) | 3.83 (+0.50) | 4.27 (**+0.94**, 더 후한 채점) |
| 문항품질 ±1 일치율 | **0.800** | 0.733 |
| 구조유사도 difficulty_match | 0.867 | 0.933 |
| 구조유사도 overall MAE | 1.689 | **0.600** |

GPT 쪽 프롬프트는 별도 튜닝 없이 qwen용으로 만든 few-shot(3점 앵커 등)을 그대로 재사용했는데도,
overall MAE 0.600은 qwen2.5-14B를 몇 달간 튜닝해 얻은 최고 기록(MAE 1.185, 2026-07-12)보다도
낮음.

### 2.4 최종 채택: **gpt-5.6-luna (2026-07-21 결정)**

- **판단 근거**: GPT가 이진 판단 일치도(kappa)·MAE 전반에서 더 신뢰할 만한 채점을 냄
  (문항품질 kappa 0.328→0.595, 구조유사도 overall MAE 1.689→0.600 — 몇 달간 튜닝한
  qwen2.5-14B 최고 기록 1.185보다도 낮음). 절대 점수 편향(+0.94)이 qwen(+0.50)보다
  크다는 상충 신호는 있으나, 표본이 작다는 한계(n=30/45)를 감안해도 신뢰도 지표
  전반의 개선폭이 편향 증가분보다 크다고 판단해 채택.
- **교체 시 감수한 트레이드오프**: 비용은 걸림돌 아님(한 사이클 105회 호출에 약 $0.10).
  과거 EVAL.md의 kappa/MAE 히스토리가 전부 qwen 기준이라 Judge 교체 이후 수치는
  직접 비교가 아니라 재보정 기준선으로 다시 쌓아야 함 — 이 단절은 감수하기로 결정.
- **적용**: `.env.example`의 `JUDGE_BACKEND=openai`(기본값), `OPENAI_JUDGE_MODEL=gpt-5.6-luna`.
  로컬 `OLLAMA_JUDGE_MODEL=qwen2.5:14b`는 `JUDGE_BACKEND=local`로 되돌릴 때를 위한
  대안 경로로 유지(어댑터 구현은 이미 존재, `scripts/compare_judge_models.py` 참고).
- **생성 모델과 다른 백엔드를 쓰는 이유**: 생성은 로컬 Qwen2.5-14B(1.4절, 비용·데이터
  로컬 처리 우선), Judge는 gpt-5.6-luna(신뢰도 우선) — 두 역할의 우선순위가 다르다는
  점(§2.1)이 그대로 서로 다른 채택 결론으로 이어진 사례.

### 2.5 아키텍처 변경 — 런타임 self-judge 폐기, 별도 judge 노드로 분리 (2026-07-23)

gpt-5.6-luna를 Judge로 채택한 시점엔 `JUDGE_BACKEND`가 **오프라인 eval 스크립트에만**
영향을 줬다 — 런타임 구조 유사도 판단은 생성 에이전트 자신이 `similarity_judge`
도구로 스스로 채점했다(self-judge). 이 self-judge 신뢰도는 사람 라벨과 한 번도
대조된 적이 없었고, "검증에 쓰는 Judge"(오프라인, `get_judge_backend()`)와 "실제
배포된 judge"(런타임, 생성 모델 자기채점)가 서로 다른 코드였다 — **검증-배포
불일치**.

**결정**: 생성 모델과 Judge 모델을 런타임에서도 완전히 분리한다. `similarity_judge`
도구를 제거하고 별도 `judge` 그래프 노드를 추가해 `get_judge_backend()`를 호출하며,
오프라인 eval(`judge_structure_one()`)과 완전히 같은 함수(`app/modules/exam/judge.py`의
`judge_structure()`)를 공유한다 — 이제 EVAL.md의 구조 Judge 신뢰도 수치가 곧 실제
배포된 judge의 신뢰도다.

**감수한 트레이드오프**: `JUDGE_BACKEND=openai`(기본값)에서는 매 문항 세트 생성마다
`passage_text`가 OpenAI로 전송된다(PII는 마스킹되지만 저작권 있는 교사 지문 자체는
마스킹 대상 아님). API 키 누락·호출 실패 시 조용히 로컬로 폴백하지 않고 그대로
실패한다(fail-fast) — 신뢰도가 검증되지 않은 채로 게이트를 통과시키는 문제를
반복하지 않기 위함. 로컬 전용으로 처리하려면 `JUDGE_BACKEND=local`로 전환.

---

## 3. 공통 제약 (두 선정 모두에 적용)

- **하드룰 1 (실제 학생 데이터 미사용)**: 두 비교 실험 모두 합성/공개 데이터 기반 골든셋만
  사용 — 생성 모델 비교는 교사 입력 예시 문제 지문(개인정보 아님), Judge 비교는
  ITEM_GOLDEN/STRUCTURE_GOLDEN(전부 합성/실제 성취기준 기반 생성물).
- **GPT/OpenAI 백엔드의 용도**: 생성 모델 비교 실험은 여전히 합성 데이터 전용. Judge는
  2026-07-23부터 프로덕션 런타임에도 gpt-5.6-luna를 기본값으로 실제 사용(§2.5) — "로컬
  우선"은 생성 모델에는 유지되지만 Judge에는 신뢰도를 우선한 의도적 예외.
- **RunPod 서버리스는 컴퓨트 시간 과금** — 생성 모델 선정에서 "속도가 느리면 곧 비용
  증가"라는 제약이 작용(14B 채택 판단에서 budget=5 속도 동률 확인이 중요했던 이유).
