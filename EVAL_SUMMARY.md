# 분필(Bunpil) 평가 체계 한눈에 보기

> 이 문서 하나로 **무엇을 왜 재는지 · 어떻게 재는지 · 그래서 뭐가 좋아졌는지**를 파악할 수
> 있게 압축한 요약본이다. 회차별 raw 로그와 시행착오 전체는 [EVAL.md](./EVAL.md), 어떤
> 스크립트가 뭘 담당하는지는 [EVAL_MAP.md](./EVAL_MAP.md), 모델·검색 방식을 왜 그걸로
> 골랐는지는 [MODEL_SELECTION.md](./MODEL_SELECTION.md)에 있다.
>
> 2026-08-03 생기부 윤문 모듈을 걷어내면서 지금 평가 대상은 **출제 모듈 하나**다. 과거
> 생기부 평가 기록은 역사적 사실로 EVAL.md에 남겨뒀고, 왜 걷어냈는지는 4.5절에 있다.

**표기 약속** — "이 지표를 왜 골랐나"를 설명할 때 근거의 출처를 두 가지로 구분한다.

- **〔문서〕** 프로젝트 문서(DESIGN.md·EVAL.md 등)에 실제로 적혀 있는 근거. 출처를 함께 적었다.
- **〔추론〕** 문서에 기록이 없어, 코드와 남은 정황을 근거로 지금 다시 세운 설명이다.

**목차**

1. [이 평가 체계는 무엇을 재려고 만들었나](#1-이-평가-체계는-무엇을-재려고-만들었나)
2. [런타임 평가 — 문항 하나가 나가기까지](#2-런타임-평가--문항-하나가-나가기까지-3단계)
3. [오프라인 평가 — 지표 전체 목록과 선택 근거](#3-오프라인-평가--지표-전체-목록과-선택-근거) ← **지표 목록만 필요하면 [3.3절](#33-정기-평가-지표-전체-목록--evals)**
4. [개선 스토리 — 무엇을 바꿔서 얼마나 좋아졌나](#4-개선-스토리--무엇을-바꿔서-얼마나-좋아졌나)
5. [현재 열린 이슈](#5-현재-열린-이슈-2026-08-07-기준)
6. [참고 문서 맵](#6-참고-문서-맵)

---

## 1. 이 평가 체계는 무엇을 재려고 만들었나

### 1.1 진짜 알고 싶은 것은 하나, 나머지는 그걸 대신 재는 값

DESIGN.md 5절은 최종 목표 지표를 하나만 적어뒀다 — **"교사가 수정 없이 그대로 쓴 비율"**
(북극성 지표)〔문서〕. 다른 지표는 전부 이것을 대신 재는 값이다.

문제는 이 북극성을 직접 잴 수 없다는 점이다. 실사용자가 지인 교사 1명이라 A/B로 나눌
트래픽이 없고, 하드룰 1(실제 학생 데이터 미사용) 때문에 현장 데이터를 모아 검증할 수도
없다. 그래서 **"교사가 그대로 쓸 만한 문항인가"를 사람이 미리 라벨링해 둔 골든셋으로
근사한다.** 지표가 여러 개인 이유는 문항 하나가 버려지는 경로가 여러 갈래이기 때문이다 —
검색이 엉뚱한 근거를 물어와서, 문항 자체가 허술해서, 세트 구성이 요청과 달라서.

### 1.2 설계 원칙 3개 — 왜 이런 모양이 됐나

DESIGN.md 5절이 못박은 원칙이다〔문서〕. 각 원칙이 이 프로젝트에서 왜 필요했는지 덧붙인다.

| 원칙 | 이 프로젝트에서의 의미 |
|---|---|
| ① **LLM Judge는 사람 라벨과 먼저 대조한다** | 문항의 좋고 나쁨은 함수로 못 재니 채점을 LLM에 맡길 수밖에 없다. 그러면 "Judge가 제대로 채점하는가"라는 질문이 새로 생긴다. 그래서 **지표의 절반이 결과 품질이 아니라 Judge 신뢰도**다(κ·±1 일치율·MAE·편향). 이걸 건너뛰면 Judge가 후하게 준 점수를 품질 개선으로 착각한다 |
| ② **정량은 함수로, 정성은 Judge·사람으로 나눈다** | 검색 Recall처럼 정답이 정해진 값에는 LLM을 쓰지 않는다. 섞으면 검색이 못 찾은 건지 Judge가 잘못 본 건지 원인이 갈라지지 않는다 |
| ③ **골든셋은 실제 컬렉션에서 만든다** | 검색 골든셋 22건은 실제 ChromaDB 청크에서 뽑아 사람이 전량 검수했다. 사람이 지어낸 질문으로 재면 실제 코퍼스에 없는 걸 묻게 되고, 검색기 성능이 아니라 질문의 난이도를 재게 된다 |

이 원칙들이 실제로 방향을 결정한 사례가 4.1절(self-judge 폐기)과 4.5절(코퍼스에 조항 자체가
없었던 사건)이다.

### 1.3 런타임 평가 vs 오프라인 평가

| | 런타임 평가 | 오프라인 평가 |
|---|---|---|
| **언제** | 교사가 문항을 생성하는 매 요청마다 | 모델·프롬프트·검색 방식을 바꿀 때 사람이 수동으로 실행 |
| **무엇을 보나** | 이번에 만든 세트가 기준을 넘는지(재시도할지) | 그 기준을 매기는 Judge와 검색기 자체를 믿어도 되는지 |
| **판단 주체** | 코드(임계값) — 근거는 Judge LLM이 매긴 점수 | 사람 라벨(골든셋)과 대조한 Judge·검색기의 정확도 |
| **코드 위치** | `app/modules/exam/graph.py`(`judge_node`, `validate_node`) | `evals/`, `tests/`, `experiments/` |

한 줄로 요약하면, **런타임은 "이번 결과물이 기준을 넘었나"를 매번 판정하고, 오프라인은 "그
기준 자체가 사람 판단과 얼마나 맞나"를 검증한다.** 2026-07-23부터 둘은 **같은 채점 함수**
(`judge.py`의 `judge_structure()`)를 쓴다. 그래서 오프라인에서 잰 신뢰도가 곧 실제 배포된
Judge의 신뢰도다(3절).

---

## 2. 런타임 평가 — 문항 하나가 나가기까지 3단계

```
agent node(생성 LLM) → judge node(별도 Judge LLM) → validate(코드) → 통과 시 종료 / 미달 시 재시도(budget)
```

### 2.1 agent node — `save_item`의 결정론적 게이트 3개 (코드, LLM 판단 아님)

| 게이트 | 실제 기준 | 이 기준이 나온 근거 |
|---|---|---|
| 한자 오염 | 한자/(한자+한글) 비율이 **5% 이상**이면 거부. 한글이 0개면 무조건 거부 | 비한국어 전체가 아니라 **CJK 한자 비율**만 본다(영어 오염은 이 게이트로 안 잡힌다) |
| 예시 단순 복사 | 생성 문항과 예시 문제(`passage_text`)의 bigram containment **≥ 0.90** | 완전 복사는 실측 1.00, 정상 변형은 0.73 이하였다. 1.00보다 낮게 잡아 근접 복사까지 막는다 |
| 세트 내 중복 | 이미 저장된 문항과 bigram Jaccard 유사도 **≥ 0.80** | 같은 요청 안의 draft 세트끼리만 비교한다(외부 DB와 대조하지 않는다) |

세 게이트 중 하나라도 걸리면 `save_item`이 저장을 거부하고, 거부 사유를 담은 메시지를
에이전트에게 그대로 돌려준다. 에이전트는 같은 턴 안에서 그 사유를 보고 다시 쓴다 — 그래프
단위의 재시도가 아니라, 도구 호출 루프 안에서 일어나는 자가교정이다.

> **왜 이 셋을 코드로 막나**
> 한자 오염은 상상해서 만든 위험이 아니라 실제로 겪은 버그다. qwen2.5:7b가 한국어로 지시해도
> 중국어를 섞어 뱉는 사례가 반복 관찰됐고(TROUBLESHOOTING.md), RAG 품질 평가 도구를 만들다가
> **5% 문턱에 못 미치는 옅은 오염**까지 발견됐다(EVAL.md 8절)〔문서〕.
> 셋 다 LLM 없이 셀 수 있는 값이라 설계 원칙 ②에 따라 함수로 처리했다. 여기서 Judge를 부르면
> 느리고 비쌀 뿐 아니라, 판정이 실행할 때마다 흔들린다〔추론〕.

### 2.2 judge node — 구조 유사도 채점 (별도 Judge LLM)

에이전트가 `submit_for_review`를 호출해 작성을 마치면(또는 14턴 한도에 걸려 루프가 끝나면),
`judge` 노드가 생성 모델과 **완전히 분리된 백엔드**(`get_judge_backend()`, 기본값
`gpt-5.6-luna`)로 세 값을 매긴다.

| 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|
| `type_ratio_score` (0~1) | Judge LLM이 (예시 문제, 생성 세트) 쌍을 보고 직접 매긴다 — 계산식이 아니라 루브릭과 few-shot 6개로 유도한다 | 문항 유형(객관식·서술형 등) 구성이 예시를 따라갔는가 |
| `difficulty_match` (참/거짓) | 〃 | 난이도 구성이 예시와 맞는가 |
| `overall_score` (0~5 정수) | 〃 — 유형·난이도·주제의 유사성에서 중복·원문복사·환각·언어오염을 깎아 합산한다. 0~5 루브릭과 점수대별 앵커 예시를 시스템 프롬프트에 넣어 점수가 흔들리지 않게 한다 | 세트 전체의 종합 품질. 재시도 여부를 사실상 결정하는 단일 지표 |

세 값은 `judge_structure()`(`app/modules/exam/judge.py`)가 JSON 하나로 함께 돌려준다.
3.3절의 "구조 유사도 Judge 신뢰도"가 바로 **이 세 값의 신뢰도**다.

> **왜 생성 모델과 Judge를 갈랐나** (2026-07-23, "옵션 B")〔문서: EVAL_MAP.md 7.1절〕
> 원래는 생성 에이전트가 `similarity_judge` 도구로 자기 출력을 스스로 채점했다(self-judge).
> 이 self-judge의 신뢰도는 사람 라벨과 한 번도 대조된 적이 없었고, 더 큰 문제는 **오프라인
> eval이 검증하는 Judge와 실제 배포된 judge가 서로 다른 코드**였다는 점이다. 검증한 것과
> 배포한 것이 달랐다는 뜻이다. 그래서 런타임 judge_node와 오프라인 eval이 같은
> `judge_structure()`를 쓰도록 합쳤다 — 동기는 "self-bias 방지"보다 **"검증한 것과 배포한
> 것을 같게 만든다"**는 쪽이 먼저였다(4.1절).

### 2.3 validate node — 재시도 여부 결정 (코드, LLM 미사용)

아래 **4개 조건을 전부 만족해야(AND)** 통과한다(`graph.py:330-334`). Judge가 매긴 3개
지표뿐 아니라 문항 개수도 함께 본다.

| 조건 | 기준 | 판단 주체 |
|---|---|---|
| 개수 일치 | 생성된 문항 수 == 요청한 `num_items` | 코드(`len(draft_items)` 비교) |
| 유형 비율 | `type_ratio_score` ≥ 0.5 | Judge 값에 코드가 임계값을 적용 |
| 난이도 일치 | `difficulty_match` == true | 〃 |
| 종합 점수 | `overall_score` ≥ 3 | 〃 |

넷을 다 통과하면 재시도 없이 세트를 제출한다. 하나라도 미달이면 남은 `budget`(기본 5회)
안에서 부족한 만큼만 이어서 다시 만든다. **budget이 떨어지면 미달 상태로도 종료**하므로,
"종료"가 곧 "성공"은 아니다.

> **왜 개수는 Judge가 아니라 코드가 보나**〔문서: EVAL.md 1절, 2026-07-09〕
> 원래는 "생성 개수가 예시 문제의 문항 수와 같아야 한다"는 전제로 Judge가 함께 봤다.
> 그런데 개수는 예시와 무관하게 `ExamSpec.num_items`로 따로 지정된다는 사실이 드러나 전제
> 자체가 틀린 것으로 판명됐다. 셀 수 있는 값을 LLM에게 물어볼 이유가 없으니 코드로 옮겼다.

**이 게이트가 지금 모습이 되기까지** — 두 번의 변경이 있었다.

| 시점 | 무엇을 바꿨나 | 왜 |
|---|---|---|
| 2026-08-04 | 임계값 재보정: `overall ≥ 4`·`type_ratio ≥ 0.7` → **`≥ 3`·`≥ 0.5`** | 옛 기준의 실측 통과율이 6.7~8.9%라 사실상 통과가 불가능했다(Judge가 4~5점을 거의 주지 않았다). 재보정 후 42.2~48.9% — 4.6절·EVAL.md 15절 |
| 2026-08-05/06 | `record_score`(에이전트 자기채점)의 "전량 승인" 조건을 게이트에서 제외 → 이어서 **도구 자체를 제거** | 사람 라벨과 대조된 적 없는 지표가 통과 여부를 결정하고 있었다. 게이트에서 뺀 뒤에도 문항당 4턴 중 1턴을 쓰고 있었는데, 정작 병목은 14턴 한도였다 — EVAL.md 16·17절 |

---

## 3. 오프라인 평가 — 지표 전체 목록과 선택 근거

### 3.1 4개 계층

Judge와 검색기를 믿어도 되는지, 코드가 늘 같은 방식으로 동작하는지를 사람 라벨·골든셋과
대조해 확인하는 계층이다. 넷 중 `evals/`만 "품질 게이트"이고 나머지는 성격이 다르다.

| 계층 | 위치 | 확인하는 것 | 실행 시점 |
|---|---|---|---|
| ① 정기 평가 | `evals/eval_exam.py`, `eval_ragas.py`, `eval_trajectory.py` | 골든셋 대비 정량 지표(검색 Recall, Judge 신뢰도, RAG 충실도, 에이전트 궤적) | 모델·프롬프트를 바꿀 때마다 수동 실행 |
| ② pytest | `tests/*.py` | 결정론적 로직(마스킹, 저장 게이트, 검증 순서) | `pytest`로 회귀 방지 |
| ③ 스모크 | `scripts/test_*.py` | 로컬 Ollama로 배선이 끊기지 않았는지 | 구조를 바꾼 직후 1회 |
| ④ 일회성 실험 | `experiments/*.py` | 의사결정 하나(A/B, 파라미터 튜닝) | 이미 끝남, 결과는 `data/golden/_*.json`에 보관 |

②와 ③은 수치가 아니라 **통과·실패만** 내므로 아래 지표 목록에서 뺐다. 즉 "관리하는 지표"는
①(3.3절)이고, ④는 결정을 내리려고 한 번 재고 끝낸 값들(3.4절)이다.

### 3.2 골든셋 — 무엇으로 채점하나

| 골든셋 | 규모 | 사람 라벨 | 쓰는 지표 |
|---|---|---|---|
| `retrieval_golden_final.json` | 22 (standards 12 + regulations 10) | 전량 검수 | Recall@5, MRR |
| `item_golden.json` (ITEM_GOLDEN) | 30 | `human_score` | 문항 품질 5개 + 문항 Judge 신뢰도 5개 |
| `structure_golden.json` (STRUCTURE_GOLDEN) | 45 | `human_label` | 구조 Judge 신뢰도 3개 |
| 마스킹 20건 | 20 | — | 2026-08-03부터 `tests/test_masker.py`가 흡수(지표가 아니라 통과·실패) |

> **골든셋을 이렇게 만든 이유와, 이미 알고 있는 한계**
> - 검색 골든셋은 실제 ChromaDB 컬렉션에서 뽑아 전량 사람이 검수했다(설계 원칙 ③)〔문서〕.
> - 골든셋은 전부 `data/golden/*.json`으로 빼놨다. 하드코딩을 금지한 건 실제로 데인 적이
>   있어서다 — ITEM_GOLDEN이 코드에 박혀 있던 시절, 생성 프롬프트를 고친 효과를 이 골든셋으로
>   재려다 **고정된 문항에 프롬프트 변경이 반영될 리 없다**는 방법론 오류를 뒤늦게 발견했다
>   (bunpil_roadmap.md, EVAL.md 4절 정정)〔문서〕. 그 뒤로 "생성 쪽 변경은 실제 생성물로 재고,
>   골든셋은 Judge 쪽 검증에 쓴다"로 역할을 갈랐다.
> - 학생 데이터는 하드룰 1로 금지돼 있어 마스킹 골든셋은 전부 합성이다〔문서: CLAUDE.md〕.

### 3.3 정기 평가 지표 전체 목록 — `evals/`

정기적으로 관리하는 지표는 **총 32개**이고, 그중 통과·실패 게이트가 걸린 건 **4개**(아래 ✅)다.
나머지는 참고값이거나, 문제가 생겼을 때 원인을 쪼개 보는 값이다.

| 스크립트 | 지표 수 | 한 줄로 | 게이트 |
|---|---|---|---|
| `eval_exam.py` | 15 | 검색이 정답을 찾는가 + 문항이 좋은가 + **Judge를 믿어도 되는가** | 4개 |
| `eval_ragas.py` | 4 | 만든 문항이 검색해온 근거에 충실한가 | 없음 |
| `eval_trajectory.py` | 13 | 에이전트가 어디서 헛도는가(실패·재시도 원인 분해) | 없음 |

---

#### (1) `evals/eval_exam.py` — 15개 (로직은 `evals/eval_lib.py`)

**① 검색 성능** — `eval_retrieval()` · 골든셋 22건

| 지표 | 구하는 법 | 무엇을 보나 | 기준 |
|---|---|---|---|
| `recall_at_5` | 골든셋 쿼리를 `top_k=5`로 검색해, 정답 청크 preview의 앞 80자가 결과 본문에 들어 있는 쿼리 수 ÷ n | 검색이 정답을 후보 안에 넣기는 하는가 | ✅ ≥ 0.8 |
| `mrr` | Σ(1 ÷ 정답이 나온 순위) ÷ n (1위=1.0, 2위=0.5) | 넣은 정답을 얼마나 위로 올리는가(리랭커 품질) | 참고값(≥0.6) |

> **왜 이 두 개인가**
> 검색은 정답 청크가 정해져 있어 LLM 없이 셀 수 있다. 설계 원칙 ②에 따라 함수로 잰다〔문서〕.
> Recall이 게이트이고 MRR이 참고값이라는 건 EVAL.md 1절에 명시돼 있지만, 왜 그렇게 갈랐는지는
> 적혀 있지 않다. 정황상 **생성 LLM은 검색된 청크를 통째로 프롬프트에 넣기 때문에, 정답이
> 후보 안에 있기만 하면 1위든 3위든 결과가 크게 달라지지 않는다.** 반면 후보에 아예 없으면
> 근거 없는 문항이 나온다. 그래서 "있느냐(Recall)"는 합격선으로, "몇 등이냐(MRR)"는 리랭커
> 상태를 보는 진단 신호로 둔 것으로 보인다〔추론〕.
> 기준 0.8은 DESIGN.md 5절에 "시작값"으로 제시된 뒤 재검토된 기록이 없다. 5개 후보 중 4번은
> 건져야 한다는 실용적인 선으로 읽힌다〔추론〕.
>
> **알아둘 어긋남**: 런타임 검색 도구는 `top_k=3`인데(`tools.py:103`) 이 지표는 5로 잰다.
> 즉 Recall@5는 실제 운영보다 **한 뼘 관대한 조건**이다. top_k를 3→2로 줄이는 실험에서 Recall이
> 0.810→0.762로 떨어진 기록이 있으니(bunpil_roadmap.md) 이 격차는 실재한다〔추론〕.

**② 문항 품질** — `judge_one()` → `eval_item_quality()` · ITEM_GOLDEN 30건
Judge가 문항 하나(질문·선지·정답)만 보고 세 기준을 1~5 정수로 채점한다(few-shot 3개).

| 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|
| `avg_정답유일성` | 문항별 점수의 산술평균 | 정답이 하나로 확정되는가 — 2026-08-07 프롬프트 개입으로 3.375 → **4.125**(목표 달성, EVAL.md 23절) |
| `avg_오답매력도` | 〃 | 오답 선지가 그럴듯한가 — 3.75에서 유일성 개입 후 3.50. 맞바꿈일 가능성이 있어 지켜보는 중 |
| `avg_근거성` | 〃 | 교육과정에 근거한 문항인가 (4.375로 셋 중 가장 안정적) |
| `avg_overall` | 문항별로 3기준 평균을 낸 뒤 다시 전체 평균 | 문항 품질 대표값 · ✅ **≥ 4.0** |
| `pass_rate` | `overall ≥ 4.0`인 문항의 비율 | 평균 하나로는 안 보이는 분포 — "몇 개나 합격선을 넘었나" |

> **왜 하필 이 세 기준인가**
> 세 기준은 DESIGN.md 5절에 설계 초기부터 적혀 있지만, 왜 이 셋인지는 어디에도 없다〔문서 없음〕.
> **교사가 문항을 버리는 이유를 셋으로 나눈 것**으로 보인다 — ① 정답이 둘이면 시험 자체가
> 무효가 되고, ② 오답이 뻔하면 변별이 안 돼 쓸모가 없고, ③ 교육과정 밖이면 출제 범위를
> 벗어난다. 품질을 잘게 쪼갠 게 아니라 **폐기 사유를 분류한 쪽에 가깝다**〔추론〕.
> 이 해석은 나중에 실제로 뒷받침됐다. 2026-08-07 재측정에서 8건 중 3건이 **정답이 실제로 두
> 개인** 파손 문항이었고, 그래서 추적 타깃을 오답매력도에서 정답유일성으로 옮겼다. 프롬프트에
> 자가 점검 지시를 넣자 치명적 실패가 37.5%→12.5%로 줄었다(EVAL.md 22·23절)〔문서〕.
> 세 기준의 무게가 서로 다르다는 것도 이때 드러났다 — 정답이 두 개인 문항은 교사가 못 쓰지만,
> 오답이 조금 약한 문항은 쓸 수 있다.
>
> **왜 평균과 합격률을 같이 보나**: 평균만 보면 경계에 몰린 문항을 놓친다. 실제로 few-shot 하나를
> 추가했을 때 평균은 조금 움직였는데 합격률은 47%→73%로 크게 뛴 적이 있다 — 4.0 문턱 근처에
> 문항이 잔뜩 몰려 있었다는 뜻이다(EVAL.md 4절 주석)〔문서〕.
> 기준 4.0은 DESIGN.md에 "(보정 후 확정)"이라는 단서와 함께 시작값으로 적혀 있다. 5점 척도에서
> "손대지 않고 그대로 쓸 수 있다"의 하한선으로 잡은 것으로 보인다〔추론〕.

**③ 문항 Judge 신뢰도** — `eval_judge_reliability()` · 같은 30건
Judge가 매긴 `overall`을 반올림해 정수로 만든 뒤 사람 점수(`human_score`)와 맞춰 본다.

| 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|
| `exact_agreement` | 사람 점수 == Judge 점수인 비율 | 가장 엄격한 일치도(참고값) |
| `agreement_within_1` | \|사람 − Judge\| ≤ 1인 비율 | "거의 맞았다" 기준 · ✅ **≥ 0.7** |
| `cohen_kappa` | 두 점수를 `≥3`으로 이진화한 뒤 (Po − Pe) / (1 − Pe) | 우연 일치를 걷어낸 실질 일치도 · ✅ **≥ 0.4** |
| `human_avg` | 사람 점수의 평균 | 편향을 재는 기준선 |
| `llm_avg` | Judge 점수의 평균 | **`llm_avg − human_avg`가 곧 편향(bias)** — 코드는 두 평균만 내고, 빼는 건 사람이 한다 |

> **이 묶음이 존재하는 이유**가 설계 원칙 ① 그 자체다〔문서〕. 문항 품질 점수(②)는 Judge가
> 매긴 값이라, Judge를 검증하지 않으면 ②는 아무 의미가 없다.
>
> **왜 일치율만으로는 안 되고 κ가 필요한가**: 사람 점수가 특정 값에 몰려 있으면 아무렇게나
> 찍어도 일치율이 높게 나온다. 가정이 아니라 이 프로젝트에서 실제로 겪은 일이다 — 구조 Judge
> 쪽에서 Judge 점수가 {3,4}에만 몰려 분산이 0이 되자 κ가 0.000으로 주저앉았다(EVAL.md 4절)〔문서〕.
> κ는 그런 상황을 드러내라고 있는 지표다.
> **κ와 ±1을 같이 보는 이유**: 5점 척도에서는 사람끼리도 1점은 흔히 갈린다. κ 하나만 보면
> 실무적으로 쓸 만한 Judge까지 탈락시킨다. 그래서 관대한 기준을 하나 더 둔 것으로 보인다〔추론〕.
> 기준값 0.4와 0.7이 어디서 왔는지는 문서에 없다. 0.4는 κ 해석 관례(0.41~0.60 = moderate)의
> 시작점과 맞아떨어지고, 0.7은 "10개 중 7개는 사람과 1점 이내"라는 눈높이로 보인다〔추론〕.
>
> **평균 두 개(편향)를 왜 남기나**: 편향은 실제로 문제를 잡아낸 적이 있다. Judge 평균이 사람보다
> 높게 뜨는 원인을 파고들었더니 few-shot 점수 분포가 {1,1,2,4,5}로 **3점이 비어 있었고**, Judge가
> 애매한 사례를 3점으로 회피하고 있었다. 3점 앵커를 채워 넣자 MAE·±1·κ가 함께 좋아졌다
> (EVAL.md 5절 "3점 미앵커링")〔문서〕.

**④ 구조 유사도 Judge 신뢰도** — `eval_structure_judge()` · STRUCTURE_GOLDEN 45건
2.2절의 런타임 `judge_structure()`를 그대로 불러서 사람 라벨(`human_label`)과 맞춰 본다.

| 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|
| `difficulty_match_agreement` | Judge의 `difficulty_match`가 사람 라벨과 같은 비율 | 난이도 판정을 사람과 같게 내리는가 |
| `overall_score_mae` | \|Judge `overall` − 사람 `overall`\|의 평균 | 점수가 몇 점이나 어긋나는가(방향 무관) |
| `count_match_code_rate` | `len(generated_items) == num_items`인 비율 | **사람↔Judge 대조가 아니다** — 골든셋 엔트리 자체의 데이터 정합성 확인용 |

> **왜 여기는 κ가 아니라 MAE인가** — 이 프로젝트에서 가장 값비싼 교훈이다〔문서: EVAL.md 5·6절〕.
> 원래는 여기도 "overall을 ≥3으로 이진화한 κ ≥ 0.4"를 목표로 뒀다. 그런데 몇 달을 튜닝해도
> 0.000~0.178을 벗어나지 못했다. 원인은 Judge가 못해서가 아니라 **이진 κ가 이 데이터에 안
> 맞아서**였다 — Judge 점수가 {3,4}에 몰리면 분산이 0이 되어 κ가 정의상 0으로 붕괴한다.
> 같은 기간 연속 지표인 MAE는 2.15 → 1.80 → 1.00으로 개선을 또렷이 보여주고 있었다.
> 2026-07-24 사용자 확인을 거쳐 **이진 κ 목표를 공식 폐기**하고 difficulty 일치율과 MAE 두
> 개로 정리했다. 난이도는 참·거짓이라 MAE를 낼 수 없어 일치율을 쓴다〔추론〕.
>
> **왜 게이트가 없나**: 이 값들은 배포된 Judge를 얼마나 믿을 수 있는지를 보여줄 뿐, 이번
> 생성물의 합격 여부가 아니다. 통과·실패 판정은 런타임 게이트(2.3절)가 이미 하고 있다
> 〔문서: EVAL.md 1절〕.
>
> **`count_match_code_rate`가 여기 섞여 있는 이유**: 개수는 2026-07-09에 Judge 대상에서
> 빠졌지만(2.3절), 골든셋 엔트리 자체가 `num_items`와 맞는지는 여전히 확인해야 해서 남았다.
> 사람과 Judge를 비교하는 값이 아니므로 표에 그렇게 못박아 뒀다〔문서〕.

---

#### (2) `evals/eval_ragas.py` — 4개

Ragas 패키지를 쓰지 않고 알고리즘만 직접 구현했다. 실제 그래프를 돌려 만든 문항을 채점하므로
골든셋이 아니라 **그때그때의 생성물**이 대상이다.

| 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|
| `avg_faithfulness` | 문항을 진술문(claim)으로 쪼갬 → Judge가 claim마다 검색 컨텍스트로 뒷받침되는지 판정 → `supported ÷ total`을 샘플 평균 | 검색해온 근거에 없는 내용을 지어내지 않았는가(환각) |
| `avg_answer_relevancy` | 생성된 답변에서 질문 3개를 거꾸로 만들어, 원 질문과의 BGE-M3 임베딩 코사인 유사도를 평균 | 만든 문항이 원래 요청한 주제에서 벗어나지 않았는가 |
| `n_faithfulness_applicable` | claim 추출·파싱에 성공해 점수가 `None`이 아닌 샘플 수 | 위 평균이 몇 건을 근거로 한 값인지 함께 밝히려고 |
| `n_answer_relevancy_applicable` | 〃 | 〃 |

> **왜 이 두 지표를 따로 두나** — 문항 품질 Judge(①-②)는 **문항만** 보고 채점한다. 그래서
> "그럴듯하지만 검색해온 근거에는 없는 내용"을 잡아낼 수 없다. Faithfulness는 생성물을 검색
> 컨텍스트와 직접 대조하므로 그 빈틈을 메운다〔추론〕. 실제로 이 도구를 만들다가 기존 게이트가
> 못 잡던 옅은 한자 오염을 처음 발견하기도 했다(EVAL.md 8절)〔문서〕.
>
> **왜 라이브러리를 안 쓰나**〔문서: EVAL.md 8절〕: `ragas`가 `ChatVertexAI`를 무조건 import하는데
> 그 경로가 최신 `langchain-community`에서 사라졌다. 호환되는 구버전으로 내리면 `langchain-core`가
> 0.3.x로 끌려 내려가 langgraph·langchain-openai 등 프로젝트 전체가 깨진다. 그래서 **알고리즘만**
> 원 정의대로 옮겼다. 매핑도 그때 정했다 — `passage_text`=질문, 검색된 성취기준 청크=컨텍스트,
> 생성된 문항 세트=답변.
>
> **왜 게이트가 없나**〔문서: EVAL.md 8절〕: 표본이 n=5~8로 작고 회차마다 값이 크게 흔들려서
> "첫 측정 기준선"으로만 기록하고, **절대 품질 판정이 아니라 변경 전후 비교용**으로 쓰기로 했다.

> 문항 단위 원자료(`claims`, `supported`, `total`, `reverse_questions`, `similarities`)는
> `data/golden/_ragas_eval_results.json`에 쌓인다.

---

#### (3) `evals/eval_trajectory.py` — 13개 (LangSmith 트레이스 집계)

골든셋이 아니라 **실행 트레이스**를 집계한다. 결과물의 품질이 아니라 "에이전트가 어디서
헛도는가"를 보는 계층이라, 좋고 나쁨보다 **원인 분해**에 쓴다.

| 그룹 | 지표 | 구하는 법 | 무엇을 보나 |
|---|---|---|---|
| A. 도구 호출 신뢰도 | `n` | `run_type == "tool"`인 run 수 | 아래 비율들의 분모 |
| | `error_rate` | 예외로 끝난 tool run ÷ n | 인자 형식 오류 등 **진짜 실패** |
| | `rejection_rate` | 게이트가 거부한 tool run ÷ n | 가드레일이 제대로 작동한 것 — **실패가 아니다** |
| | `empty_result_rate` | 빈 결과를 돌려준 tool run ÷ n | RAG가 아무것도 못 찾은 비율 |
| | `by_tool` | 위 상태를 도구 이름별로 쪼갠 카운터 | 어느 도구가 병목인지 짚어냄 |
| B. 재시도 원인 | `n_validate_runs` | validate 노드 run 수 | 분모 |
| | `feedback_categories` | validate 피드백 문자열을 `none(passed)`/`format`/`judgment`/`both`/`unclassified`로 분류 | **재시도를 부른 게 형식인가 Judge 점수인가** |
| | `malformed_tool_call_llm_calls` | 재작성 요청 마커가 입력에 섞여 들어간 LLM run 수 | tool-call 문법이 깨진 빈도 |
| | `malformed_tool_call_rate` | 위 ÷ 전체 LLM run 수 | 〃 (비율) |
| C. 궤적 형태 | `n_sessions(plan_runs)` | plan 노드 run 수 | 세션 수(= 아래 값들의 분모) |
| | `n_agent_node_runs` | agent 노드 run 수 | agent 재진입 총량 |
| | `avg_agent_reentries_per_session` | agent run ÷ 세션 수 | 요청 하나가 몇 바퀴 도는가(비용·지연에 직결) |
| | `submit_for_review_calls` | 해당 도구 호출 수 | 에이전트가 스스로 마무리했는가 |
| | `validation_passed_rate` | `validation_passed`인 validate run ÷ 전체 | 런타임에서 실측한 게이트 통과율 |

> **왜 이 계층을 새로 만들었나**〔문서: EVAL.md 11절〕: 기존 eval은 전부 **최종 산출물**을
> 채점한다. "그 결과에 어떻게 도달했는가"(도구를 몇 번 잘못 불렀는지, 왜 재시도했는지)는
> LangSmith UI에서 트레이스를 하나씩 눈으로 펼쳐볼 수만 있었고 집계된 적이 없었다.
> 이 스크립트는 **앱 코드를 전혀 건드리지 않고** LangGraph가 이미 남기는 run만 읽는다.
>
> **`error`와 `rejected`를 굳이 가르는 이유**〔문서: EVAL.md 11절〕: `save_item`의 복사 게이트나
> `validate_item_format`의 형식 검증에 걸리는 건 **가드레일이 의도대로 작동한 것**이지 실패가
> 아니다. 둘을 한 숫자로 뭉치면 "도구 에러율 31%"처럼 잘못 읽힌다. 실제로 이 구분 덕분에
> 4.4절에서 기존 가설이 뒤집혔다.
>
> **재시도 원인을 왜 문자열로 분류하나**〔문서: EVAL.md 11절〕: `graph.py`가 이미 피드백을 6종
> 문구로 라벨링해 두기 때문에 따로 라벨링할 필요가 없다. 그래서 `unclassified`는 단순한 기타
> 항목이 아니라 **문자열 매칭이 깨졌다는 경고등**이다.
>
> **전부 참고값인 이유**〔문서: EVAL.md 1절〕: 사람 라벨과 대조하는 게 아니라 실패 모드의
> 분포를 보는 것이라, 여기에 합격선을 긋는 건 의미가 없다.

> ※ 실사용 관측(production observability)이 아니라 `LANGCHAIN_TRACING_V2=true`로 돌린
> 로컬·eval 트레이스를 모은 값이다(RunPod 크레딧이 떨어져 실사용 트래픽이 없다).

### 3.4 일회성 실험에서만 나오는 지표 — `experiments/`

정기 관리 대상이 아니라 **의사결정 하나를 위해 한 번 재고 끝낸** 값들이다. 나중에 "왜 그렇게
정했나"에 답하는 근거로 남긴다.

| 스크립트 | 도출 지표 | 답한 질문 |
|---|---|---|
| `measure_validate_gate.py` | judge/human `overall_score` 분포·평균, **편향**(judge 평균 − human 평균), `type_ratio_score` 분포, **임계값 조합별 통과율** | 게이트 임계값이 Judge 점수 분포에 비춰 도달 가능한 값인가 (4.6절) |
| `measure_budget_effect.py` | 게이트 통과율, 개수 달성률, 문항 0개 비율, 평균 소요시간, agent 실행 횟수별 분포, **재시도로 회복된 통과 건수** | 프로덕션 조건(budget=5)에서도 실제로 통과하는가 |
| `diagnose_near_copy.py` | 구간별(overall ≤1 / ≥3) bigram `containment`·`embed_cosine` 평균, 임계값 구간 분포, 오탐 위험 건수 | 게이트 실패가 어휘를 베낀 탓인가 의미가 겹친 탓인가 (**둘 다 반증**) |
| `diagnose_distractor.py` | 문항 품질 3기준의 평균·최소·최대, 목표 대비 격차, 최약점 | 지금 스택에서 가장 약한 기준은 무엇인가 (→ 타깃 전환의 근거) |
| `test_topk_recall.py` | top_k별 Recall·MRR, 3→2로 줄일 때의 하락폭 | `top_k`를 줄여도 되는가 |
| `compare_reranker.py` | 리랭커 모델별 Recall@5·MRR | 리랭커를 바꾸면 얼마나 나아지는가 |
| `compare_models.py` | 생성 모델별 실패율·평균 생성시간·개수 충족률(budget 1 vs 5) | 7B / 14B / GPT-4o-mini 중 무엇을 쓸 것인가 (4.2절) |
| `compare_judge_models.py` | Judge 모델별 kappa·±1 일치율·difficulty_match·overall MAE | Judge를 로컬 qwen에서 gpt-5.6-luna로 바꿀 것인가 (4.1절) |
| `compare_distractor_quality.py` | 프롬프트 변경 전/후의 오답매력도 평균 | 오답매력도 프롬프트 개선이 먹혔는가 |
| `test_temperature_effect.py` | temperature별 tool-calling 성공률 | 0.7과 0.2 중 어느 쪽인가 |
| `test_retry_preservation.py` | 재시도 시 "정확히 일치" 비율 | 재시도해도 앞서 만든 문항이 남아 있는가 |
| `eval_example_retrieval.py` | 쿼리 문체별 Recall@5·MRR 격차 | 문체 차이가 검색을 실제로 방해하는가 (**라벨링 미완료, 판정 보류**) |

> 이 실험들을 정기 지표로 올리지 않은 이유는 단순하다. **한 번 답이 나오면 다시 물을 필요가
> 없는 질문들**이라서다. 반대로 모델이나 프롬프트를 바꿀 때마다 답이 달라지는 질문만 3.3절에
> 남겼다〔추론〕.

### 3.5 용어 사전 — 지표를 읽는 법

지표 **목록**은 3.3절이고, 이 표는 그 목록에 반복해 나오는 통계 용어의 뜻만 모은 것이다.

| 용어 | 의미 | 왜 보는가 |
|---|---|---|
| Recall@5 | 정답 청크가 상위 5개 후보 안에 들어왔는가 | "검색이 정답을 찾긴 하는가" |
| MRR | 정답이 몇 순위에 나왔는가(1위=1.0, 2위=0.5…) | "찾은 정답을 얼마나 위에 놓는가" |
| Cohen's kappa (κ) | 우연 일치를 걷어낸 사람↔Judge 판단 일치도 | 단순 정확도는 우연만으로도 높게 나올 수 있어 κ로 보정 |
| MAE | Judge 점수와 사람 점수의 평균 절대 오차 | 점수가 몇 점이나 어긋나는지(방향 무관) |
| ±1 일치율 | 사람 점수와 Judge 점수가 1점 이내로 맞는 비율 | κ보다 관대한 "거의 맞았다" 기준 |
| 편향(bias) | Judge 평균 − 사람 평균 | Judge가 후하게/박하게 채점하는 방향과 크기 |

---

## 4. 개선 스토리 — 무엇을 바꿔서 얼마나 좋아졌나

### 4.1 self-judge → 분리형 Judge (2026-07-23) + Judge 모델 교체

몇 달간 로컬 qwen2.5(7B→14B)로 few-shot과 루브릭을 손봐도 구조 Judge 신뢰도가 한계에
부딪혔다. "생성물은 고정하고 Judge만 교체"하는 비교 실험(`compare_judge_models.py`,
2026-07-17, 동일 생성물 n=30/45) 결과:

| 지표 | qwen2.5:7b | gpt-5.6-luna |
|---|---|---|
| 문항품질 kappa | 0.328 | **0.595** |
| 문항품질 ±1 일치율 | 0.800 | 0.733 |
| 구조유사도 difficulty_match | 0.867 | 0.933 |
| 구조유사도 overall MAE | 1.689 | **0.600** |

이후 런타임에서도 생성과 Judge를 분리하고(2026-07-23) gpt-5.6-luna를 기본 Judge로 채택했다.
검증과 배포가 같은 코드를 쓰게 만든 뒤 첫 정식 재측정(2026-07-24, `evals/eval_exam.py`):

| 지표 | 이전 최고(qwen, 수개월 튜닝) | 이후(gpt-5.6-luna, 첫 실행) |
|---|---|---|
| 문항품질 kappa | 0.328(목표 0.4 미달 지속) | **0.468** ✅ 첫 목표 달성 |
| 문항품질 종합평균 | 3.79 | **4.06** ✅ 첫 목표 달성 (합격률 80%) |
| 구조유사도 difficulty_match | 0.867 | 0.933 |
| 구조유사도 overall MAE | 1.185(14B 최고 기록) | **0.644** 역대 최저 |

트레이드오프도 있다. gpt-5.6-luna는 qwen보다 후하게 채점하는 편향이 더 크다(+0.50→+0.94).
신뢰도가 좋아진 폭이 편향이 커진 폭보다 크다고 보고 감수했다. 비용은 사이클당 ~$0.10라
문제가 되지 않았다.

> **overall 이진(≥3) κ ≥ 0.4 목표는 2026-07-24에 공식 폐기**했다 — 수개월을 튜닝해도 최고
> 0.178을 넘지 못했고(저점 few-shot을 넣었다가 Pearson r이 0.305→0.097로 무너진 부작용도
> 겪었다), difficulty_match 일치율과 overall MAE만으로 충분하다고 판단했다. "실패를 숨기지
> 않고 목표 자체를 다시 검토해 폐기"한 사례로 남겨둔다.

### 4.2 생성 모델 Qwen2.5 7B → 14B (2026-07-15)

budget=1(재시도 없음) 조건에서는 14B가 7B보다 3배 느려 보였다(158s vs 50s). 그런데 실제
프로덕션 조건인 budget=5로 다시 재니 그림이 뒤집혔다 — 7B는 재시도가 잦아 속도 이점을
스스로 갉아먹고 있었다.

| 모델 | 실패율(b=1→5) | 평균 생성시간(b=1→5) | 개수 충족률(b=1→5) |
|---|---|---|---|
| Qwen2.5-7B | 40% → 6.7% | 50.3s → 258.8s | 0.15 → 0.649 |
| Qwen2.5-14B | **0% → 0%** | 158.1s → **260.2s**(사실상 동률) | 0.76 → **1.036** |

속도가 같아지는 순간 7B를 쓸 이유가 사라져 14B로 올렸다(GPT-4o-mini는 budget=5에서 실패율이
0%→13.3%로 오르고 과다생성 결함도 드러나 채택하지 않았다).

### 4.3 하이브리드 검색(BM25+dense) + `n_candidates` 20→10 (2026-08-03)

dense 단독 검색은 애초에 문서화된 채택 근거가 없었다. BM25(RRF 융합)를 새 의존성 없이
추가하고, 후보 수는 오히려 줄였을 때(늘릴수록 리랭커가 오답을 위로 올리는 반직관적 현상을
확인) 검색 골든셋(n=22) 결과:

| 지표 | 2026-07-24 (dense-only) | 최종 (2026-08-03, hybrid + n_candidates=10) |
|---|---|---|
| 전체 Recall@5 | 0.955 | **1.000**(첫 만점) |
| 전체 MRR | 0.789 | **0.854** |
| regulations Recall@5 | 0.900 | **1.000** |
| regulations MRR | 0.667 | **0.753** |

덤으로 리랭커가 처리할 후보 쌍이 절반으로 줄어 속도도 2배 빨라졌다(25s→13s). 다만 이
골든셋은 Recall@5=1.000으로 천장에 닿아 더 이상 개선을 잴 수 없다 — 4.5절로 이어진다.

### 4.4 LangSmith "한도 소진" 오진 사건 (2026-08-04)

Agent Trajectory 재측정이 "0건"으로 나와 무료 플랜 조회 한도가 떨어진 것으로 결론 냈었다.
실제로는 배선 버그 3개였다. ① `test_exam.py`에 `load_dotenv()`가 없어 API 키 없이 트레이싱이
켜져 401 ② 5개 스크립트가 `init_langsmith_project()`를 부르지 않아 트레이스가 dev/prod 구분
없이 `bunpil`로 새어 나감 ③ `--since`를 UTC 자정으로 잘못 해석해 KST 오전 트레이스가 누락.
ingest 엔드포인트에 직접 POST해 **202 Accepted**를 확인하고서야 한도 문제가 아님을 확정했다.

버그를 고치고 다시 재니(세션 6건, 도구 호출 91건) 기존 가설 2건이 뒤집혔다.

| 가설(오염된 표본, EVAL.md 11절) | 재측정 후(깨끗한 표본, EVAL.md 11.1절) |
|---|---|
| `record_score` 거부율 74%로 단독 최악 | **0.500**으로 오히려 `save_item`(0.625)·`discard_item`(0.636)보다 낮음 — 기각 |
| 재시도 원인 `format`이 44건으로 우세 | `format` **0건**, 전부 `judgment`/`both` — Judge 게이트가 단독 병목 |
| malformed tool-call 0건 | 표본을 늘리자 **16건(18.4%)** 재관측 — 0건은 표본이 부족했던 탓 |

교훈으로 남긴 것: 증상("조회는 되는데 수집이 안 된다")을 원인("한도 소진")으로 성급히
확정하기 전에, 엔드포인트를 직접 두드려 반증 가능한 형태로 확인할 것.

### 4.5 코퍼스 커버리지 갭 → 생기부 모듈 제거 (2026-08-03)

"규정 위반 탐지가 RAG 검색 약점 때문에 가정환경·종교 규정을 못 찾는다"고 몇 달간 진단해
왔다. 더 어려운 검색 골든셋을 만들려고 regulations 코퍼스 472청크를 전수 조사한 결과:

| 검색어 | 472청크 중 등장 횟수 |
|---|---|
| 종교 / 정치적 성향 / 외모·용모 / 추측 / 가정환경 | **0건** (전부) |
| 부모의 사회·경제적 지위(교사가 "가정형편"이라 부르는 것의 실제 규정 표현) | 1건 |

**검색이 상위로 못 올린 게 아니라, 조항 자체가 인덱싱된 문서에 없었다** — 하이브리드 검색도
리랭커 교체도 고칠 수 없는 문제였다. 이어서 적대적 테스트셋(오탐 14건 + 미탐 3건, n=17)으로
키워드 규칙과 LLM 판정을 나란히 재보니:

| 판정 주체 | Precision | Recall | 정확도 |
|---|---|---|---|
| 키워드 규칙 | 0.000 | 0.000 | **0.000** |
| LLM(`VALIDATE_TPL`) | 0.500~0.600 | 0.667~1.000 | **0.82~0.88** |

키워드 규칙 6종 중 규정 근거가 있는 건 1개뿐이었고, 그 1개마저 오작동했다(교과 주제 언급을
잘못 막으면서 정작 진짜 위반은 놓쳤다). 하드룰 1(실제 학생 데이터 미사용) 때문에 현장
데이터로 검증할 수도 없었다. 그래서 "착수 조건"(교사 확인)을 기다리는 대신 **모듈 자체를
제거**하기로 했다 — 검증할 수 없는 기능을 포트폴리오에 남기는 것보다 걷어내는 쪽이 정직하다고
판단했다. 과거 생기부 평가 수치(위반 Recall 0.927 등)는 "왜 그 수치를 믿을 수 없었는가"를
보여주는 기록으로 EVAL.md에 그대로 남겨뒀다.

### 4.6 "통과할 수 없는 게이트" 발견 → 임계값 재보정 (2026-08-04)

"에이전트가 작성을 정상적으로 마치는 경우가 거의 없다"는 관찰에서 출발했다. 조사해 보니
`submit_for_review`는 멀쩡했고(10회 호출·거부 0건), 문제는 그다음 `validate` 게이트가
**통과 자체가 거의 불가능하게 맞춰져 있던 것**이었다.

`overall_score >= 4` 임계값은 2026-07-06에 도입된 뒤 한 번도 재검토되지 않았고 근거 기록도
없었다 — 그 사이 Judge 프롬프트는 네 번 넘게 재보정됐는데 합격선만 그대로였다. 프로덕션
Judge로 실제 생성물 45건을 다시 채점해 처음으로 분포를 측정했다.

| | 옛 기준 (`overall>=4, type>=0.7`) | 새 기준 (`overall>=3, type>=0.5`) |
|---|---|---|
| 실측 통과율 (2회) | 0.089 / **0.067** | 0.489 / **0.422** |

결정적 근거는 따로 있었다. Judge 프롬프트가 "이게 정답"이라고 가르치는 few-shot 6개 중
**옛 게이트를 통과하는 건 1개뿐**이었다. 특히 *"이런 건 감점하지 말라"*고 가르치려고 넣은
균형 예시(`overall` 4)가 게이트에서 탈락했다 — **Judge에게 좋은 세트라고 가르쳐 놓고 코드가
그걸 반려**하고 있었던 셈이다.

**런타임 확인(2026-08-07)**: 스모크 테스트 조건까지 고친 뒤 6세션을 다시 재서, 같은 측정치에
두 기준을 각각 적용했다 — 옛 기준 **1/6**, 새 기준 **3/6**. 통과 3건 중 2건이 `overall=3`이라
옛 기준이었다면 탈락했을 값이다. 골든셋 근거(6.7%→42.2%)와 방향·규모가 일치한다(EVAL.md 19절).

**프로덕션 조건에서 결정적으로 확인(2026-08-07)**: `budget=5`(실제 프로덕션 값)로 재니 통과율
**0.833**(5/6). 그런데 통과한 5건의 `overall`이 **전부 3**이라, 옛 기준(≥4)을 적용하면 **0/6**이
된다 — 재보정이 없었다면 재시도를 아무리 늘려도 통과가 나오지 않았다는 뜻이다. 재시도 예산과
임계값 재보정은 **함께** 작동해야 한다(EVAL.md 21절).

실사용 영향은 "결과를 못 받는 것"이 아니라(문항은 그대로 반환됐다) **매 요청이 재시도 예산을
전량 소진**한 것이었다 — 불필요하게 느리고 비쌌다. 상세는 EVAL.md 15절.

---

## 5. 현재 열린 이슈 (2026-08-07 기준)

| 이슈 | 현황 |
|---|---|
| ~~`record_score` self-judge~~ | **해소(2026-08-06)** — 게이트에서 뺀 뒤 도구 자체를 제거했다(EVAL.md 16·17절). 신뢰도는 끝내 측정하지 않았는데, 제거하면서 측정 대상 자체가 사라졌다 |
| ~~Judge payload 불일치~~ | **해소(2026-08-06)** — 공유 함수 `judge_structure()` 안에서 입력을 정규화해, 런타임과 오프라인이 구조적으로 어긋날 수 없게 했다(EVAL.md 17절) |
| **정답유일성 ~3.0 / 목표 4.0** | ⬜ **미해결.** 프롬프트 지시를 넣어 3.375→4.125를 얻었으나, 교차 A/B로 재검증하니 **배치 효과였고 실제 효과는 없었다**(대조 3.083 vs 처치 3.000, p=0.58) — 지시는 되돌렸다. 치명적 실패(정답이 2개인 문항)가 **50%**로 앞선 측정보다 나쁘다. 원인(선지 중 조건 만족 개수 미검증)과 구조적 공백은 유효하나 **텍스트 지시로는 해결 안 됨**(EVAL.md 23·24절) |
| ~~오답매력도 미달~~ | **해소(2026-08-07)** — 추적하던 2.846은 7B 생성 + qwen Judge 시절 값이라 무효였다. 현재 스택으로 재측정하니 **3.75**로 목표에 근접 |
| ~~`budget=5` 미측정~~ | **해소(2026-08-07)** — 프로덕션 조건에서 통과율 **0.833**(스모크 조건 0.500). 통과 5건 중 3건이 재시도로 회복됐고 전부 `overall=3`이라, **재보정이 없었다면 0.000**이었다. 개선 작업 없이도 프로덕션은 이미 양호하다(EVAL.md 21절) |
| 실패 양상이 옮겨갔을 가능성 | 자기채점을 없앤 뒤 malformed tool-call은 18.4%→0%로 사라졌으나 `validate_item_format` 거부가 0→18로 늘었다 — 실패가 "호출 문법 깨짐"에서 "내용 형식 미달"로 옮겨간 것일 수 있다(n=6으로는 구분 불가) |
| 검색 골든셋 천장 도달 | 기존 22건은 Recall@5 1.000이라 더 이상 개선을 잴 수 없다. `regulations_retrieval_candidates.json` 10건(현재 Recall@5 0.600)을 정식 편입할지 검토 중 |
| 코드 리뷰 진행 중 | [CODE_REVIEW_CHECKLIST.md](./CODE_REVIEW_CHECKLIST.md) 참고 |
| RunPod GPU 서빙 | 2026-07-22부터 크레딧 소진으로 일시 중단(설정은 그대로라 복구만 하면 된다) |

---

## 6. 참고 문서 맵

| 문서 | 역할 |
|---|---|
| [EVAL.md](./EVAL.md) | 회차별 raw 로그, 시행착오, 의사결정 서사 전체(2000줄+) — "왜 이렇게 바꿨는지"의 1차 사료 |
| [EVAL_MAP.md](./EVAL_MAP.md) | 어떤 스크립트·골든셋이 어느 계층에 속하는지 색인 |
| [MODEL_SELECTION.md](./MODEL_SELECTION.md) | 생성/Judge/임베딩/리랭커/검색 방식/백엔드 6개 항목의 선택 근거 |
| [LANGSMITH_GUIDE.md](./LANGSMITH_GUIDE.md) | LangSmith 웹 UI 보는 법, 트레이스가 안 보일 때 체크리스트 |
| [DESIGN.md](./DESIGN.md) §5 | 평가 설계 원칙(요약), 상세는 위 문서들 참고 |
