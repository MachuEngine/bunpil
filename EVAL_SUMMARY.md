# 분필(Bunpil) 평가 체계 한눈에 보기

> 이 문서 하나로 "평가가 어떻게 이루어지는지 · 어떤 지표를 어떻게 구하는지 · 뭐가 얼마나
> 좋아졌는지"를 파악할 수 있게 압축한 요약본이다. 회차별 raw 로그·시행착오 전체는
> [EVAL.md](./EVAL.md), 어떤 스크립트가 뭘 담당하는지는 [EVAL_MAP.md](./EVAL_MAP.md),
> 모델·검색 방식 선택 근거는 [MODEL_SELECTION.md](./MODEL_SELECTION.md) 참고.
>
> 2026-08-03 생기부 윤문 모듈 제거로 현재 평가 대상은 **출제 모듈 하나**다(과거 생기부
> 평가 기록은 역사적 사실로 EVAL.md에 보존되어 있고, 4.5절 "코퍼스 커버리지 갭"에
> 그 판단 과정 자체를 정리해뒀다).

**목차**

1. [큰 그림 — 런타임 vs 오프라인](#1-큰-그림--런타임-vs-오프라인)
2. [런타임 평가 — 문항 하나가 나가기까지](#2-런타임-평가--문항-하나가-나가기까지-3단계)
3. [오프라인 평가 — 지표 전체 목록](#3-오프라인-평가--계층골든셋지표-전체-목록) ← **지표 목록만 필요하면 [3.3절](#33-정기-평가-지표-전체-목록--evals)**
4. [개선 스토리 — 무엇을 바꿔서 얼마나 좋아졌나](#4-개선-스토리--무엇을-바꿔서-얼마나-좋아졌나)
5. [현재 열린 이슈](#5-현재-열린-이슈-2026-08-07-기준)
6. [참고 문서 맵](#6-참고-문서-맵)

---

## 1. 큰 그림 — 런타임 vs 오프라인

| | 런타임 평가 | 오프라인 평가 |
|---|---|---|
| **언제** | 교사가 실제로 문항을 생성하는 매 요청마다 | 모델·프롬프트·검색 방식을 바꿀 때 사람이 수동 실행 |
| **무엇을 보나** | 이번 생성 세트가 통과 기준을 넘는지(재시도 여부) | 그 통과 기준(Judge)·검색기 자체가 믿을 만한지 |
| **판단 주체** | 코드(threshold) — 근거는 Judge LLM 점수 | 사람 라벨(golden set) 대비 Judge/검색기의 정확도 |
| **코드 위치** | `app/modules/exam/graph.py`(`judge_node`, `validate_node`) | `evals/`, `tests/`, `experiments/` |

한 문장으로: **런타임은 "이번 결과물이 기준을 넘었는가"를 매번 판정하고, 오프라인은 "그
기준(Judge 모델)과 검색기 자체가 사람 판단과 얼마나 일치하는가"를 사람 라벨로 검증한다.**
2026-07-23부터 이 둘은 **완전히 같은 채점 함수**(`judge.py`의 `judge_structure()`)를 쓰므로,
오프라인에서 측정한 신뢰도 수치가 곧 런타임에 배포된 Judge의 신뢰도다(3절 참고).

---

## 2. 런타임 평가 — 문항 하나가 나가기까지 3단계

```
agent node(생성 LLM) → judge node(별도 Judge LLM) → validate(코드) → 통과 시 종료 / 미달 시 재시도(budget)
```

### 2.1 agent node — `save_item` 결정론적 게이트 3개 (코드, LLM 판단 아님)

| 게이트 | 실제 기준 | 근거 |
|---|---|---|
| 한자 오염 | 한자/(한자+한글) 비율이 **5% 이상**이면 거부. 한글이 0개면 무조건 거부 | 비한국어 전체가 아니라 **CJK 한자 비율**만 본다(영어 오염은 이 게이트로 잡히지 않음) |
| 예시 단순 복사 | 생성 문항과 예시 문제(`passage_text`)의 bigram containment **≥ 0.90** | 완전 복사는 실측 1.00, 정상 변형은 ~0.73 이하 — 1.00보다 낮은 문턱으로 근접 복사까지 차단 |
| 세트 내 중복 | 이미 저장된 문항과 Jaccard bigram 유사도 **≥ 0.80** | 같은 요청의 draft 세트 내부만 비교(외부 DB 대조 아님) |

세 게이트 중 하나라도 걸리면 `save_item`이 저장을 거부하고, 거부 사유를 담은 메시지를 그대로
에이전트에게 돌려준다. 에이전트는 같은 턴 안에서 그 사유를 보고 스스로 다시 쓴다 — 그래프
단위의 재시도가 아니라 도구 호출 루프 안에서 일어나는 자가교정이다.

### 2.2 judge node — 구조 유사도 채점 (별도 Judge LLM)

`agent`가 `submit_for_review`를 호출해 작성을 마치면(또는 14턴 한도 등으로 루프가 끝나면)
`judge` 노드가 생성 모델과 **완전히 분리된 백엔드**(`get_judge_backend()`, 기본값
`gpt-5.6-luna`)로 3개 지표를 매긴다:

| 지표 | 구하는 법 | 역할 |
|---|---|---|
| `type_ratio_score` (0~1) | Judge LLM이 (예시 문제, 생성 세트) 쌍을 보고 직접 산출 — 계산식이 아니라 루브릭+few-shot 6개로 유도 | 문항 유형(객관식/서술형 등) 구성이 예시를 따라갔는가 |
| `difficulty_match` (bool) | 〃 | 난이도 구성이 예시와 부합하는가 |
| `overall_score` (0~5 정수) | 〃 — 유형·난이도·주제의 유사성 점수에서 중복·원문복사·환각·언어오염을 감점해 합산. 0~5 루브릭과 점수대별 앵커 예시를 시스템 프롬프트에 넣어 점수 일관성을 유지 | 세트 전체의 종합 품질. 재시도 여부를 사실상 결정하는 단일 지표 |

> 세 값은 `judge_structure()`(`app/modules/exam/judge.py`)가 JSON 한 번으로 함께 반환한다.
> 오프라인 3.3절의 **"구조 유사도 Judge 신뢰도"가 바로 이 세 값의 신뢰도**다 — 같은 함수를
> 쓰므로 오프라인에서 잰 값이 곧 배포된 Judge의 신뢰도가 된다.

**생성·Judge를 분리한 이유**(2026-07-23, "옵션 B"): 원래는 생성 에이전트가 `similarity_judge`
도구로 자기 출력을 스스로 채점했다(self-judge). 이 self-judge의 신뢰도는 사람 라벨과
한 번도 대조된 적이 없었고, 게다가 **오프라인 eval이 검증하는 Judge와 실제 배포된
judge(생성 모델 자신)가 서로 다른 코드 경로**였다(검증-배포 불일치). 이를 해소하려고
런타임 judge_node와 오프라인 eval이 `judge.py`의 `judge_structure()`를 공유하도록
바꿨다 — "self-bias 방지"보다는 **"검증한 것과 배포한 것을 같게 만든다"**는 신뢰성 문제가
1차 동기였다(4.1절 스토리 참고).

### 2.3 validate node — 재시도 여부 결정 (코드, LLM 미사용)

아래 **4개 조건을 전부(AND) 만족해야** 통과한다(`graph.py:330-334`). Judge가 매긴 3개
지표뿐 아니라 문항 개수도 함께 본다.

| 조건 | 기준 | 판단 주체 |
|---|---|---|
| 개수 일치 | 생성된 문항 수 == 요청한 `num_items` | 코드(`len(draft_items)` 비교) |
| 유형 비율 | `type_ratio_score` ≥ 0.5 | Judge 값에 코드가 임계값 적용 |
| 난이도 일치 | `difficulty_match` == true | 〃 |
| 종합 점수 | `overall_score` ≥ 3 | 〃 |

넷을 다 통과하면 재시도 없이 종료하고 세트를 제출한다. 하나라도 미달이면 남은
`budget`(기본 5회)만큼 부족분만 이어서 재생성한다. **budget이 소진되면 미달 상태로도
종료**하므로, "종료"가 곧 "성공"은 아니다.

**이 게이트가 지금 모습이 되기까지** — 두 번의 변경이 있었다.

| 시점 | 무엇을 바꿨나 | 왜 |
|---|---|---|
| 2026-08-04 | 임계값 재보정: `overall ≥ 4`·`type_ratio ≥ 0.7` → **`≥ 3`·`≥ 0.5`** | 옛 기준의 실측 통과율이 6.7~8.9%로 사실상 도달 불가였다(Judge가 4~5점을 거의 주지 않음). 재보정 후 42.2~48.9% — 4.6절·EVAL.md 15절 |
| 2026-08-05/06 | `record_score`(에이전트 자기채점)의 "전량 승인" 조건을 게이트에서 제외 → 이어서 **도구 자체를 제거** | 사람 라벨과 대조된 적 없는 지표가 통과 여부를 결정하고 있었다. 게이트에서 뺀 뒤에도 문항당 4턴 중 1턴을 쓰고 있었는데, 정작 병목은 14턴 한도였다 — EVAL.md 16·17절 |

---

## 3. 오프라인 평가 — 계층·골든셋·지표 전체 목록

### 3.1 4개 계층

Judge·검색기 자체가 믿을 만한지, 코드가 항상 같은 방식으로 동작하는지를 사람 라벨/골든셋
대비로 검증하는 계층. `evals/`가 유일하게 "품질 게이트"이고 나머지 3개는 성격이 다르다.

| 계층 | 위치 | 확인 내용 | 실행 시점 |
|---|---|---|---|
| ① 정기 평가 | `evals/eval_exam.py`, `eval_ragas.py`, `eval_trajectory.py` | 골든셋 대비 정량 지표(Recall@5, Judge 신뢰도, RAG 충실도, 에이전트 궤적) | 모델/프롬프트 변경마다 수동 실행 |
| ② pytest | `tests/*.py` | 결정론적 로직(마스킹, 저장 게이트, 검증 순서) | `pytest` 회귀 방지용 |
| ③ 스모크 | `scripts/test_*.py` | 실제 로컬 Ollama로 배선이 안 끊겼는지 | 구조 변경 직후 1회 |
| ④ 일회성 실험 | `experiments/*.py` | 특정 의사결정 하나(A/B, 파라미터 튜닝) | 이미 종료, 결과는 `data/golden/_*.json`에 아카이브 |

②와 ③은 수치가 아니라 **pass/fail만** 내므로 아래 지표 목록에서는 제외했다. 즉 "관리하는
지표"는 ①(3.3절)이고, ④는 의사결정 근거로 한 번 재고 끝낸 값들(3.4절)이다.

### 3.2 골든셋 현황 (`data/golden/*.json`)

| 골든셋 | 규모 | 사람 라벨 | 쓰는 지표 |
|---|---|---|---|
| `retrieval_golden_final.json` | 22 (standards 12 + regulations 10) | 전량 검수 | Recall@5, MRR |
| `item_golden.json` (ITEM_GOLDEN) | 30 | `human_score` | 문항 품질 5개 + Judge 신뢰도 5개 |
| `structure_golden.json` (STRUCTURE_GOLDEN) | 45 | `human_label` | 구조 Judge 신뢰도 3개 |
| 마스킹 20건 | 20 | — | 2026-08-03부터 `tests/test_masker.py`가 흡수(지표 아님, pass/fail) |

### 3.3 정기 평가 지표 전체 목록 — `evals/`

정기적으로 관리하는 지표는 **총 32개**이고, 그중 pass/fail 게이트가 걸린 건 **4개**(아래 ✅)다.
나머지는 참고값이거나 원인을 쪼개 보기 위한 값이다.

| 스크립트 | 지표 수 | 한 줄로 | 게이트 |
|---|---|---|---|
| `eval_exam.py` | 15 | 검색이 정답을 찾는가 + 문항이 좋은가 + **Judge를 믿어도 되는가** | 4개 |
| `eval_ragas.py` | 4 | 생성한 문항이 검색해온 근거에 충실한가 | 없음 |
| `eval_trajectory.py` | 13 | 에이전트가 어디서 헛도는가(실패·재시도의 원인 분해) | 없음 |

#### (1) `evals/eval_exam.py` — 15개 (로직은 `evals/eval_lib.py`)

**검색 성능** — `eval_retrieval()`, 골든셋 22건

| 지표 | 구하는 법 | 역할 | 기준 |
|---|---|---|---|
| `recall_at_5` | 골든셋 쿼리를 `top_k=5`로 검색해, 정답 청크 preview의 앞 80자가 결과 본문에 들어 있는 쿼리 수 ÷ n | 검색이 정답을 후보 안에 넣기는 하는가 | ✅ ≥ 0.8 |
| `mrr` | Σ(1 ÷ 정답이 나온 순위) ÷ n (1위=1.0, 2위=0.5) | 넣은 정답을 얼마나 위로 올리는가(리랭커 품질) | 참고값(≥0.6) |

**문항 품질** — `judge_one()` → `eval_item_quality()` · ITEM_GOLDEN 30건
Judge가 문항 하나(질문·선지·정답)만 보고 세 기준을 1~5 정수로 채점한다(few-shot 3개).

| 지표 | 구하는 법 | 역할 |
|---|---|---|
| `avg_정답유일성` | 문항별 점수의 산술평균 | 정답이 하나로 확정되는가 — **현재 추적 타깃**(3.375 / 목표 4.0, 틀렸을 때 타격이 가장 큼. EVAL.md 22절) |
| `avg_오답매력도` | 〃 | 오답 선지가 그럴듯한가 — 현재 스택 재측정 3.75로 2026-08-07 추적 해제 |
| `avg_근거성` | 〃 | 교육과정에 근거한 문항인가 |
| `avg_overall` | 문항별 3기준 평균을 낸 뒤 다시 전체 평균 | 문항 품질 대표값 · ✅ **≥ 4.0** |
| `pass_rate` | `overall ≥ 4.0`인 문항의 비율 | 평균 하나로는 안 보이는 분포를 드러냄 — "몇 개나 합격선을 넘었는가" |

**문항 품질 Judge 신뢰도** — `eval_judge_reliability()` · 같은 30건
Judge `overall`을 반올림해 정수로 만든 뒤 `human_score`와 대조한다.

| 지표 | 구하는 법 | 역할 |
|---|---|---|
| `exact_agreement` | 사람 점수 == Judge 점수인 비율 | 가장 엄격한 일치도(참고값) |
| `agreement_within_1` | \|사람 − Judge\| ≤ 1인 비율 | "거의 맞았다" 기준 · ✅ **≥ 0.7** |
| `cohen_kappa` | 두 점수를 `≥3`으로 이진화한 뒤 (Po − Pe) / (1 − Pe) | 우연 일치를 걷어낸 실질 일치도 · ✅ **≥ 0.4** |
| `human_avg` | 사람 점수의 평균 | 편향을 재는 기준선 |
| `llm_avg` | Judge 점수의 평균 | **`llm_avg − human_avg`가 곧 편향(bias)** — 코드는 두 평균만 내고, 빼는 건 사람이 한다 |

**구조 유사도 Judge 신뢰도** — `eval_structure_judge()` · STRUCTURE_GOLDEN 45건
2.2절의 런타임 `judge_structure()`를 그대로 호출해 사람 라벨(`human_label`)과 대조한다.

| 지표 | 구하는 법 | 역할 |
|---|---|---|
| `difficulty_match_agreement` | Judge의 `difficulty_match`가 사람 라벨과 같은 비율 | 난이도 판정을 사람과 같게 내리는가 |
| `overall_score_mae` | \|Judge `overall` − 사람 `overall`\|의 평균 | 점수가 몇 점이나 어긋나는가(방향 무관) — 이진 kappa를 폐기한 뒤로는 이쪽이 주 지표다(4.1절) |
| `count_match_code_rate` | `len(generated_items) == num_items`인 비율 | **사람↔Judge 대조가 아니다** — 골든셋 엔트리 자체의 데이터 정합성 확인용 |

#### (2) `evals/eval_ragas.py` — 4개

Ragas 패키지를 쓰지 않고 알고리즘만 직접 구현했다(의존성 충돌 회피, EVAL.md 8절). 실제
그래프를 돌려 만든 문항을 채점하므로 골든셋이 아니라 **그때그때의 생성물**이 대상이다.

| 지표 | 구하는 법 | 역할 |
|---|---|---|
| `avg_faithfulness` | 문항을 진술문(claim)으로 쪼갬 → Judge가 claim마다 검색 컨텍스트로 뒷받침되는지 판정 → `supported ÷ total`을 샘플 평균 | 검색해온 근거에 없는 내용을 지어내지 않았는가(환각) |
| `avg_answer_relevancy` | 생성된 답변에서 질문 3개를 거꾸로 만들어 원 질문과 BGE-M3 임베딩 코사인 유사도를 잰 뒤 평균 | 만든 문항이 원래 요청한 주제에서 벗어나지 않았는가 |
| `n_faithfulness_applicable` | claim 추출·파싱에 성공해 점수가 `None`이 아닌 샘플 수 | 위 평균이 몇 건을 근거로 한 값인지 함께 밝히려고 |
| `n_answer_relevancy_applicable` | 〃 | 〃 |

> 문항 단위 원자료(`claims`, `supported`, `total`, `reverse_questions`, `similarities`)는
> `data/golden/_ragas_eval_results.json`에 누적된다.

#### (3) `evals/eval_trajectory.py` — 13개 (LangSmith 트레이스 집계)

골든셋이 아니라 **실행 트레이스**를 집계한다. 결과물의 품질이 아니라 "에이전트가 어디서
헛도는가"를 보는 계층이라, 좋고 나쁨보다 **원인 분해**에 쓴다.

| 그룹 | 지표 | 구하는 법 | 역할 |
|---|---|---|---|
| A. 도구 호출 신뢰도 | `n` | `run_type == "tool"`인 run 수 | 아래 비율들의 분모 |
| | `error_rate` | 예외로 끝난 tool run ÷ n | 인자 형식 오류 등 **진짜 실패** |
| | `rejection_rate` | 게이트가 거부한 tool run ÷ n | 가드레일 정상 동작 — **실패가 아님**(높다고 나쁜 게 아니다) |
| | `empty_result_rate` | 빈 결과를 반환한 tool run ÷ n | RAG가 아무것도 못 찾은 비율 |
| | `by_tool` | 위 상태를 도구 이름별로 쪼갠 카운터 | 어느 도구가 병목인지 짚어냄 |
| B. 재시도 원인 | `n_validate_runs` | validate 노드 run 수 | 분모 |
| | `feedback_categories` | validate 피드백 문자열을 `none(passed)`/`format`/`judgment`/`both`/`unclassified`로 분류 | **재시도를 부른 게 형식인가 Judge 점수인가** — 4.4절에서 기존 가설을 뒤집은 지표 |
| | `malformed_tool_call_llm_calls` | 재작성 요청 마커가 입력에 섞여 들어간 LLM run 수 | tool-call 문법이 깨진 빈도 |
| | `malformed_tool_call_rate` | 위 ÷ 전체 LLM run 수 | 〃 (비율) |
| C. 궤적 형태 | `n_sessions(plan_runs)` | plan 노드 run 수 | 세션 수(= 아래 값들의 분모) |
| | `n_agent_node_runs` | agent 노드 run 수 | agent 재진입 총량 |
| | `avg_agent_reentries_per_session` | agent run ÷ 세션 수 | 요청 하나가 재시도를 몇 바퀴 도는가(비용·지연에 직결) |
| | `submit_for_review_calls` | 해당 도구 호출 수 | 에이전트가 스스로 작성을 마무리했는가 |
| | `validation_passed_rate` | `validation_passed`인 validate run ÷ 전체 | 런타임에서 실측한 게이트 통과율 |

> ※ 실사용 관측(production observability)이 아니라 `LANGCHAIN_TRACING_V2=true`로 돌린
> 로컬·eval 트레이스를 모은 값이다(RunPod 크레딧 소진으로 실사용 트래픽이 없다).

### 3.4 일회성 실험에서만 나오는 지표 — `experiments/`

정기 관리 대상이 아니라 **의사결정 하나를 위해 한 번 재고 끝낸** 값들이다. 나중에 되돌아볼 때
"왜 그렇게 정했나"에 답하는 근거로 남긴다.

| 스크립트 | 도출 지표 | 답한 질문 |
|---|---|---|
| `measure_validate_gate.py` | judge/human `overall_score` 분포·평균, **편향**(judge 평균 − human 평균), `type_ratio_score` 분포, **임계값 조합별 통과율** | 게이트 임계값이 Judge 점수 분포에 비춰 도달 가능한 값인가 (4.6절) |
| `measure_budget_effect.py` | 게이트 통과율, 개수 달성률, 문항 0개 비율, 평균 소요시간, agent 실행 횟수별 분포, **재시도로 회복된 통과 건수** | 프로덕션 조건(budget=5)에서도 실제로 통과하는가 |
| `diagnose_near_copy.py` | 구간별(overall ≤1 / ≥3) bigram `containment`·`embed_cosine` 평균, 임계값 구간 분포, 오탐 위험 건수 | 게이트 실패가 어휘를 베낀 탓인가 의미가 겹친 탓인가 (**둘 다 반증**) |
| `diagnose_distractor.py` | 문항 품질 3기준의 평균·최소·최대, 목표 대비 격차, 최약점 | 현재 스택의 오답매력도는 실제로 몇 점인가 |
| `test_topk_recall.py` | top_k별 Recall·MRR, 3→2로 줄일 때의 하락폭 | `top_k`를 줄여도 되는가 |
| `compare_reranker.py` | 리랭커 모델별 Recall@5·MRR | 리랭커를 바꾸면 얼마나 나아지는가 |
| `compare_models.py` | 생성 모델별 실패율·평균 생성시간·개수 충족률(budget 1 vs 5) | 7B / 14B / GPT-4o-mini 중 무엇을 쓸 것인가 (4.2절) |
| `compare_judge_models.py` | Judge 모델별 kappa·±1 일치율·difficulty_match·overall MAE | Judge를 로컬 qwen에서 gpt-5.6-luna로 바꿀 것인가 (4.1절) |
| `compare_distractor_quality.py` | 프롬프트 변경 전/후의 오답매력도 평균 | 오답매력도 프롬프트 개선이 먹혔는가 |
| `test_temperature_effect.py` | temperature별 tool-calling 성공률 | 0.7과 0.2 중 어느 쪽인가 |
| `test_retry_preservation.py` | 재시도 시 "정확히 일치" 비율 | 재시도해도 앞서 만든 문항이 보존되는가 |
| `eval_example_retrieval.py` | 쿼리 문체별 Recall@5·MRR 격차 | 문체 차이가 검색을 실제로 방해하는가 (**라벨링 미완료, 판정 보류**) |

### 3.5 용어 사전 — 지표를 읽는 법

지표 **목록**은 3.3절이고, 이 표는 그 목록에 반복해 나오는 통계 용어의 뜻만 모은 것이다.

| 용어 | 의미 | 왜 보는가 |
|---|---|---|
| Recall@5 | 정답 청크가 상위 5개 후보 안에 들어왔는가 | "검색이 정답을 찾긴 하는가" |
| MRR | 정답이 몇 순위에 나왔는가(1위=1.0, 2위=0.5…) | "찾은 정답을 얼마나 위에 놓는가" |
| Cohen's kappa (κ) | 우연 일치를 걷어낸 사람↔Judge 판단 일치도 | 단순 정확도는 우연만으로도 높게 나올 수 있어 κ로 보정 |
| MAE | Judge 점수와 사람 점수의 평균 절대 오차 | 점수가 몇 점이나 어긋나는지(방향 무관) |
| ±1 일치율 | 사람 점수와 Judge 점수가 1점 이내로 맞는 비율 | kappa보다 관대한 "거의 맞았다" 기준 |
| 편향(bias) | Judge 평균 − 사람 평균 | Judge가 후하게/박하게 채점하는 방향과 크기 |

---

## 4. 개선 스토리 — 무엇을 바꿔서 얼마나 좋아졌나

### 4.1 self-judge → 분리형 Judge (2026-07-23) + Judge 모델 교체

몇 달간 로컬 qwen2.5(7B→14B)로 few-shot·rubric을 튜닝해도 구조 Judge 신뢰도가 한계에
부딪혔다. "생성물은 고정하고 Judge만 교체"하는 비교 실험(`compare_judge_models.py`,
2026-07-17, 동일 생성물 n=30/45) 결과:

| 지표 | qwen2.5:7b | gpt-5.6-luna |
|---|---|---|
| 문항품질 kappa | 0.328 | **0.595** |
| 문항품질 ±1 일치율 | 0.800 | 0.733 |
| 구조유사도 difficulty_match | 0.867 | 0.933 |
| 구조유사도 overall MAE | 1.689 | **0.600** |

이후 생성·Judge를 런타임에서도 분리(2026-07-23)하고 gpt-5.6-luna를 기본 Judge로 채택,
검증-배포가 같은 코드를 쓰게 만든 뒤 첫 정식 재측정(2026-07-24, `evals/eval_exam.py`):

| 지표 | 이전 최고(qwen, 수개월 튜닝) | 이후(gpt-5.6-luna, 첫 실행) |
|---|---|---|
| 문항품질 kappa | 0.328(목표 0.4 미달 지속) | **0.468** ✅ 첫 목표 달성 |
| 문항품질 종합평균 | 3.79 | **4.06** ✅ 첫 목표 달성 (합격률 80%) |
| 구조유사도 difficulty_match | 0.867 | 0.933 |
| 구조유사도 overall MAE | 1.185(14B 최고 기록) | **0.644** 역대 최저 |

트레이드오프: gpt-5.6-luna가 qwen보다 후하게 채점하는 편향이 더 크다(+0.50→+0.94). 신뢰도
개선폭이 편향 증가보다 크다고 판단해 감수. 비용은 사이클당 ~$0.10로 무시 가능.

> **overall 이진(≥3) kappa ≥0.4 목표는 2026-07-24 공식 폐기**됐다 — 수개월 튜닝에도 최고
> 0.178을 못 넘었고(저점 few-shot을 넣었다가 Pearson r이 0.305→0.097로 무너진 부작용도
> 겪음), difficulty_match 일치율·overall MAE만으로 충분하다고 판단했다. "실패를 숨기지
> 않고 목표 자체를 재검토해 폐기"한 사례로 남겨둔다.

### 4.2 생성 모델 Qwen2.5 7B → 14B (2026-07-15)

budget=1(재시도 없음) 조건에서는 14B가 7B보다 3배 느려 보였다(158s vs 50s). 그러나
실제 프로덕션 조건인 budget=5로 재검증하니 그림이 뒤집혔다 — 7B는 재시도가 잦아 속도
이점을 스스로 갉아먹었다:

| 모델 | 실패율(b=1→5) | 평균 생성시간(b=1→5) | 개수 충족률(b=1→5) |
|---|---|---|---|
| Qwen2.5-7B | 40% → 6.7% | 50.3s → 258.8s | 0.15 → 0.649 |
| Qwen2.5-14B | **0% → 0%** | 158.1s → **260.2s**(사실상 동률) | 0.76 → **1.036** |

속도가 같아지는 순간 7B를 쓸 이유가 사라져 14B로 승격(GPT-4o-mini는 budget=5에서 실패율
0%→13.3%·과다생성 결함이 드러나 채택하지 않음).

### 4.3 하이브리드 검색(BM25+dense) + `n_candidates` 20→10 (2026-08-03)

dense 단독 검색은 애초에 문서화된 채택 근거가 없었다. BM25(RRF 융합)를 새 의존성 0개로
추가하고, 후보 수를 오히려 줄였을 때(늘릴수록 리랭커가 오답을 상위로 올리는 반직관적
현상 확인) 검색 골든셋(n=22) 결과:

| 지표 | 2026-07-24 (dense-only) | 최종 (2026-08-03, hybrid + n_candidates=10) |
|---|---|---|
| 전체 Recall@5 | 0.955 | **1.000**(첫 만점) |
| 전체 MRR | 0.789 | **0.854** |
| regulations Recall@5 | 0.900 | **1.000** |
| regulations MRR | 0.667 | **0.753** |

부수 효과: 리랭커가 처리하는 후보 쌍이 절반으로 줄어 속도도 2배(25s→13s). 단, 이 골든셋은
Recall@5=1.000으로 "천장"에 도달해 더 이상 개선을 측정할 수 없다 — 4.5절로 이어짐.

### 4.4 LangSmith "한도 소진" 오진 사건 (2026-08-04)

Agent Trajectory 재측정이 "0건"으로 나와 무료 플랜 조회 한도 소진으로 결론 냈었다.
실제로는 배선 버그 3개였다: ① `test_exam.py`에 `load_dotenv()`가 없어 API 키 없이
트레이싱이 켜져 401 ② 5개 스크립트가 `init_langsmith_project()`를 부르지 않아 트레이스가
dev/prod 구분 없이 `bunpil`로 새어 나감 ③ `--since`를 UTC 자정으로 잘못 해석해 KST 오전
트레이스가 누락. ingest 엔드포인트에 직접 POST해 **202 Accepted**를 확인하고서야 한도
문제가 아님을 확정했다.

버그 수정 후 재측정(세션 6건, 도구 호출 91건)에서 기존 가설 2건이 뒤집혔다:

| 가설(오염된 표본, EVAL.md 11절) | 재측정 후(깨끗한 표본, EVAL.md 11.1절) |
|---|---|
| `record_score` 거부율 74%로 단독 최악 | **0.500**으로 오히려 `save_item`(0.625)·`discard_item`(0.636)보다 낮음 — 기각 |
| 재시도 원인 `format`이 44건으로 우세 | `format` **0건**, 전부 `judgment`/`both` — Judge 게이트가 단독 병목 |
| malformed tool-call 0건 | 표본 확대 후 **16건(18.4%)** 재관측 — 0건은 표본 부족이었음 |

교훈으로 남긴 것: 증상("조회는 되는데 수집이 안 된다")을 원인("한도 소진")으로 성급히
확정하기 전에 엔드포인트를 직접 두드려 반증 가능한 형태로 확인할 것.

### 4.5 코퍼스 커버리지 갭 → 생기부 모듈 제거 (2026-08-03)

"규정 위반 탐지가 RAG 검색 약점 때문에 가정환경·종교 규정을 못 찾는다"고 몇 달간 진단해
왔다. 더 어려운 검색 골든셋을 만들려고 regulations 코퍼스 472청크를 전수 스캔한 결과:

| 검색어 | 472청크 중 등장 횟수 |
|---|---|
| 종교 / 정치적 성향 / 외모·용모 / 추측 / 가정환경 | **0건** (전부) |
| 부모의 사회·경제적 지위(교사가 "가정형편"이라 부르는 것의 실제 규정 표현) | 1건 |

**검색이 상위로 못 올린 게 아니라 조항 자체가 인덱싱된 문서에 없었다** — 하이브리드 검색도
리랭커 교체도 고칠 수 없는 문제였다. 이어서 적대적 테스트셋(오탐 14건 + 미탐 3건, n=17)으로
키워드 규칙과 LLM 판정을 나란히 재보니:

| 판정 주체 | Precision | Recall | 정확도 |
|---|---|---|---|
| 키워드 규칙 | 0.000 | 0.000 | **0.000** |
| LLM(`VALIDATE_TPL`) | 0.500~0.600 | 0.667~1.000 | **0.82~0.88** |

키워드 규칙 6종 중 규정 근거가 있는 건 1개뿐이었고 그 1개조차 오작동(교과 주제 언급을
잘못 차단하면서 정작 진짜 위반은 놓침)했다. 하드룰 1(실제 학생 데이터 미사용) 때문에
실제 현장 데이터로 검증할 수도 없어, "착수 조건"(교사 확인)을 기다리는 대신 **모듈 자체를
제거**하기로 했다 — 검증 불가능한 기능을 포트폴리오에 남기는 것보다 걷어내는 쪽이 정직하다는
판단. 과거 생기부 평가 수치(위반 Recall 0.927 등)는 "왜 그 수치를 신뢰할 수 없었는가"를
보여주는 기록으로 EVAL.md에 그대로 남겨뒀다.

### 4.6 "통과할 수 없는 게이트" 발견 → 임계값 재보정 (2026-08-04)

"에이전트가 작성을 정상적으로 마치는 경우가 거의 없다"는 관측에서 출발했다. 조사 결과
`submit_for_review`는 정상이었고(10회 호출·거부 0건), 문제는 그 다음 `validate` 게이트가
**통과 자체가 거의 불가능하게 설정돼 있던 것**이었다.

`overall_score >= 4` 임계값은 2026-07-06 도입 후 한 번도 재검토된 적이 없고 근거 기록도
없었다 — 그 사이 Judge 프롬프트는 4회 이상 재보정됐는데 합격선만 그대로였다. 프로덕션
Judge로 실제 생성물 45건을 재채점해 처음으로 분포를 측정했다:

| | 옛 기준 (`overall>=4, type>=0.7`) | 새 기준 (`overall>=3, type>=0.5`) |
|---|---|---|
| 실측 통과율 (2회) | 0.089 / **0.067** | 0.489 / **0.422** |

결정적 근거: Judge 프롬프트가 "이게 정답"이라고 가르치는 few-shot 6개 중 **옛 게이트를
통과하는 건 1개뿐**이었다. 특히 *"이런 건 감점하지 말라"*고 가르치려고 넣은 균형
예시(`overall` 4)가 게이트에서 탈락했다 — **Judge에게 좋은 세트라고 가르쳐 놓고 코드가
그걸 반려**하고 있었다.

**런타임 확인(2026-08-07)**: 스모크 테스트 조건까지 고친 뒤 6세션을 재측정해, 같은 측정치에
두 기준을 각각 적용했다 — 옛 기준 **1/6**, 새 기준 **3/6**. 통과 3건 중 2건이 `overall=3`으로
옛 기준이었다면 탈락했을 값이다. 골든셋 근거(6.7%→42.2%)와 방향·규모가 일치한다(EVAL.md 19절).

**프로덕션 조건에서 결정적으로 확인(2026-08-07)**: `budget=5`(실제 프로덕션 값)로 측정하니
통과율 **0.833**(5/6). 그런데 통과 5건의 `overall`이 **전부 3**이라, 옛 기준(≥4)을 적용하면
**0/6**이 된다 — 즉 재보정이 없었다면 재시도를 아무리 늘려도 통과가 나오지 않았다.
재시도 예산과 임계값 재보정이 **함께** 작동해야 한다는 뜻이다(EVAL.md 21절).

실사용 영향은 "결과를 못 받는 것"이 아니라(문항은 그대로 반환됨) **매 요청이 재시도
예산을 전량 소진**한 것 — 불필요하게 느리고 비쌌다. 상세는 EVAL.md 15절.

---

## 5. 현재 열린 이슈 (2026-08-07 기준)

| 이슈 | 현황 |
|---|---|
| ~~`record_score` self-judge~~ | **해소(2026-08-06)** — 게이트 제외 후 도구 자체를 제거(EVAL.md 16·17절). 신뢰도는 끝내 측정하지 않았는데, 제거로 측정 대상 자체가 사라졌다 |
| ~~Judge payload 불일치~~ | **해소(2026-08-06)** — 공유 함수 `judge_structure()` 안에서 입력을 정규화해 런타임·오프라인이 구조적으로 어긋날 수 없게 했다(EVAL.md 17절) |
| 정답유일성 4.125 / 목표 4.0 ✅ | **2026-08-07 개입 완료.** 프롬프트에 유일성 자가 점검 지시를 추가해 3.375→**4.125**(독립 2배치 재현), 치명적 실패 37.5%→12.5%. 원인은 "지시가 약해서"가 아니라 **애초에 지시가 없어서**였다. 다만 최악(1점)이 완전히 사라지진 않았고, 텍스트 지시로 못 막는 실패 모드가 남아 있다(EVAL.md 23절) |
| ~~오답매력도 미달~~ | **해소(2026-08-07)** — 추적하던 2.846은 7B 생성+qwen Judge 시절 값이라 무효였다. 현재 스택 재측정 **3.75**로 목표 근접 |
| ~~`budget=5` 미측정~~ | **해소(2026-08-07)** — 프로덕션 조건 측정 결과 통과율 **0.833**(스모크 조건 0.500). 통과 5건 중 3건이 재시도로 회복됐고, 전부 `overall=3`이라 **재보정이 없었다면 0.000**이었다. 개선 작업 없이도 프로덕션은 이미 양호(EVAL.md 21절) |
| 실패 양상이 옮겨갔을 가능성 | 자기채점 제거 후 malformed tool-call은 18.4%→0%로 사라졌으나 `validate_item_format` 거부가 0→18로 늘었다 — 실패가 "호출 문법 깨짐"에서 "내용 형식 미달"로 옮겨간 것일 수 있다(n=6으로는 구분 불가) |
| 검색 골든셋 천장 도달 | 기존 22건은 Recall@5 1.000이라 더 이상 개선을 측정 불가. `regulations_retrieval_candidates.json` 10건(현재 Recall@5 0.600)을 정식 편입 검토 중 |
| 코드 리뷰 진행 중 | [CODE_REVIEW_CHECKLIST.md](./CODE_REVIEW_CHECKLIST.md) 참고 |
| RunPod GPU 서빙 | 2026-07-22부터 크레딧 소진으로 일시 비활성(설정은 유지, 복구만 하면 됨) |

---

## 6. 참고 문서 맵

| 문서 | 역할 |
|---|---|
| [EVAL.md](./EVAL.md) | 회차별 raw 로그, 시행착오, 의사결정 서사 전체(1600줄+) — "왜 이렇게 바꿨는지"의 1차 사료 |
| [EVAL_MAP.md](./EVAL_MAP.md) | 어떤 스크립트·골든셋이 어느 계층에 속하는지 색인 |
| [MODEL_SELECTION.md](./MODEL_SELECTION.md) | 생성/Judge/임베딩/리랭커/검색 방식/백엔드 6개 항목 선택 근거 |
| [LANGSMITH_GUIDE.md](./LANGSMITH_GUIDE.md) | LangSmith 웹 UI 보는 법, 트레이스 안 보일 때 체크리스트 |
| [DESIGN.md](./DESIGN.md) §5 | 평가 설계 원칙(요약), 상세는 위 문서들 참고 |
