# 자기소개서 (지원용 초안)

> 5000자 제한 필드용. 최종 업데이트: 2026-08-09
> (2026-08-09 정리: 2026-08-03 제거된 생기부 모듈 관련 서술을 "제거 판단" 서사로 교체, 도구 개수·골든셋 종수·Recall@5 최신값 반영)

미디어젠에서 차량·음성인식 서비스를 개발하며, 서강대학교 AI SW대학원에서 인공지능을 병행 전공해 2026년 2학기 졸업을 앞두고 있습니다. 임베디드-서버 하이브리드 구조의 음성인식 시스템 기능 개발과 유지보수를 주력으로 해왔고, 최근에는 개인 프로젝트로 LLM 기반 AI 에이전트의 설계·개발·평가·배포 전 과정을 직접 경험하며 역량을 넓히고 있습니다. 프로덕션 서비스를 안정적으로 운영해 온 경험과 최신 LLM 기술을 함께 갖춘 것이 강점입니다.

**AI 네이티브 에듀테크 서비스 개발 (미디어젠 사내 TF, 2025.10–2026.03)**
외국인 한국어 학습자를 위한 발음 교정 피드백 서비스를 백엔드 단독으로 개발했습니다. 자사 발음 분석 모델의 음소별 채점 결과를 해석해 피드백으로 바꾸는 부분은 입력이 구조화되어 있고 도메인 규칙이 유한·안정적이라는 판단 아래 에이전트 루프 없이 프롬프트 구조화(루브릭+Few-shot+CoT)만으로 설계하고, 검색이 필요한 한국어 학습 챗봇만 별도 RAG 파이프라인(ChromaDB+Cross-encoder rerank)으로 분리했습니다 — 에이전트가 항상 정답은 아니라는 판단을 내린 지점입니다. 모델 선정도 실험으로 결정해, 로컬 모델과 OpenAI를 JSON 스키마 준수율 기준으로 비교 후 실시간 생성은 GPT-4.1, 오프라인 평가는 자기 편향(self-preference bias)을 피하기 위해 다른 제공사의 Gemini 2.5 Pro를 Judge로 분리했습니다. 평가 체계도 릴리즈를 막는 하드 게이트(스키마 준수율, 사실 일치율)와 개선 방향만 추적하는 참고 지표(교육적 유용성 등)를 구분해 설계했고, 검색과 생성 품질을 분리 측정해 문제 원인을 좁히는 방식으로 RAG 검색 품질을 Recall@5/MRR 기준 5~10% 개선했습니다.

**분필 (Bunpil) — 문제 생성 AI 에이전트 (2026, 개인 프로젝트)**
고등학교 사회 교사의 시험 문항 출제를 보조하는 서비스로, LangGraph 기반 ReAct 에이전트가 교육과정·규정 RAG(ChromaDB+BGE-M3+BGE-reranker)를 참조해 문항을 생성하고, 생성 모델과 완전히 분리된 별도 Judge LLM이 구조 유사도를 채점하면 코드가 통과 여부를 결정론적으로 판정하는 구조로 설계했습니다. "LLM은 판단, 코드는 결정"이라는 원칙 아래 문항 개수·언어·재시도 로직은 전부 결정론적 코드로 처리해 신뢰성을 확보했습니다.

Tool Calling 구조에도 이 원칙을 그대로 적용했습니다. 에이전트가 추론과 생성을 전담하고, 검색·저장·검증을 맡는 도구들은 내부에 LLM 호출이 전혀 없는 순수 계산만 수행하도록 설계해 "LLM을 도구 안에 중첩하는" 안티패턴을 제거했습니다. 형식 오류 발생 시 도구가 수정 지침을 반환해 에이전트가 스스로 재작성하는 자기교정 루프, 재시도 시 이미 저장된 문항은 유지한 채 부족분만 이어서 생성하는 부분 진행 보존 로직도 함께 구현했습니다.

개발 과정에서 로컬 LLM 특유의 문제들을 정량적으로 진단하고 해결했습니다. 생성 성공률이 37%에서 6%로 급락한 원인을 LangSmith 트레이스 분석으로 추적해 컨텍스트 길이 설정 문제임을 밝혀냈고, 문항의 45%에서 발생한 중국어 오염 문제는 트레이스 100건 정량 분석 후 결정론적 언어 검증 게이트로 해결했습니다. 또한 "채점에 쓰는 Judge"와 "실제 배포된 Judge"가 서로 다른 코드 경로였던 검증-배포 불일치를 스스로 발견해, 생성 모델과 Judge 모델을 런타임까지 완전히 분리하는 아키텍처로 개선했습니다.

평가 체계는 사람이 라벨링한 골든셋을 기반으로, 파이프라인이 에러 없이 동작하는지 확인하는 기능 검증과 결과 품질을 수치로 추적하는 품질 평가를 레이어로 분리해 운영했습니다. LLM Judge를 그대로 신뢰하지 않고 사람 라벨과 대조해 신뢰도(Cohen's κ 0.468)를 직접 검증한 뒤 채택했고, 검색은 하이브리드 검색을 도입해 Recall@5 1.000을 확보했습니다. 특히 프롬프트를 바꿔도 품질 수치가 전혀 움직이지 않는 현상을 직접 발견해, 그동안 품질 지표로 읽어 온 종합평균 4.06/5.0이 사실은 Judge 검증용 고정 골든셋을 채점한 값이라 생성 품질을 반영할 수 없는 구조였음을 규명했습니다. 실제 생성물을 채점하는 경로를 따로 만들어 다시 재니, 배포보다 작은 모델 조건에서 3.14(게이트 없는 참고값)였습니다. 유리한 쪽 숫자를 남기는 대신 두 값을 측정 조건과 함께 공개 문서 대표 지표에 그대로 올렸습니다. 그 밖에도 판정력이 안정적이지 않던 이진 게이트는 폐기하고 이미 계산 중이던 다른 지표로 대체하는 등 평가 체계 자체의 타당성도 지속적으로 검증했습니다.

AI 거버넌스와 안전장치(Guardrails) 설계에도 관심을 두고 실제로 구현했습니다. PII 마스킹은 반드시 모델 호출 이전에 수행하고 실제 학생 데이터는 전혀 사용하지 않는 등 데이터 거버넌스 원칙을 하드룰로 고정했고, 생성 모델이 자기 출력을 스스로 채점하지 않도록 완전히 분리된 별도 Judge LLM을 두어 신뢰 계층(Trust Layer)을 구성했습니다. 문항 저장 단계에는 한국어 오염·예시 복사·중복 여부를 판정하는 결정론적 가드레일을 두어 LLM 판단에만 의존하지 않도록 설계했습니다. 이런 원칙은 개발 과정에서 사용한 AI 코딩 에이전트(Claude Code)에도 동일하게 적용해, 위험 명령을 사전 차단하고 편집 직후 PII 패턴을 자동 스캔하는 훅 기반 가드레일을 직접 구축해 AI 도구 활용 자체도 통제 가능한 형태로 운영했습니다.

---

## 경력·프로젝트 상세 (사람인 형식)

> 원티드 지원 + Notion 포트폴리오 기준으로만 작성한 버전입니다. 아래 1·2·3번 모두 Notion 포트폴리오 원문과 대조해 사실관계를 맞췄습니다(2번의 RAG 파이프라인은 In-Memory/SQLite가 아니라 ChromaDB+Cross-encoder rerank가 맞는 내용).

### 1. 현대자동차·기아 양산 차량 탑재 음성인식 시스템(HMI 앱) 개발
- **소속회사**: 미디어젠(주)
- **연계회사**: 현대자동차 / 현대모비스 / LG전자 / SoundHound AI / Cerence
- **기간**: 2020.06 – 2026.06

**주요 역할**
1) 음성인식 서비스 레이어 개발(Android/Kotlin) — 차량 내장 인식기(Embedded)와 서버 인식기(AI Server) 결과를 Intent/Slot 스키마 검증과 Confidence 점수 기반으로 비교해 채택하는 하이브리드 Arbitration 로직 개발, Cerence/SoundHound 두 엔진의 서로 다른 결과 포맷을 흡수하는 엔진별 분기 구조 설계
2) ASR 서버 연동 및 인식 결과 유효성 검증 — 서버 응답의 비정상 포맷을 클라이언트에서 방어적으로 복구하는 전처리 로직 구현, 인증 정보를 설정 파일 기반 관리로 전환, 운영/스테이징 환경별 라우팅 구조 설계
3) 비즈니스 로직 및 예외 처리 — 인식된 이름과 차량 주소록 연락처를 연결하는 매칭 후처리 로직, 음성 안내 중 터치 조작이 끼어드는 인터럽트 상황의 세션/상태 관리 로직 개발
4) 동시성 결함 해결 및 글로벌 품질 대응 — 언어 전환 시 레이스 컨디션, 모듈 간 락 획득 순서로 인한 데드락 리스크, 대화 이력 유실 등 재현이 어려운 결함을 로그 분석으로 원인 규명 후 재현→수정→검증 사이클로 해결, 20개 이상 언어 대상 텍스트 정규화 오류·RTL(아랍어) 렌더링 등 국제화(i18n) 이슈 대응
5) 고객사 커뮤니케이션 — 품질 이슈 개선/신규 아이템 개발 관련 일정·설계 협의 지속, AI 정적분석 도구의 결함 리포트에 대해 실제 결함/오탐 여부를 코드 레벨에서 판별하고 근거 기반 소명 문서 작성

**업무 성과**
1) 재현이 어려운 동시성 결함(레이스 컨디션·데드락 리스크·대화 이력 유실)을 로그 분석 기반 재현→수정→검증 사이클로 해결하고, "실패 사례를 재현 가능한 TestCase로 축적"하는 원칙을 정립 — 이후 개인 프로젝트(분필)의 골든셋 회귀 테스트 운용에도 동일하게 적용함
2) 차량 주행 환경 특유의 빈번한 VUI 인터럽트·예외 상황을 방어하는 코드로 앱 크래시·오작동 비율 감소에 기여함
3) 현지 품질 평가 이슈를 당일 분석·익일 패치 배포하는 빠른 대응 사이클로 20개 이상 언어의 글로벌 품질을 지속 개선함
4) 중복 로그 제거로 커맨드당 로그량 약 85% 감소, 개인정보 마스킹 정책은 유지한 채 운영 효율화 달성

### 2. AI 네이티브 에듀테크 서비스 구축 (미디어젠 사내 TF)
- **소속회사**: 미디어젠(주)
- **기간**: 2025.10 – 2026.03 (현대차 프로젝트 병행 중 합류)
- **주요 역할**: AI API 연동 백엔드 아키텍처 제로베이스 설계 및 구축(백엔드 단독 개발)

**업무 성과**
1) 비용 최적화 및 Latency 단축 — 자사 발음 분석 모델의 1차 처리와 OpenAI GPT-4.1 비동기 호출 2차 처리로 API를 2-Depth로 분리 설계함. 사용자 체감 대기 시간을 최소화하고 불필요한 외부 API 호출을 차단해 토큰 과금(Cost)을 최적화함
2) RAG 기반 한국어 학습 챗봇 구축 — TOPIK 공식 안내·기출/문법 자료·서비스 내부 문서를 문장/문단 단위로 청킹해 ChromaDB에 벡터화하고, Cross-encoder 기반 Rerank로 검색 정확도를 높이는 RAG 파이프라인을 구축함. 베이스라인 대비 검색 품질(Recall@5/MRR)을 5~10% 개선함
3) 프롬프트 엔지니어링 고도화 — 점수 구간별 해석 기준(루브릭), Few-shot 예시 앵커링, CoT 기반 단계적 추론(점수 확인→오류 음소 식별→규칙 매칭→교정법 제시)을 구조화해 발음 피드백의 일관성과 정확성을 확보함
4) LLM 모델 선정 실험 — 로컬 모델(Qwen2.5, EXAONE)과 OpenAI를 JSON 스키마 준수율 기준으로 비교해 실시간 생성은 GPT-4.1을 채택하고, 오프라인 평가(LLM-as-Judge)는 자기 편향(self-preference bias)을 피하기 위해 다른 제공사의 Gemini 2.5 Pro로 역할을 분리함

### 3. 분필 (Bunpil) — 문제 생성 AI 에이전트
- **소속**: 개인 프로젝트
- **기간**: 2026 (진행 중)
- **주요 역할**: 기획·설계·개발·평가·배포 전 과정 단독 수행

**주요 역할**
1) LangGraph 기반 ReAct 에이전트 설계 — 고등학교 사회 교사의 시험 문항 출제를 보조하는 서비스를 기획하고, 교육과정·규정 RAG(ChromaDB+BGE-M3+BGE-reranker)를 참조해 문항을 생성하는 에이전트 아키텍처를 설계함
2) 생성-Judge 모델 분리 아키텍처 구축 — 생성 모델과 완전히 분리된 별도 Judge LLM이 구조 유사도를 채점하고 코드가 통과 여부를 결정론적으로 판정하는 구조를 설계·구현함. 오프라인 평가와 런타임이 서로 다른 코드 경로(self-judge)였던 검증-배포 불일치를 스스로 발견해 런타임까지 완전 분리하는 아키텍처로 개선함
3) Tool Calling 구조 설계 — 에이전트가 추론·생성을 전담하고, 검색·저장·검증을 맡는 도구들은 내부에 LLM 호출이 전혀 없는 순수 계산만 수행하도록 설계함(현재 5개). 형식 오류 시 도구가 수정 지침을 반환해 에이전트가 스스로 재작성하는 자기교정 루프, 재시도 시 기존 저장 문항은 유지한 채 부족분만 이어서 생성하는 부분 진행 보존 로직을 구현함
4) 검증 불가능한 기능을 스스로 걷어낸 판단 — 초기에는 생활기록부 문구 교정 모듈도 함께 개발했으나, 검증 규칙의 근거를 추적하는 과정에서 규정 원문 26만 자를 전수 검색해 해당 조항이 한 건도 없음을 확인함. 규칙의 실제 출처가 합성 골든셋 라벨이었고 그 골든셋으로 채점해 높은 점수가 나오는 순환 구조였으며, 적대적 테스트 17건에서 규칙 기반 판정의 정확도가 0.000으로 측정됨. 실제 학생 데이터를 쓸 수 없어 검증 자체가 불가능하다고 판단해 모듈 전체를 제거하고 출제 단일 모듈로 범위를 좁힘
5) 평가 체계 구축 — 사람이 라벨링한 골든셋을 기반으로 기능 검증과 품질 평가를 레이어로 분리하고, LLM Judge를 그대로 신뢰하지 않고 사람 라벨과 대조해 신뢰도(Cohen's κ)를 직접 검증한 뒤 채택함
6) 배포 및 인프라 설계 — AWS EC2 + RunPod 서버리스(vLLM) + Caddy HTTPS로 배포하고, 환경변수 전환만으로 로컬(Ollama)·프로덕션(RunPod) LLM 백엔드를 전환할 수 있는 추상화를 설계함

**업무 성과**
1) 로컬 LLM 특유의 문제를 정량적으로 진단·해결 — 생성 성공률이 37%에서 6%로 급락한 원인을 LangSmith 트레이스 분석으로 추적해 컨텍스트 길이 설정 문제(4096→16384)를 규명했고, 문항의 45%에서 발생한 중국어 오염 문제는 트레이스 100건 정량 분석 후 결정론적 언어 검증 게이트로 해결함
2) 검증-배포 불일치를 스스로 발견해 아키텍처 개선 — "채점에 쓰는 Judge"와 "실제 배포된 Judge"가 서로 다른 코드 경로였음을 발견해 생성 모델과 Judge 모델을 런타임까지 완전히 분리함
3) 정량 평가 목표 달성과 그 한계 규명 — 검색 Recall@5 **1.000**(BM25+dense 하이브리드 도입과 리랭커 후보 수 조정으로 0.955→1.000), Judge 신뢰도(Cohen's κ) 0.468을 달성함. 문항 품질 종합평균은 고정 골든셋 기준 4.06/5.0, 실제 생성물 기준 3.14(배포보다 작은 모델 조건, 게이트 없는 참고값)로 성격이 다른 두 값이라 공개 문서에 측정 조건과 함께 나란히 기록함(경위는 4번 항목). 다만 Recall@5 1.000은 22건 골든셋에서 천장에 도달했다는 뜻이라, 추가 개선을 측정하려면 더 어려운 골든셋이 필요하다는 한계도 함께 기록함
4) 평가 시스템 자체의 타당성 검증 — 프롬프트를 개선해도 eval 수치가 그대로인 버그를 직접 발견해 원인이 고정된 골든셋만 재채점하는 구조였음을 규명하고, 실제 생성-재채점 스크립트로 보완함. 이 과정에서 대표 지표로 써 온 4.06이 생성 품질이 아니라 Judge 신뢰도 측정값이었음이 드러나, 공개 문서의 설명을 정정하고 실측값을 함께 노출하도록 고침
5) AI 거버넌스·가드레일 설계 — PII 마스킹 하드룰, 결정론적 언어·예시복사·중복 게이트를 구현하고, 개발에 사용한 AI 코딩 에이전트(Claude Code)에도 위험 명령 차단·PII 자동 스캔 훅 기반 가드레일을 직접 구축해 적용함

### AI 활용 워크플로우

업무·개인 프로젝트 환경에 따라 AI 도구를 다르게 활용했습니다.

**회사 업무(에듀테크 프로젝트)** — 사내 보안 정책상 Claude Code 등 에이전트형 코딩 툴은 사용할 수 없어(사내 개발 PC, 원청사 코드 반출 제약), ChatGPT/Gemini를 설계 논의(아키텍처 결정, 트러블슈팅 방향 상담)와 보일러플레이트 코드 생성 보조로만 활용했습니다. 모델 선정, 아키텍처 결정, API 연동 트러블슈팅 등 핵심 판단은 전부 직접 수행했습니다.

**개인 프로젝트(분필)** — Claude Code 기반으로 "AI 코딩 에이전트가 안전하고 예측 가능하게 작동하는 개발 환경"을 별도로 설계·구축했습니다. 분석→설계→구현→검증→완료선언 5단계 워크플로우를 명문화해 설계안을 먼저 제시하고 승인 전에는 구현하지 않는 Plan-First 원칙을 세웠고, 조사(읽기 전용)·설계·구현·검증을 서로 다른 역할로 분리해 검증 에이전트가 코드를 직접 고칠 권한 없이 문제만 보고하도록 구성했습니다. 위험 명령을 사전 차단하고 편집 직후 PII 패턴을 자동 스캔하는 훅 기반 가드레일도 직접 구축했으며, 이 검증 장치 자체를 독립 리뷰로 검증하는 과정에서 로깅 코드가 차단된 명령 원문(시크릿 노출 위험)을 그대로 남기는 결함을 발견해 즉시 수정한 경험도 있습니다. "AI를 활용했다"가 아니라 "AI를 어떻게 통제했는가"에 초점을 둔 작업입니다.

자기소개서 초안 (LLM 에이전트 개발 엔지니어 포지셔닝)

1. 자기소개

안녕하십니까. 저는 (주)미디어젠에서 차량 음성인식 서비스 개발을 담당하고 있습니다. 음성인식 서비스 개발자로 일하며, ASR이 초기 임베디드 환경의 확률 기반(음향/언어 모델) 인식에서 클라우드 기반 대규모 End-to-End STT로 전환되는 기술적 패러다임의 변화를 프로덕션 환경에서 직접 경험했습니다. 이 변화를 지켜보며 대규모 언어 모델이 단순 인식·변환을 넘어 스스로 추론하고 도구를 활용해 문제를 해결하는 "에이전트"로 진화하고 있다는 점에 주목했고, 이를 직접 설계·구현해보고 싶어 대학원 과정에 진학해 AI 이론을 체계적으로 학습하는 한편, 사내 TF와 개인 프로젝트를 통해 LLM 기반 에이전트를 실제로 만들어보며 역량을 쌓아왔습니다.

2. 학업 및 기술 역량 강화 노력

대학원 진학 후 LLM과 에이전트 아키텍처를 집중적으로 학습하며, 이를 실제 서비스로 구현하는 경험을 병행했습니다. 사내 TF에서는 외국인 한국어 학습자를 위한 발음 교정 피드백 서비스를 백엔드부터 제로베이스로 설계했습니다. 구조화된 입력에는 에이전트 루프 없이 프롬프트를 구조화하고, 검색이 필요한 부분만 RAG로 분리하는 등 "에이전트가 항상 정답은 아니다"라는 판단 기준을 스스로 세워 적용했습니다. 이어서 개인 프로젝트 분필(Bunpil)에서는 LangGraph 기반 ReAct 에이전트로 시험 문항 출제 서비스를 직접 기획·개발했습니다. 생성 모델과 완전히 분리된 별도 Judge LLM으로 품질을 검증하는 아키텍처를 설계하고, 도구(Tool)는 내부에 LLM 호출 없이 순수 계산만 수행하도록 만들어 안티패턴을 제거했으며, 사람이 라벨링한 골든셋으로 정량 평가 체계를 구축해 검색 Recall@5 1.000, Judge 신뢰도(Cohen's κ) 0.468을 달성했습니다. 이 과정에서 로컬 LLM의 컨텍스트 길이·언어 오염 문제를 정량적으로 진단해 해결했고, "검증에 쓰는 Judge와 실제 배포된 Judge가 다른 코드였다"는 설계 결함을 스스로 발견해 개선하기도 했습니다. 이렇게 만든 에이전트를 실제로 안정적으로 서빙하기 위해 AWS EC2와 RunPod 서버리스로 배포하는 경험도 쌓았고, DVC·MLflow로 데이터/실험을 관리하고 Docker로 서빙 파이프라인을 구성하는 등 모델 개발부터 배포·운영까지 AI 서비스 전체 라이프사이클을 이해하려 노력하고 있습니다.

3. 입사 후 포부

입사 후 가장 먼저 기존 AI 서비스의 에이전트 아키텍처와 배포 환경을 파악해 실무에 빠르게 적응하겠습니다. 그동안 상용 LLM/RAG API를 연동하고 속도감 있게 에이전트 MVP를 만들며 겪었던 시행착오 — 도구 설계, Judge/평가 체계 구축, 프로덕션 배포 — 를 바탕으로, 서비스가 예외 없이 안정적으로 동작하는 에이전트 시스템을 구축하겠습니다. 거창한 목표를 앞세우기보다는, 에이전트가 실패하거나 판단을 잘못하는 지점을 하나씩 찾아 정량적으로 진단하고 개선하는 데 집중하고 싶습니다. 이를 통해 내부적으로는 팀의 개발·평가 프로세스를 효율화하고, 외부적으로는 사용자에게 신뢰할 수 있는 AI 에이전트 서비스를 제공하겠습니다.
