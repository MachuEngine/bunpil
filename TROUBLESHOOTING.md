# 분필(Bunpil) 트러블슈팅 기록

> 개발·운영 중 만난 문제를 **증상 → 처음 의심한 것 → 실제 원인 → 조치 → 교훈** 형태로
> 모아둔 문서. 상세 수치·raw 데이터는 각 사례의 "상세" 링크(EVAL.md 등) 참고.
>
> 이 문서의 목적은 "무엇을 고쳤나"보다 **"왜 처음 가설이 틀렸나"**를 남기는 것이다.
> 아래 14건 중 9건은 처음 세운 가설이 실제 원인과 달랐다.

## 한눈에 보기

| # | 사례 | 증상 | 처음 의심 | 실제 원인 |
|---|---|---|---|---|
| 1 | [Ollama num_ctx](#1-ollama-num_ctx-기본값-4096-2026-07-09) | 생성 성공률 35%→6% 급락 | 그날 바꾼 프롬프트 | Ollama 기본 컨텍스트 4096 (모델은 32K 지원) |
| 2 | ["멈춤" vs "느림"](#2-멈춤이-아니라-느림이었다-2026-07-10) | 배치가 30분+ 무응답 | 프로세스 누적·GPU 경합·무한 재시도 | 정상 처리 중이었음, old 설계 자체가 느림 |
| 3 | [중국어 언어 오염](#3-한국어-지시에도-중국어-문항-생성-2026-07-11) | 문항 45%에 한자 혼입 | 컨텍스트 잘림 | 입력 길이에 비례한 확률적 드리프트 (잘림 무관) |
| 4 | [tool-calling 형식 오류](#4-tool-calling-형식-오류를-정상-종료로-오판-2026-07-11) | 0문항으로 조기 종료 | 모델이 작다 → 14B면 해결 | 깨진 형식을 코드가 "자발적 종료"로 오판 (14B도 재현) |
| 5 | [청킹 변경 후 Recall 하락](#5-recall-하락은-실제-저하가-아니라-측정-아티팩트였다-2026-07-14) | Recall@5 0.944→0.778 | 청킹 변경이 검색을 망침 | 골든셋 채점 방식이 청크 경계에 과민 (성능은 불변) |
| 6 | [vLLM tool calling 미동작](#6-runpod-vllm에서-tool-calling이-동작하지-않음) | 에이전트가 도구를 0회 호출 | 프롬프트·모델 문제 | 어댑터가 `tool_calls` 필드를 안 채움 (직접 구현 필요) |
| 7 | [EBS 데이터 소실](#7-ebs를-붙였는데-컨테이너-교체마다-데이터가-사라짐) | 재시작마다 컬렉션 0건 | 볼륨 마운트 실패 | `--env-file`이 Dockerfile `ENV`를 조용히 덮어씀 |
| 8 | [앱 기동 실패·헬스체크 오탐](#8-배포-검증-중-드러난-기존-버그-2건-2026-07-12) | 앱이 아예 안 뜸 / 계속 unhealthy | Docker 설정 | 의존성 누락(`python-multipart`) / `localhost` IPv6 해석 |
| 9 | [프롬프트를 바꿔도 지표 불변](#9-생성-프롬프트를-고쳐도-eval-점수가-똑같았다-2026-07-09) | 전/후 평균이 완전히 동일 | 노이즈인가? | 그 지표는 고정 골든셋 채점이라 생성 경로를 안 탐 |
| 10 | [리랭커 후보 역설](#10-후보를-늘릴수록-검색이-나빠졌다-2026-08-03) | 하이브리드 도입해도 목표 미개선 | 검색기가 약하다 | 병목은 리랭커 — 후보 20→10으로 줄이자 개선 |
| 11 | [코퍼스 커버리지 갭](#11-검색이-못-찾은-게-아니라-조항이-없었다-2026-08-03) | 특정 규정을 계속 못 찾음 | RAG 검색 약점 | 그 조항이 코퍼스에 0건 — 없는 걸 검색할 수는 없음 |
| 12 | [LangSmith "한도 소진" 오진](#12-langsmith-한도-소진은-오진이었다-2026-08-04) | 트레이스 수집 0건 | 무료 플랜 한도 소진 | 배선 버그 3건 (한도는 멀쩡했음) |
| 13 | [통과할 수 없는 게이트](#13-통과할-수-없는-게이트-2026-08-04) | 정상 완료가 거의 없음 | 종료 로직·생성 품질 | 합격 기준선이 Judge 점수 분포 위에 있었음 |
| 14 | [통과 불가 스모크 테스트](#14-측정-도구가-측정할-수-없는-것을-재고-있었다-2026-08-07) | 게이트 고쳤는데 통과율 그대로 0.000 | 재보정이 무효했나 | 테스트 조건이 루브릭상 2점 확정 — 애초에 통과 불가 |

## 공통 패턴 (반복해서 배운 것)

1. **증상이 가리키는 곳과 원인이 있는 곳은 자주 다르다.** 14건 중 9건이 첫 가설이 틀렸다.
2. **추론하지 말고 직접 찔러본다.** 12번은 ingest 엔드포인트에 직접 POST해 `202`를 받고서야
   한도 문제가 아님이 확정됐다. 2번은 서버 로그의 task ID 증가를 보고서야 "느림"과 "멈춤"이
   구분됐다.
3. **지표가 안 움직이면 그 지표가 내 변경을 실제로 지나는지부터 확인한다** (9번).
4. **모델을 키우면 해결될 것이라는 가정은 검증 전까지 믿지 않는다** (4번: 7B→14B 동일 재현).
5. **로컬과 프로덕션의 기본값 차이가 유령 버그를 만든다** (1번: Ollama 4096 vs vLLM 32K,
   7번: `--env-file` 우선순위).
6. **측정 도구 자체가 틀릴 수 있다** — 이 프로젝트에서만 3번 반복됐다
   (5번: 채점 방식이 청크 경계에 과민, 9번: 고정 골든셋이 생성 경로를 안 탐,
   14번: 테스트 조건이 통과 불가). **지표가 안 움직이면 지표부터 의심할 것.**

---

## 1. Ollama num_ctx 기본값 4096 (2026-07-09)

| | |
|---|---|
| **증상** | STRUCTURE_GOLDEN 생성 성공률이 35~40%에서 2/34(약 6%)로 급락. 도구 호출 대신 중국어·스페인어 섞인 텍스트 응답 |
| **처음 의심** | 그날 시스템 프롬프트에 추가한 문구 2개(오답매력도·num_items 지시) |
| **실제 원인** | Ollama가 `num_ctx` 미설정 시 **4096 토큰**으로 제한. 모델(qwen2.5:7b)은 32K 네이티브 지원. 멀티턴 ReAct에서 검색 결과가 누적되며 몇 턴 만에 초과 → 컨텍스트가 잘리며 시스템 프롬프트를 잃고 응답 붕괴 |
| **조치** | `app/modules/exam/llm.py`의 `ChatOllama`에 `num_ctx=16384` 명시. 동일 케이스 재현: **0/5 → 5/5** |
| **교훈** | 프론티어 모델 기준의 "컨텍스트 누적은 정상" 전제를 소형·양자화 모델에 그대로 적용하면 안 됨 |

**프로덕션 영향 없음**: `runpod_handler/handler.py`가 `max_model_len`을 지정하지 않아 vLLM이
모델 네이티브 값(32K)을 씀 — **로컬 개발 환경에만 있던 격차**(설정상 추정, 직접 검증은 못 함).

---

## 2. "멈춤"이 아니라 "느림"이었다 (2026-07-10)

| | |
|---|---|
| **증상** | 비교 스크립트를 배치로 돌리면 첫 샘플에서 30분+ 진전 없음. 같은 케이스를 단발 호출하면 30초 |
| **처음 의심** | 프로세스/커넥션 누적, GPU 메모리 경합, `_invoke_with_retry()` 무한 백오프 |
| **실제 원인** | **전부 아니었음.** `ollama.log`를 직접 보니 서버는 매 턴 10~12초로 정상 처리 중(task ID 증가). old 방식(전체 재시도)이 budget=3 × 최대 14턴을 도는 구조라 단일 샘플이 10~20분 걸리는 게 정상 |
| **조치** | 코드 수정 없음 — 설계 자체의 느림으로 결론 |
| **교훈** | 클라이언트 쪽 증상(출력 없음)만으로 "멈춤"을 단정하지 말 것. 서버 로그의 진행 지표를 볼 것 |

**확인 방법**: `brew services info ollama`로 로그 경로 확인 후
`tail -f /opt/homebrew/var/log/ollama.log` — task ID가 증가하면 "느린 것", 멈춰 있으면 "멈춘 것".

> 정직하게 남길 점: 이전(07-10 새벽) 시도에서 "정말 멈춘 것"처럼 보였던 것과 이번의
> "정상이지만 느림"이 같은 현상인지는 직접 비교하지 못했다.

---

## 3. 한국어 지시에도 중국어 문항 생성 (2026-07-11)

| | |
|---|---|
| **증상** | STRUCTURE_GOLDEN 20개 중 **9개(45%)** 문항에 한자 혼입. 시스템 프롬프트에 "한국어로만"이 명시돼 있었음 |
| **처음 의심** | 1번과 같은 컨텍스트 잘림 |
| **실제 원인** | **잘림과 무관.** LangSmith 트레이스 100건 정량 분석 결과 — 오염 run의 입력 크기 중앙값 **11,263자** vs 정상 run **8,009자**. 입력이 길수록 오염 확률이 오르는 **확률적 드리프트**(절벽이 아니라 곡선). 오염된 기존 문항이 재시도 프롬프트에 들어가 다음 출력을 오염시키는 캐스케이드도 확인 |
| **조치** | `save_item`에 결정론적 한글 비율 게이트(`_check_korean`) — 한글 0개거나 한자 비율 ≥5%면 저장 거부 + 재작성 피드백. 5% 문턱은 정당한 괄호 병기(예: 사법(私法)) 허용용 |
| **교훈** | "성공"의 정의가 좁으면 새 실패 유형을 놓친다 — 당시 "6/6 성공"은 **개수 기준**이었고 내용 언어는 검증하지 않았다 |

**검증**: 유닛테스트 5케이스 + 분리 보관한 오염 엔트리 9개 소급 판정에서 오염 전부 차단·정상
전부 통과(수동 분류와 100% 일치). 완화 없이 재생성만 반복했을 땐 오염률 ~50%가 안정적으로 재현됐다.

---

## 4. tool-calling 형식 오류를 "정상 종료"로 오판 (2026-07-11)

| | |
|---|---|
| **증상** | 0문항·목표 미달로 조기 종료 (20개 중 6개가 0문항) |
| **처음 의심** | 모델이 작아서 — 7B→14B로 키우면 해결될 것 |
| **실제 원인** | **14B에서도 동일 재현.** 모델은 도구를 부르려는 의도가 있었으나 구조화된 `tool_calls` 필드가 아니라 content에 깨진 형식으로 출력(`iNdEx\n{"name":...}\n</tool_call>`). `agent_node`의 `if not response.tool_calls: break`가 이를 "자발적 종료"로 오판 |
| **조치** | `_looks_like_broken_tool_call()` + `malformed_streak` 카운터 — `tool_calls`가 비어도 도구 호출 흔적이 있으면 재작성 요청(연속 2회까지), turn cap 14는 최종 방어선 |
| **교훈** | 인프라(템플릿·파서) 계층 문제는 모델 규모로 해결되지 않는다 |

**결과**: 0문항이던 6개 재실행 → 6/6 개선. 20개 전체 비교에서도 0문항 6/20 → **2/20**.

> **인과관계는 확정 못 함**: 재실행에서 재시도 로직이 **실제 발동한 횟수는 0회**였다.
> qwen 계열의 run-to-run variance일 가능성이 높다. 로직이 해가 되지 않는 것은 확실하나
> ("최악의 경우도 이전과 동일"), 개선의 원인이라 주장할 근거는 부족하다.

---

## 5. Recall 하락은 실제 저하가 아니라 측정 아티팩트였다 (2026-07-14)

| | |
|---|---|
| **증상** | 청킹 재설계(고정 길이 → 문장 경계 존중) 후 Recall@5가 **0.944 → 0.778**로 하락 |
| **처음 의심** | 청킹 변경이 검색 품질을 망침 → 롤백 검토 |
| **실제 원인** | **검색 성능은 불변이었다.** 미스 4건 중 3건은 검색기가 정답을 **rank1로 정확히 반환**하는데, 골든셋의 `chunk_preview[:80]` substring 매칭이 새 청크 경계에 걸려 실패한 것 |
| **조치** | 골든셋 앵커를 실제 청크 원문 기준으로 재작성(6건) → **Recall@5 0.955 / MRR 0.789** 회복 |
| **교훈** | 채점 방식이 특정 구현의 산물이면 그 구현을 바꿀 때 **골든셋도 함께 재보정**해야 공정한 비교가 된다 |

**부수 발견**: 인덱싱 스크립트의 idempotent 스킵이 `source`(파일명) 기준이라, 청킹 로직만
바꾸고 재실행하면 "이미 적재됨"으로 스킵된다 — 컬렉션 drop & recreate 필요.

**청킹 개선 자체의 효과**(별개): 최소 청크 길이 1자(노이즈) → 31~117자, 페이지 경계 자투리
128~139개/문서 → 4~5개로 흡수. 문장 절단율은 59~75% → 48~62%로 개선폭이 작은데, 이 문서들이
표·불릿이 많은 정부 규정이라 원래부터 마침표로 안 끝나는 줄이 많은 것이 원인.

상세: [EVAL.md 10절](./EVAL.md)

---

## 6. RunPod vLLM에서 tool calling이 동작하지 않음

| | |
|---|---|
| **증상** | 배포 직후 `검증 통과: ✗ \| 생성: 0문항` — 에이전트가 도구를 **한 번도** 호출하지 않음 |
| **처음 의심** | 프롬프트 또는 모델 능력 |
| **실제 원인** | `ChatRunPod._agenerate()`가 항상 `AIMessage(content=text)`만 반환 — `tool_calls` 필드가 없으니 ReAct 루프가 첫 스텝에서 종료. vLLM은 OpenAI API처럼 `tools`를 자동 처리하지 않고, chat template으로 프롬프트에 주입한 뒤 `<tool_call>` 태그 출력을 **직접 파싱**해야 함 |
| **조치** | 핸들러에 `apply_chat_template(tools=...)` + `<tool_call>` 정규식 파싱, 어댑터에서 `tool_calls` 구성 |
| **교훈** | "OpenAI 호환"이라는 말이 tool calling까지 호환한다는 뜻은 아니다 |

**후속 버그**: Qwen이 `arguments`를 JSON 문자열로 내면 핸들러가 다시 `json.dumps()`로 감싸
**이중 인코딩** → `AIMessage.tool_calls[].args`가 dict가 아니라 문자열이 되어 Pydantic
ValidationError. `isinstance(args, str)` 분기로 해결.

---

## 7. EBS를 붙였는데 컨테이너 교체마다 데이터가 사라짐

| | |
|---|---|
| **증상** | EBS 마운트·인덱싱 완료 후에도 컨테이너를 교체할 때마다 컬렉션이 전부 0건 |
| **처음 의심** | 볼륨 마운트 설정 오류 |
| **실제 원인** | `docker run --env-file .env`의 `CHROMA_PERSIST_DIR=./chroma_db`가 Dockerfile의 `ENV CHROMA_PERSIST_DIR=/data/chroma_db`를 **조용히 덮어씀**. 데이터는 컨테이너 내부 `/app/chroma_db`에 쌓이고 EBS는 빈 채로 남아 있었음 |
| **조치** | `.env`의 경로를 EBS 경로로 수정 |
| **교훈** | 환경변수가 여러 곳에 정의될 수 있으면 **컨테이너 안에서 실제 값을 확인**할 것 — `docker exec bunpil printenv CHROMA_PERSIST_DIR` |

---

## 8. 배포 검증 중 드러난 기존 버그 2건 (2026-07-12)

Caddy 경유 전체 경로를 재현하다 이번 작업과 **무관한** 기존 버그 2건을 발견.

| | |
|---|---|
| **증상 A** | 앱이 아예 기동되지 않음 |
| **원인 A** | `requirements.txt`에 `python-multipart` 누락. FastAPI가 `Form(...)` 라우트 등록 시점(임포트 시점)에 `RuntimeError` — Docker만의 문제가 아니라 로컬 `.venv`에서도 동일 재현 |
| **증상 B** | 서비스는 정상인데 컨테이너가 계속 `unhealthy` |
| **원인 B** | `HEALTHCHECK`가 `http://localhost:3000`을 쓰는데 Alpine 리졸버가 `localhost`를 IPv6(`::1`)로 먼저 시도 → IPv4에만 바인딩된 Next.js에 연결 실패. `127.0.0.1` 명시로 해결 |
| **교훈** | 컨테이너 포트에 직접 접근하는 검증은 리버스 프록시 경유 경로를 검증하지 못한다 — 실제 경로를 재현해야 숨은 버그가 드러난다 |

---

## 9. 생성 프롬프트를 고쳐도 eval 점수가 똑같았다 (2026-07-09)

| | |
|---|---|
| **증상** | `agent_node` 프롬프트에 오답매력도 지시를 추가하고 전/후를 재실행했는데 평균이 **2.815로 완전히 동일** |
| **처음 의심** | 실행 간 노이즈 |
| **실제 원인** | `eval_item_quality()`가 채점하는 `ITEM_GOLDEN`은 **하드코딩된 고정 30문항**이라 `agent_node`를 아예 호출하지 않음 — 생성 프롬프트를 바꿔도 이 지표엔 원리적으로 반영될 수 없었음 |
| **조치** | 실제 생성 후 채점하는 별도 스크립트(`compare_distractor_quality.py`)로 재검증 → 2.500 → 2.846 |
| **교훈** | **"eval이 있다"와 "내가 바꾼 부분이 그 eval이 지나는 경로에 있다"는 별개다.** Judge 프롬프트 변경은 같은 방식으로 검증 가능하지만(채점 대상이 고정이어도 채점 기준이 바뀌므로) 생성 프롬프트는 불가능 |

---

## 10. 후보를 늘릴수록 검색이 나빠졌다 (2026-08-03)

| | |
|---|---|
| **증상** | 하이브리드 검색(BM25+dense) 도입했는데 주 목표였던 regulations MRR이 **0.667 그대로** |
| **처음 의심** | 검색기(retriever)가 약하다 → 더 강한 리랭커 모델(~2.3GB) 교체 검토 |
| **실제 원인** | 병목은 검색기가 아니라 **리랭커**였고, 통념과 반대로 **후보를 많이 줄수록 나빠졌다** — `n_candidates` 10/20/30/50에서 Recall@5가 1.000/0.955/0.955/0.909 |
| **조치** | 모델 교체 대신 `n_candidates` 20→10 (비용 0). 골든 22건 전수 대조에서 개선 3건·악화 0건, 리랭커 처리 쌍이 절반이라 **속도도 2배**(25s→13s) |
| **교훈** | 무거운 해결책(모델 교체)에 착수하기 전에 **비용 0인 파라미터부터** 측정할 것 |

**결과**: 전체 Recall@5 **1.000**(첫 만점)·MRR 0.854. 상세: [EVAL.md 12·13절](./EVAL.md)

---

## 11. "검색이 못 찾은 게 아니라 조항이 없었다" (2026-08-03)

| | |
|---|---|
| **증상** | 규정 위반 탐지에서 가정환경·종교 관련 규정을 계속 못 찾음. 몇 달간 "RAG 검색 약점"으로 진단 |
| **처음 의심** | 검색기가 관련 청크를 상위로 못 올림 |
| **실제 원인** | regulations 코퍼스 **472청크 전수 스캔** 결과 '종교'·'정치적 성향'·'외모/용모'·'추측'·'가정환경'이 **전부 0건**. 인덱싱된 문서에 그 조항 자체가 없었음 — 하이브리드도 리랭커 교체도 고칠 수 없는 문제 |
| **조치** | 키워드 규칙을 차단 → 경고로 강등. 최종적으로 검증 불가능한 생기부 모듈 전체를 제거 |
| **교훈** | **없는 것을 검색할 수는 없다.** 검색 품질을 의심하기 전에 정답이 코퍼스에 실재하는지부터 확인할 것 |

**어휘 격차 사례**: 교사가 "가정형편"이라 쓰는 것을 규정은 "부모(친인척 포함)의 사회･경제적
지위"라고 쓴다. **공유 토큰이 0이라** BM25로도 안 잡히고 dense도 이 격차를 못 넘었다.

상세: [EVAL.md 14절](./EVAL.md)

---

## 12. LangSmith "한도 소진"은 오진이었다 (2026-08-04)

| | |
|---|---|
| **증상** | 트레이스 재측정이 계속 0건. 조회(`list_runs`)는 되는데 수집만 안 됨 |
| **처음 의심** | 무료 플랜 조회 한도 소진 (전날 이렇게 결론 내고 작업을 중단했었음) |
| **실제 원인** | **한도는 멀쩡했다.** 배선 버그 3건 — ① `test_exam.py`에 `load_dotenv()`가 없어 API 키 없이 트레이싱이 켜져 401 ② 5개 스크립트가 `init_langsmith_project()`를 안 불러 트레이스가 `bunpil-dev`가 아닌 `bunpil`로 샘 ③ `--since`를 UTC 자정으로 해석해 KST 오전 트레이스가 통째로 누락 |
| **조치** | 3건 모두 수정. 재측정 정상 완료 |
| **교훈** | **추론하지 말고 직접 찔러본다** — ingest 엔드포인트에 최소 run을 POST해 **`202 Accepted`**를 받고서야 한도 문제가 아님이 확정됐다 |

"조회는 되는데 수집은 안 된다"는 비대칭을 한도 증상으로 읽은 것이 오진의 출발점이었다.
실제로는 조회 스크립트만 `load_dotenv()`를 부르고 있었을 뿐이다.

**부수 성과**: 깨끗한 표본으로 재측정하니 기존 가설 2건이 기각됐다 — `record_score` 거부율
74%(단독 최악)는 오염된 표본의 산물이었고(실제 0.500으로 다른 도구보다 낮음), "malformed
tool-call 0건"도 표본 부족이었다(재측정 시 18.4%).

상세: [EVAL.md 11·11.1절](./EVAL.md) · 재발 시 점검 순서: [LANGSMITH_GUIDE.md 3.3.1절](./LANGSMITH_GUIDE.md)

---

## 13. 통과할 수 없는 게이트 (2026-08-04)

| | |
|---|---|
| **증상** | 에이전트가 `submit_for_review`를 호출해 정상 완료하는 경우가 거의 없음 (validate 통과율 0.000) |
| **처음 의심** | 종료 신호(`submit_for_review`) 버그 또는 생성 품질 문제 |
| **실제 원인** | **종료 신호는 정상**(6세션 10회 호출·거부 0건). 트레이스를 집계하니 재시도 사유가 `judgment 11 · both 7 · format 0` — **Judge 지표 게이트가 18/18(100%) 실패**했고 그중 11회는 개수·품질은 통과하고 Judge 지표만 단독으로 막았다 |
| **조치** | 게이트가 쓰는 Judge의 실제 점수 분포를 45건 재채점해 **처음으로 측정**. `overall_score` 임계값 4→3, `type_ratio` 0.7→0.5 재보정 |
| **교훈** | **채점자를 계속 튜닝하면서 합격선은 한 번도 안 봤다.** 임계값은 2026-07-06 도입 후 한 번도 재검토되지 않았고 근거 기록도 없었는데, 그 사이 Judge 프롬프트는 4회 이상 재보정됐다 |

**결정적 증거**: Judge 프롬프트가 "이게 정답"이라고 가르치는 few-shot 6개를 게이트에 넣어보니
**5개가 탈락**했다. 특히 *"이런 건 감점하지 말라"*고 가르치려고 넣은 균형 예시(`overall` 4점)가
반려됐다 — **프롬프트와 판정 코드가 정면으로 모순**이었다. 통과하는 유일한 1개는 `overall=5`를
요구하는데, 실측에서 Judge는 5점을 거의 주지 않는다(45건 중 0~1건).

**아이러니**: Judge를 더 정확한 모델(gpt-5.6-luna)로 교체한 것이 게이트를 **더** 어렵게 만들었다.
관대하던 이전 모델(편향 +1.0)은 4점 이상을 24% 줬지만, 사람 라벨을 잘 추종하는 새 모델은
사람 평균(2.04/5)을 따라가므로 4점을 거의 주지 않는다.

| 지표 | 재보정 전 | 재보정 후 |
|---|---|---|
| 게이트 실측 통과율(2회 측정) | 8.9% / **6.7%** | 48.9% / **42.2%** |

**실사용 영향**: 교사가 결과를 못 받는 문제는 **아니었다**(문항은 그대로 반환되고 UI는
`validation_passed`를 표시하지 않음). 실질 비용은 통과가 불가능하니 **매 요청이 재시도 예산을
전량 소진**한 것 — 불필요하게 느리고 비쌌다.

**재발 방지**: 측정 스크립트를 `experiments/measure_validate_gate.py`로 정식 편입하고 임계값
근거를 코드 주석에 남겼다(이전엔 근거가 어디에도 없던 것이 문제의 뿌리였으므로).

**조사 중 발견한 별건**: 문항 품질 점수(`record_score`)를 **문항을 생성한 에이전트가 자기
자신에게** 매기고 그 점수가 게이트를 쥐고 있었다. 사람 라벨과 대조된 적이 없어 게이트에서
제외하고 참고값으로 강등했다(2026-08-05).

상세: [EVAL.md 15·16절](./EVAL.md)


---

## 14. 측정 도구가 측정할 수 없는 것을 재고 있었다 (2026-08-07)

| | |
|---|---|
| **증상** | 게이트 임계값을 재보정(13번)했는데 런타임 통과율이 baseline과 똑같이 **0.000** |
| **처음 의심** | 재보정이 무효했나 / 자기채점 도구 제거가 역효과를 냈나 |
| **실제 원인** | **이 스모크 테스트로는 애초에 통과가 불가능했다.** `test_exam.py`의 예시 문제는 객관식1+서술형1인데 `num_items=1`을 요청한다 — 1문항 세트는 2가지 유형 구성을 재현할 수 없어 `type_ratio`가 0.5에서 막히고, Judge 프롬프트의 few-shot 3번이 **정확히 이 상황을 `overall=2`로 채점하라고 가르치고 있다** |
| **조치** | 예시 문항 2개를 **둘 다 객관식**으로 바꿔(유형 균일) 게이트를 도달 가능하게 하고, `num_items=1`은 유지해 개수 디커플링 검증은 보존. 실패 진단도 3단계(배선 / 개수 / Judge 게이트)로 분리 |
| **교훈** | 지표가 안 움직이면 **변경이 아니라 지표부터 의심**할 것 — 이 프로젝트에서만 세 번째다(5·9번) |

**통제 실험으로 확정** — 동일한 생성 문항 1개를 예시 구성만 바꿔 재채점:

| 예시 구성 | type_ratio | overall | 게이트 |
|---|---|---|---|
| 객관식1 + 서술형1 (스모크 조건) | 0.5 | **2** | 탈락 |
| 객관식1만 (유형 구성 일치) | 1.0 | **4** | 통과 |

같은 문항인데 예시만 바꾸니 2점 → 4점. baseline의 0.000도 같은 이유였으므로 **이
지표로는 재보정 전후를 비교할 수 없다**. 재보정 근거(STRUCTURE_GOLDEN 45건,
6.7%→42.2%)는 num_items 3/5/7의 다양한 조합이라 이 상한에 걸리지 않아 여전히 유효하다.

**부수 성과**: 측정 가능한 축에서는 뚜렷한 변화가 있었다 — 자기채점 도구(`record_score`)
제거 후 **malformed tool-call 18.4% → 0%**. 그 도구가 "직전 턴이 반환한 item_id를 다음
턴에 다시 넣어 호출"하는 상태 의존 단계였고 실측 거부율이 0.500이었는데, 단계를 없애자
그 실패 경로가 사라졌다. 다만 `validate_item_format` 거부가 0→18로 늘어, 실패가 "호출
문법 깨짐"에서 "내용 형식 미달"로 **옮겨간 것**일 수도 있다(n=6 세션으로는 구분 불가).

**수정 후 첫 통과 관측(2026-08-07)**: 6세션 재측정에서 개수 달성 6/6, 게이트 통과 **3/6**.
이 프로젝트에서 런타임 통과가 관측된 것은 처음이다. 재보정 효과만 분리하려고 **같은 6회
측정치에 두 기준을 각각 적용**하니 옛 기준 1/6 vs 새 기준 3/6 — 통과 3건 중 2건이
`overall=3`으로 옛 기준이었다면 탈락했을 값이었다.

덤으로 게이트가 실제로 변별한다는 것도 확인됐다: `overall` 분포가 {1,1,1,3,3,4}로 이봉이고,
1점은 루브릭상 "원문 근접 복사"다 — `save_item`의 어휘 기반 복사 게이트(bigram containment)가
못 잡는 **의미적 근접 복제**를 Judge가 메우고 있다.

상세: [EVAL.md 17·18·19절](./EVAL.md)
