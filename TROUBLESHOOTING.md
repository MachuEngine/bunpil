# 트러블슈팅: 로컬 Ollama 컨텍스트 제한으로 인한 STRUCTURE_GOLDEN 생성 실패율 급락

**날짜**: 2026-07-09 ~ 07-10
**영향 범위**: 로컬 개발 환경(Ollama)만 해당, 프로덕션(RunPod)은 설정상 영향 없었을 것으로 추정

## 증상

STRUCTURE_GOLDEN 골든셋 추가 생성 중, 정상 범위였던 실패율(~35~40%)이 갑자기 2/34(약 6%)
성공률로 급락. qwen2.5:7b가 도구 호출 대신 일반 텍스트로 답하며 중국어/스페인어가
섞인 응답을 내는 패턴 관찰.

## 원인

- Ollama가 `num_ctx` 미설정 시 기본값 4096 토큰으로 컨텍스트를 제한
- qwen2.5:7b 모델 자체는 32K 네이티브 지원하지만, 로컬 실행 설정이 이를 활용 못 함
- 멀티턴 ReAct 루프(문항마다 validate→save→score 반복 + search_standards/
  search_regulations 검색 결과 누적)에서 몇 턴만 지나도 4096을 초과
- 컨텍스트가 잘리면서 모델이 시스템 프롬프트를 "잃고" 응답이 붕괴
- 같은 날 시스템 프롬프트에 문구 2개(오답매력도 지시, num_items 지시)가 추가되며
  한계를 더 자주 넘겨 실패율이 급격히 악화된 것으로 추정

## 조치

`app/modules/exam/llm.py`의 `ChatOllama` 초기화에 `num_ctx=16384` 명시적 설정 추가.
설정 전/후 동일 케이스(str_010) 재테스트: 0/5 → 5/5로 개선 확인.

## 남은 이슈 (해결 안 됨, 별도 트래킹 필요)

1. **tool-calling 자체의 간헐적 실패**: 컨텍스트 문제와 별개로, qwen2.5:7b가 이유 없이
   도구 호출 대신 텍스트 서술로 답하는 현상이 여전히 남아있음(원래 수준인 ~35~40%
   실패율로 추정, 100% 해결 아님). 원인 후보: temperature=0.7(구조화 출력 모드 불안정),
   max_tokens=2048 캡으로 인한 도중 절단, 턴 누적에 따른 형식 준수력 저하.
   **→ 14B에서도 동일 실패 재현 확인(2026-07-11)**: "모델을 키우면 나아질 것"이라는
   가정으로 STRUCTURE_GOLDEN을 qwen2.5:14b(budget=1, 유사도 게이트 활성)로 재생성했더니
   20개 중 6개가 0문항, 9개가 목표 미달로 끝남(정확 일치 5/20). LangSmith 트레이스로 실제
   원인을 확인한 결과, 모델은 도구를 부르려는 의도는 있었으나 **구조화된 tool_calls
   필드가 아니라 content 텍스트에 깨진 형식으로 출력**함:
   ```
    iNdEx
   {"name": "search_standards", "arguments": {"query":"지적재산권 보호"}}
   </tool_call>
   ```
   `agent_node`의 루프(`if not response.tool_calls: break`, graph.py)는 이 응답의
   `tool_calls`가 비어있으므로 "에이전트가 자발적으로 끝냈다"고 오판해 즉시 종료함 —
   실제로는 형식 파싱 실패이지 의도적 종료가 아님. 프롬프트 지시("도구 호출 외 텍스트
   금지")는 오히려 준수하려 한 흔적(자유 서술 없이 도구 호출 흉내만 냄)이 있어, 이건
   프롬프트 튜닝으로 고칠 문제가 아니라 **Ollama/qwen 계열 tool-calling 파싱 자체의
   구조적 결함**이며 모델 크기와 무관하게 재현됨. 개선 방안은 이 문서 최하단
   "tool-calling 형식 오류 개선 방안 (제안, 미적용)" 참고.

2. **컨텍스트 누적 자체는 정상적인 ReAct 패턴의 본질**이나, 관리 없이 방치됨:
   - 검색 도구(search_standards/search_regulations) top_k=3 × 300~400자 청크가
     한 턴에 최대 6개까지 쌓일 수 있음 (top_k=3의 근거는 코드/커밋 히스토리에 문서화 안 됨)
   - 모델이 도구 호출 사이 불필요한 서술을 길게 씀
   - 대화 슬라이딩 윈도우나 오래된 턴 압축 로직 없음
3. **속도**: 로컬 M5(24GB)에서 5문항 요청 1회 시도(성공/실패 무관) 약 2분 34초.
   프로덕션 budget=5 기준 최악의 경우(5회 전부 실패) 약 12~13분 소요 가능 — 데이터센터
   GPU(RunPod)에서는 훨씬 빠르겠지만 방향성(턴이 쌓일수록 느려짐)은 동일하게 남음.

## 프로덕션 영향 여부

- **컨텍스트 잘림**: `runpod_handler/handler.py`가 `max_model_len`을 명시하지 않아
  vLLM이 모델 네이티브 값(32K)을 기본 사용 — 이 구체적 증상은 프로덕션에서 안 겪었을
  가능성 높음(직접 검증은 못 함, 설정상 추정).
- **속도/비용**: 구조 자체(순차 다중 호출 + 매턴 전체 재처리)는 프로덕션에도 동일하게
  적용되므로, 컨텍스트 관리 개선은 비용·응답시간 관점에서 여전히 가치 있음.

## 추가 조사: test_retry_preservation.py 배치 실행이 첫 샘플에서 30분+ 멈추는 현상 (2026-07-10~11)

**증상**: 재시도 구조 개선(부분 진행 보존) 전/후 비교 스크립트를 배치로 돌리면 첫 샘플에서
30분 넘게 진전이 없어 보임(20개 표본, 8개로 축소해도 재현). 반면 동일 케이스를 단발
수동 호출하면 30초 만에 정상 응답.

**조사 방법**: Monitor 도구로 20초 간격 `ollama ps`/커넥션 수 샘플링 + `sample` 명령으로
Python 프로세스 스택 확인 + `/opt/homebrew/var/log/ollama.log`(llama-server 실제 로그,
`brew services info ollama`로 위치 확인) 실시간 확인.

**의심했던 원인과 결과**:
- 프로세스/커넥션 누적, GPU 메모리 경합 → **아니었음**. `ps aux`로 확인 시 중복 프로세스
  없음, `lsof -i :11434` 커넥션 수는 4~6개로 안정적(누적 없음)
- `_invoke_with_retry()`의 재시도/백오프가 무한정 늘어지는 것 → **아니었음**. 재현 시도
  중 재시도 래퍼가 발동하지 않고도 오래 걸림(예외 자체가 안 남)

**실제로 확인된 것**: `ollama.log`를 직접 보니 서버는 매 턴 10~12초로 정상 처리 중이었고
(task ID가 꾸준히 증가), Python 프로세스가 `sock_recv`에 멈춰있는 것처럼 보였던 건
스트리밍 응답을 청크 단위로 받는 정상적인 대기였음(오해였음). "old"(전체 재시도) 방식은
`num_items`가 큰 샘플에서 budget=3 전부를 소진하며 각 시도가 최대 14턴을 도는 구조라,
단일 샘플 하나가 실제로 10분~20분 이상 걸릴 수 있음 — **버그가 아니라 old 설계 자체의
느림**이었을 가능성이 높음.

**다만 완전히 결론 내리지 못한 부분**: 이전(2026-07-10 새벽) 시도들에서 실제로 "정말
멈춘 것"처럼 보였던 것(수십 분간 `ollama.log`도 확인 안 하고 판단했던 것이라 그때도
정상 처리 중이었는지는 불확실함)과, 이번에 관찰한 "정상이지만 느림"이 같은 현상인지는
직접 비교하지 못함. 다음에 이 스크립트를 다시 돌릴 때는 `tail -f /opt/homebrew/var/log/ollama.log`를
같이 열어두고 task ID 증가 여부로 즉시 "느린 것"과 "멈춘 것"을 구분할 것 — 이번에 배운
가장 실용적인 디버깅 방법.

## 추가 발견: 언어 오염 — 한국어 지시에도 중국어 문항 생성 (2026-07-11)

**증상**: STRUCTURE_GOLDEN 20개 중 9개(45%)의 generated_items에 중국어(한자)가 섞임.
시스템 프롬프트에 "한국어로만 응답하세요"가 명시돼 있고, num_ctx=16384 수정·재시도
구조 개선(부분 진행 보존) **이후에 생성된 신버전 6개 중에서도 3개(str_037/038/040)가
오염** — 재시도 구조 개선은 "개수 미달"만 고쳤을 뿐 언어 오염은 별개 이슈로 남아있었음.
당시 "6/6 성공" 집계는 개수 일치 기준이었고 내용 언어는 검증하지 않았던 것이 원인
(성공 지표 정의가 좁으면 새 실패 유형을 놓친다는 교훈).

**관찰된 패턴** (오염 엔트리의 문항별 K=한국어/C=중국어):
- str_038: `KKKKCC` — 앞 문항은 한국어, 뒤로 갈수록 중국어
- str_037: `KKC`, str_017: `KCCC`(1번만 한국어)
- 오염 문항 다수가 **앞선 한국어 문항의 중국어 번역-복제본**(예: str_038 item5~6은
  item1~4와 같은 질문의 중국어판)

**정량 확인 (LangSmith 트레이스, str_035~040 생성 배치 100개 LLM run)**:
- 중국어 출력 run 13개: 입력 크기 중앙값 **11,263자** (최소 5,236자)
- 정상 출력 run 87개: 입력 크기 중앙값 **8,009자** (최대 17,378자)
- 입력이 길수록 오염 확률이 높아지는 **확률적 드리프트** — num_ctx(16384 토큰) 잘림과는
  무관(오염 발생 입력들은 전부 한도 내). "작은 모델이 컨텍스트 노이즈에 취약하다"는
  기존 가설과 일치하되, 절벽(threshold)이 아니라 확률 상승 곡선임
- 재시도 프롬프트에 오염된 기존 문항 요약이 들어가면(입력에 한자 포함) 그 다음 출력이
  또 오염되는 캐스케이드 경로도 확인 — 단, 항상 전파되지는 않음(입력 한자 352자인데
  출력 0자인 run도 있음)

**temperature 상관**: 확인 불가 — temperature A/B 실험은 점수만 저장하고 생성 텍스트를
남기지 않았음. STRUCTURE_GOLDEN은 전부 기본값 0.7로 생성됨. 향후 실험 시 텍스트(최소한
한자 개수)도 함께 기록할 것.

**모델 특성 여부**: Qwen 계열에서 비영어 프롬프트에 중국어가 섞이는 언어 혼입은 커뮤니티
리포트가 존재(예: 히브리어 질문에 중국어 문자가 섞이는 QwenLM GitHub 이슈). 공식 문서에
"알려진 한계"로 명시된 것은 찾지 못했으나, 중국어 중심 사전학습 데이터 특성상 저자원
언어 출력에서 중국어로 새는 현상 자체는 개연성 있음.

**조치(2026-07-11)**: 심각 오염 6개(str_003/007/017/032/038/040)를
`data/golden/structure_golden_contaminated_examples.json`으로 분리 보관(실패 사례 자료),
경미 오염 3개(str_001/014/037)는 `has_language_contamination: true` 플래그로 남김,
분리분만큼 신규 생성(str_041~046)으로 보충 시도. ※ str_017은 최초 분류에서 '경미(선지만)'로
봤으나 실제로는 4문항 중 3개 question이 통째로 중국어(한자 비율 58%)라 심각으로 재분류.

**보충 생성에서도 오염 재현(2026-07-11)**: 대체용 신규 6개(str_041~046, budget=1, 현재
코드) 중 **3개(str_044/045/046)가 또 question 필드까지 오염** — 오염률 ~50%가 안정적으로
재현됨. 완화책 없이 재생성을 반복하는 것은 동전 던지기와 같아 중단. 오염 3개는 분리
파일로 이동(총 9개 보관), 정상 3개(str_041/042/043, 전부 한국어이나 budget=1이라 개수
부족)만 골든셋에 유지 — **최종 17개**. 완화 방안 1(save_item 한글 비율 게이트) 적용 후
재생성하는 것이 순서상 맞음.

**완화 방안 검토 결과**:
1. **[적용됨, 2026-07-11 승인]** `save_item`에 한글 비율 결정론적 게이트(`_check_korean`,
   `app/modules/exam/tools.py`) — 한글이 없거나 한자 비율이 5% 이상이면 저장 거부 +
   "한국어로 다시 작성" 피드백 반환. 5% 문턱은 정당한 괄호 병기(예: 사법(私法))를
   허용하기 위함. 검증: 단위 테스트 5케이스(한국어 통과/중국어 거부/혼합 거부/괄호
   병기 통과/영어 거부) + 분리된 오염 엔트리 9개 소급 판정에서 오염 문항 전부 차단,
   정상 문항 전부 통과(수동 분류와 100% 일치) + test_exam.py 엔드투엔드 정상.
   오염 캐스케이드(재시도 프롬프트로 오염 전파)도 원천 차단됨
2. 언어 지시를 매 턴 반복 주입 — 미적용(컨텍스트 증가 부작용, 1번으로 충분한지 관찰 후 판단)
3. `similarity_judge` 단계 언어 검증 — 미적용(세트 전체 재시도를 유발해 1번보다 낭비)

## 배운 것

- 로컬 개발 환경과 프로덕션 인프라 설정이 은근히 다를 수 있다는 것을 이번에 직접 확인함
  (Ollama 기본값 vs vLLM 기본값의 차이) — 향후 다른 설정값도 로컬/프로덕션 parity를
  한번 점검할 필요
- 작은 모델(7B, Q4 양자화)일수록 컨텍스트 노이즈에 더 취약함 — 프론티어 모델 기준의
  "컨텍스트 누적 정상" 전제를 그대로 가져오면 안 됨
- "느려 보임"과 "멈춤"을 클라이언트 쪽 증상(출력 없음)만으로 판단하지 말 것 — Ollama의
  실제 서버 로그(`brew services info <서비스명>`으로 로그 경로 확인, 이번엔
  `/opt/homebrew/var/log/ollama.log`)를 직접 보면 task ID 증가 여부로 즉시 구분 가능
- 모델 크기를 키우면 저절로 좋아질 거라는 가정은 검증 없이 믿지 말 것 — tool-calling
  파싱처럼 인프라(Ollama 템플릿·파서) 계층의 문제는 7B→14B로 규모를 키워도 재현됨

## tool-calling 형식 오류 개선 방안 (2026-07-11, 1번 적용됨)

`agent_node`(graph.py)의 루프는 `if not response.tool_calls: break`로 종료를 판단한다.
모델이 도구를 부르려는 의도로 텍스트에 깨진 형식(예: `iNdEx\n{"name":...}\n</tool_call>`)을
냈을 때도 `tool_calls`가 비어있으므로 "정상 종료"로 오판해 루프가 즉시 끝난다 — 이게
7B/14B 공통으로 관찰된 조기 종료(0문항·목표 미달)의 상당 부분을 설명한다. 검토한 옵션:

1. **형식 오류 감지 시 종료 대신 재시도 (권장 후보)** — `tool_calls`가 비어있어도
   `content`에 `{"name":`나 `</tool_call>` 같은 도구 호출 흔적이 있으면 "정상 종료"가
   아니라 "형식 오류"로 분류. 이 경우 루프를 끝내지 않고, 사람이 읽는 오류 메시지를
   HumanMessage로 보내 재시도시킨다(예: "도구 호출 형식이 손상되었습니다. 정확한
   tool call 형식으로 다시 시도하세요") — `validate_item_format`이 형식 오류를
   자기교정시키는 것과 같은 패턴. turn cap(14)은 그대로 안전장치로 유지.
   장점: 기존 자기교정 패턴과 일관, 코드 변경이 국소적. 단점: 재시도해도 같은 파싱
   실패가 반복될 수 있어 완전한 해결은 아님(성공률 개선이지 100% 보장 아님).
2. **content에서 도구 호출을 정규식으로 파싱해 직접 실행** — 깨진 형식이라도
   `{"name": "...", "arguments": {...}}` 패턴을 추출할 수 있으면 코드가 파싱해 정상
   tool_call처럼 실행. 장점: 모델의 "의도"를 실제로 살릴 수 있어 성공률 개선 폭이
   가장 클 가능성. 단점: 파서가 또 다른 실패 지점이 되고, 형식이 매번 달라 정규식
   커버리지를 계속 넓혀야 할 수 있음(유지보수 부담).
3. **Ollama 서버 설정 조정** — 현재 `--no-jinja --chat-template chatml`로 강제 템플릿
   사용 중(brew services 프로세스 인자 확인됨). Qwen2.5 네이티츠 템플릿(jinja) 사용이나
   `format=json` 강제 등으로 파싱 안정성이 개선되는지 검증 필요. 장점: 근본 원인(템플릿)에
   더 가까운 수정. 단점: 로컬 Ollama 설정 변경이라 RunPod(vLLM) prod에는 적용 안 됨 —
   dev/prod parity 문제를 새로 만들 수 있어 신중히 검토 필요.
4. **아무 조치 없이 현행 유지** — turn cap과 budget 재시도가 이미 안전망 역할. 단
   비용(시간·API 호출) 대비 완주율이 낮은 채로 남음.

우선순위 제안: 1번을 먼저 적용해 효과를 측정하고, 부족하면 2번을 보강하는 순서.
3번은 prod 영향 범위가 넓어 별도로 검증 후 판단 권장.

### 1번 적용 결과 (2026-07-11)

`graph.py`에 `_looks_like_broken_tool_call()`(정규식으로 `"name":` 또는 `<tool_call>`
흔적 감지) + `malformed_streak` 카운터 추가. `tool_calls`가 비어도 이 흔적이 있으면
"형식 오류"로 보고 "정확한 tool call 형식으로 다시 시도하라"는 HumanMessage를 넣어
연속 2회까지 재시도, 그래도 안 되면 기존처럼 종료(바깥쪽 turn cap 14는 항상 최종 방어선
— 무한루프 불가). 로컬(Ollama)·프로덕션(RunPod) 둘 다 같은 `agent_node` 코드를 타므로
양쪽에 동일 적용됨.

**검증**:
- 단위 테스트: 실제 관찰된 깨진 응답(`iNdEx\n{"name":...}\n</tool_call>`) → 정상 감지,
  자유 서술 텍스트·빈 문자열 → 오탐 없음
- `test_exam.py` e2e: 정상 동작, 회귀 없음
- **타겟 회귀 테스트**: STRUCTURE_GOLDEN v2(게이트 후, 14B)에서 0문항으로 끝났던 6개
  샘플(str_011/014/025/037/039/049)을 동일 조건(budget=1)으로 재실행 →
  **6/6 전부 1개 이상 생성으로 개선**(0문항 6/6 → 0/6)

**단, 정직하게 밝혀야 할 한계**: 이 6개 재실행에서 `malformed_streak` 재시도 로직이
**실제로 발동한 횟수는 0회**였다 — 즉 이번 개선은 새 재시도 로직이 직접 구제한 결과가
아니라, qwen 계열의 확률적 특성상 같은 조건이라도 실행마다 결과가 달라지는 자연스러운
변동(run-to-run variance)일 가능성이 높다(str_011을 앞서 개별 재실행했을 때도 게이트
후 재생성 시점엔 0개였다가 이번엔 3개가 나온 전례 있음). 새 로직이 "해가 되지는 않는다"는
것은 확실하다(최악의 경우도 이전과 동일하게 종료, 추가 비용은 최대 2턴) — 다만 "이번
개선의 원인이 이 로직이다"라고 인과관계를 주장할 근거는 이 테스트만으로는 부족하다.
실제 발동 사례를 직접 잡으려면 표본을 더 늘려야 하는데, 확률적 현상이라 소표본으로는
발동 자체를 보장할 수 없다 — 다음에 배치 생성을 더 크게 돌릴 기회가 있으면 발동 횟수를
같이 기록해 효과를 재확인할 것.

**후속: STRUCTURE_GOLDEN v3(20개 전체) 재생성 결과 (2026-07-11)**: 소표본(6개)이 아니라
동일한 20개 passage 전체로 재시도 로직 적용 전(v2)/후(v3)를 비교. 0개 비율 6/20 →
**2/20**로 개선, 정확 일치 5/20 → **8/20**으로 개선 — 대표본에서는 방향이 뚜렷함(EVAL.md
결과 이력 참고). 다만 `gen_structure_golden.py`가 `agent_messages`를 저장하지 않아 이번에도
재시도 로직의 실제 발동 횟수는 직접 확인하지 못함 — "로직이 원인이다"는 상관관계 수준의
증거이지 인과 증명은 아님. 발동 여부까지 확정하려면 골든셋 생성 스크립트에 재시도 발동
카운트를 기록하는 계측을 추가해야 함(현재 미적용).

## RAG 청킹 전략 수정 — 고정 길이 분할 → 문장/문단 경계 존중 (2026-07-14)

**성격**: 지표(Recall@5)를 고치는 작업이 아니라, 검색 품질의 기반이 되는 **청킹 품질 개선(기술 부채 상환)**. Recall@5는 이미 0.905로 목표(≥0.8)를 넘긴 상태였고, 이번 변경으로 그 수치가 크게 오를 것으로 기대하지 않는다(오히려 검색 노이즈 ±0.05 범위에서 흔들릴 수 있음). 그럼에도 청킹 자체에 명백한 결함이 있어 수정.

### 원인 (기존 `chunk_document()`의 두 가지 결함)

3개 실사용 문서(규정 2·성취기준 1)를 실제로 청킹해 데이터로 확인:

1. **페이지마다 청킹 리셋** — 페이지 끝에서 chunk_size(800자)를 못 채운 자투리가 페이지마다 발생. 심하면 **1글자짜리 청크**까지 생김(기재요령 문서 최소 청크 = 1자). 페이지의 46~58%가 400자 미만의 짧은 마지막 청크로 끝남. 이런 초소형 청크는 임베딩이 사실상 노이즈.
2. **글자 수 기준 고정 분할** — 문장 한가운데서 잘려("환경권은 국민의 기본" / "권으로서...") 조각만으로 의미 파악이 어렵고 임베딩 품질이 떨어짐.

### 조치

`app/common/rag/parser.py`의 `chunk_document()` 재작성:
- 페이지를 하나로 **이어붙인 뒤** 청킹(페이지 경계 리셋 제거) — 각 문자가 어느 페이지에서 왔는지 병렬 배열로 추적해, 청크의 시작 페이지(`page`, 하위호환)와 끝 페이지(`page_end`)를 메타데이터에 기록.
- `RecursiveCharacterTextSplitter`(langchain-text-splitters)로 문장/문단 경계 우선 분할. separators를 한국어/영어 문장부호 고려해 지정(`["\n\n", "\n", ". ", ".\n", "。", "! ", "? ", " ", ""]`).
- chunk_size/overlap은 **800/100 유지** — 600/1000으로 바꿔봤으나 문장 절단율 개선폭이 미미하거나 오히려 나빠서, "청킹 방식"이라는 변수 하나만 바꾸기 위해 크기는 고정.
- `app/common/rag/store.py`: 청크 메타데이터에 `page_end` 추가(청크 ID 생성 `{source}_p{page}_{i}`는 `page`를 그대로 써서 하위호환 유지). 구버전 청크 대비 `.get` 폴백.
- `requirements.txt`에 `langchain-text-splitters>=1.1.0` 명시(기존엔 Ragas 설치에 딸려온 미선언 상태였음).

### 효과 (동일 3개 문서, 800/100 기준)

| 지표 | 기존 | 신규 |
|---|---|---|
| 최소 청크 길이 | 1자(노이즈) | 31~117자 |
| 페이지 경계 자투리 | 128~139개/문서 | 다중페이지 청크 4~5개로 흡수 |
| 초소형(<100자) 청크 | 다수 | 4~5개/문서 |
| 문장 중간 절단율 | 59~75% | 48~62% |

**정직하게 밝힐 점**: 가장 큰 성과는 "1글자짜리 노이즈 청크 + 페이지 경계 자투리 제거"이지, 문장 절단율의 극적 개선은 아니다. 절단율이 크게 안 떨어진 이유는 이 문서들이 정부 규정·교육과정이라 **표·불릿 목록·소제목이 많아** 원래부터 마침표로 안 끝나는 줄이 많기 때문(예: `• 환경권`, `(나) 성취기준 적용 시 고려 사항`). 측정 지표가 이런 정상적 목록 경계를 "절단"으로 오집계한 것이라, 실제 절단 문제는 저 수치보다 덜 심각하다.

### 이번 범위에서 하지 않은 것 (후속 과제)

- **재인덱싱·골든셋 재생성·재평가는 보류**. 이유: (1) Recall@5가 이미 목표 초과라 재측정해도 개선이 숫자로 안 보일 가능성이 높고 오히려 노이즈로 내려간 것처럼 보일 수 있음, (2) `eval_retrieval()`이 `chunk_preview[:80]` 문자열 포함 여부로 정답을 판정하는데 청크 경계가 바뀌면 이 80자 창이 두 청크에 걸쳐 매칭이 깨질 수 있어 **인위적인 Recall 하락**을 유발할 수 있음 → 골든셋 재생성 + 사람 재검수(22개)라는 비용이 듦.
- **재인덱싱 스킵 로직 주의**: `index_regulations.py`/`index_standards.py`의 idempotent 스킵은 `source`(파일명) 기준이라, 청킹 로직만 바꾸고 재실행하면 **같은 파일이라 스킵**되어 반영 안 됨. 실제 재인덱싱하려면 컬렉션을 drop & recreate 해야 함(현재 미실행).
- **프로덕션(EC2) ChromaDB는 미반영** — 로컬 코드만 변경. 실제 서빙 반영은 나중에 컬렉션 재생성 + 재인덱싱 필요.
- 청킹 개선이 검색에 실제로 도움 되는지 숫자로 증명하려면, 지금처럼 쉬운 골든셋(0.905가 나옴) 대신 **더 어려운 골든셋**을 만드는 게 맞는 방향(별개 작업).

### 후속: 재인덱싱 + 골든셋 리매핑 + 재평가 결과 (2026-07-14)

위 청킹 재설계를 실제로 적용(재인덱싱)하고 골든셋을 리매핑한 뒤 Recall@5를 재측정했다. **핵심 발견: 청킹 코드 품질은 개선됐지만 Recall@5는 겉보기로 0.944→0.778로 떨어졌고, 조사 결과 이 하락의 대부분은 검색 성능 저하가 아니라 골든셋/eval 아티팩트였다.** 상세는 EVAL.md 10절.

- **동일 18개 항목 apples-to-apples**: 구 청킹 0.944(17/18) vs 신 청킹 0.778(14/18). 겉보기 -0.166.
- **하지만 미스 4건 중 3건(ret_003·008·022)은 검색이 정답 내용을 rank1로 정확히 반환**하는데, 골든셋 `chunk_preview[:80]`이 구 청크 경계에서 추출된 스냅샷이라 새 청크 경계가 그 80자 창을 갈라 exact substring 매칭만 실패한 것. 나머지 1건(ret_015)은 구·신 양쪽에서 미스되는 기존 문제(청킹 무관).
- **교훈 1**: `eval_retrieval()`의 `preview[:80] in chunk_text` 판정은 청크 경계·overlap에 과민해서 **서로 다른 청킹 스킴을 공정하게 비교하지 못한다**. 골든셋 preview가 특정 청킹의 산물이라, 청킹을 바꾸면 골든셋도 함께 재보정(re-anchor)하지 않는 한 인위적 하락이 나온다.
- **교훈 2**: 재인덱싱 스킵 로직이 `source`(파일명) 기준이라, 청킹 로직만 바꾸고 인덱싱 스크립트를 재실행하면 "이미 적재됨"으로 스킵된다 — 반드시 컬렉션을 drop & recreate 해야 반영된다.
- **교훈 3**: 이미 목표(0.8)를 넘긴 지표를 "품질 개선" 명목으로 건드리면, 개선이 숫자로 안 보이는 데다 골든셋 재보정 비용까지 든다. 사전에 "이건 지표가 아니라 코드 품질을 고치는 작업"이라고 성격을 못박고, 재평가에서 하락이 나와도 원인(아티팩트 vs 실제 저하)을 분리 조사할 것.
- **최종 상태(1차 리매핑 시점)**: 신 청킹 코드는 유지(커밋 28d250c). 골든셋은 `expected_chunk_id` 19개 갱신 + 경계 아티팩트/합성 3개(ret_009·013·019) `reviewed:false` 플래그. 프로덕션 EC2 ChromaDB는 미반영(로컬만).

### 후속: 골든셋 앵커 재보정(ret_003·008·022) — 아티팩트 확정 (2026-07-14)

위 3건(ret_003·008·022)의 `chunk_preview`를, 검색기가 실제로 rank1로 반환한 청크 원문에서
완전히 포함되는 문장 단위로 재작성하고 `expected_chunk_id`를 그 청크로 갱신(원문 그대로 슬라이스,
`query`는 불변, 사람 재검수 전이라 `reviewed:false` 유지). 동일 18개 항목으로 재측정한 결과
**Recall@5=0.944(17/18), MRR=0.780**으로 구 청킹 수치(0.944)와 동일하게 회복됐다(MISS는 청킹과
무관한 ret_015만 남음).

정리하면: 검색 성능 자체는 청킹 변경 전후로 줄곧 동일했고, 리매핑 직후 관측된 0.778은 골든셋
`chunk_preview[:80]` 채점 방식이 청킹 경계 변화에 취약해 생긴 측정 아티팩트였음이 이번 재앵커링으로
확정됐다. 0.944는 "개선"이 아니라 "원래 문제없었음의 확인"이다. 상세 데이터는 EVAL.md 10절 참고.

- **최종 상태(1차 재앵커링 시점)**: `ret_003`·`ret_008`·`ret_022`는 재앵커링 완료, `reviewed:false`(사람 재검수 대기).
  `ret_009`·`ret_013`·`ret_019`는 이번 작업 범위 밖 — 여전히 미착수. 프로덕션 EC2 ChromaDB는 미반영.

### 후속: ret_009·013·019 재보정 — 골든셋 22개 중 21개 정합성 확보 (2026-07-14)

남겨뒀던 나머지 3건도 마저 정리했다.

- `ret_009`·`ret_019`: 위 3건과 동일한 유형(청킹 재설계 후 `expected_chunk_id`가 존재하지 않는 옛
  ID가 되고 preview가 청크 경계에 걸침) — 검색기 실제 rank1 청크 원문에서 preview 재작성.
- `ret_013`: 성격이 달랐다. `query`·`chunk_preview`가 **원문에 존재하지 않는 내용으로 합성**돼
  있던 항목(청킹과 무관, 훨씬 이전부터 깨져있었음). 원문 전체를 뒤져도 "생기부에 개인정보를
  기재하지 않는다"는 일반 규정 문장 자체가 없어, `query`를 실제 존재하는 규정
  "정정대장 제공 시 학생 개인정보 제외 대상"으로 교체하고, 그 내용이 실제로 담긴 청크로
  `expected_chunk_id`/`chunk_preview`를 새로 작성(이 query로는 검색기가 정확히 rank1 반환 확인).

22개 전체 재측정 결과 **Recall@5=0.955(21/22), MRR=0.789**. 유일한 MISS는 `ret_015`
(청킹과 무관한 기존 문제로 판단되어 의도적으로 미착수 — 검색기가 실제로 이 질문의 정답을
top-5 안에서 못 찾는 진짜 리트리버 이슈일 가능성이 있어 골든셋 수정이 아니라 별도 조사가 필요).

- **최종 상태**: `ret_003`·`ret_008`·`ret_009`·`ret_013`·`ret_019`·`ret_022` 재보정 완료,
  전부 `reviewed:false`(사람 재검수 대기, `ret_013`은 `query` 변경도 포함). `ret_014`는 이번
  작업과 무관하게 이미 `reviewed:false` 상태였던 별개 항목. `ret_015`는 의도적 미착수.
  프로덕션 EC2 ChromaDB는 미반영.
